// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/argil__avatars__audio-to-video.json
//   data/fal/openapi/fal-ai__bytedance__omnihuman__v1.5.json
//   data/fal/openapi/fal-ai__echomimic-v3.json
//   data/fal/openapi/fal-ai__kling-video__ai-avatar__v2__pro.json
//   data/fal/openapi/fal-ai__kling-video__ai-avatar__v2__standard.json
//   data/fal/openapi/fal-ai__longcat-single-avatar__image-audio-to-video.json
//   data/fal/openapi/fal-ai__sync-lipsync__v3__image-to-video.json
//   data/fal/openapi/veed__avatars__audio-to-video.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The ONE request schema for every `fal.avatar` endpoint.
 *
 * One `z.looseObject` for the whole category, not 8 per-endpoint schemas: zod objects are
 * built eagerly, and a hundred of them would be constructed on import for the one the
 * caller actually used.
 *
 * Every field is optional here and every bound is absent. That is not laxity, it is the
 * division of labour — this schema is the SHAPE gate (is `prompt` a string?), and what
 * each endpoint requires, ranges and enumerates lives in `FAL_AVATAR_SHAPES`, where the
 * hand check battery can turn a violation into a message that names the endpoint and cites
 * its own documentation.
 *
 * `looseObject` because fal ships new parameters between refreshes and a caller who reads
 * the release notes before we do should not be blocked; unknown keys are reported as
 * warnings by the hand battery instead.
 */

import { z } from "zod";

export const falAvatarInputSchema = z.looseObject({
  /**
   * The endpoint to submit to — unmodel's route selector, not a fal body field.
   *
   * Stripped in `finalize` and interpolated into `.request.url`; fal never receives it. It
   * is declared here only so the pipeline does not report the one required parameter as an
   * unknown one. The selector is `endpoint` rather than `model` because `model` is a REAL
   * wire field on several fal endpoints.
   */
  endpoint: z.string(),
  /**
   * Every `fal.avatar` endpoint types `audio_guidance_scale` as number, but they disagree on
   * the details (fal-ai/echomimic-v3, fal-ai/longcat-single-avatar/image-audio-to-video), so
   * the union takes the bare type and the exact vocabulary is enforced per endpoint from
   * FAL_AVATAR_SHAPES.
   */
  audio_guidance_scale: z.number().optional(),
  /**
   * Every `fal.avatar` endpoint types `audio_url` as string, but they disagree on the
   * details (argil/avatars/audio-to-video, fal-ai/bytedance/omnihuman/v1.5,
   * fal-ai/echomimic-v3, fal-ai/kling-video/ai-avatar/v2/pro,
   * fal-ai/kling-video/ai-avatar/v2/standard,
   * fal-ai/longcat-single-avatar/image-audio-to-video,
   * fal-ai/sync-lipsync/v3/image-to-video, veed/avatars/audio-to-video), so the union takes
   * the bare type and the exact vocabulary is enforced per endpoint from FAL_AVATAR_SHAPES.
   */
  audio_url: z.string().optional(),
  avatar: z.enum(["Mia outdoor (UGC)", "Lara (Masterclass)", "Ines (UGC)", "Maria (Masterclass)", "Emma (UGC)", "Sienna (Masterclass)", "Elena (UGC)", "Jasmine (Masterclass)", "Amara (Masterclass)", "Ryan podcast (UGC)", "Tyler (Masterclass)", "Jayse (Masterclass)", "Paul (Masterclass)", "Matteo (UGC)", "Daniel car (UGC)", "Dario (Masterclass)", "Viva (Masterclass)", "Chen (Masterclass)", "Alex (Masterclass)", "Vanessa (UGC)", "Laurent (UGC)", "Noemie car (UGC)", "Brandon (UGC)", "Byron (Masterclass)", "Calista (Masterclass)", "Milo (Masterclass)", "Fabien (Masterclass)", "Rose (UGC)"]).optional(),
  avatar_id: z.enum(["emily_vertical_primary", "emily_vertical_secondary", "marcus_vertical_primary", "marcus_vertical_secondary", "mira_vertical_primary", "mira_vertical_secondary", "jasmine_vertical_primary", "jasmine_vertical_secondary", "jasmine_vertical_walking", "aisha_vertical_walking", "elena_vertical_primary", "elena_vertical_secondary", "any_male_vertical_primary", "any_female_vertical_primary", "any_male_vertical_secondary", "any_female_vertical_secondary", "any_female_vertical_walking", "emily_primary", "emily_side", "marcus_primary", "marcus_side", "aisha_walking", "elena_primary", "elena_side", "any_male_primary", "any_female_primary", "any_male_side", "any_female_side"]).optional(),
  enable_prompt_expansion: z.boolean().optional(),
  enable_safety_checker: z.boolean().optional(),
  guidance_scale: z.number().optional(),
  image_url: z.string().optional(),
  mask_url: z.string().nullable().optional(),
  negative_prompt: z.string().optional(),
  num_frames_per_generation: z.number().optional(),
  num_inference_steps: z.number().optional(),
  num_segments: z.number().optional(),
  prompt: z.string().nullable().optional(),
  remove_background: z.boolean().optional(),
  /**
   * Every `fal.avatar` endpoint types `resolution` as string, but they disagree on the
   * details (fal-ai/bytedance/omnihuman/v1.5,
   * fal-ai/longcat-single-avatar/image-audio-to-video), so the union takes the bare type and
   * the exact vocabulary is enforced per endpoint from FAL_AVATAR_SHAPES.
   */
  resolution: z.string().optional(),
  seed: z.number().nullable().optional(),
  text_guidance_scale: z.number().optional(),
  turbo_mode: z.boolean().optional(),
});
