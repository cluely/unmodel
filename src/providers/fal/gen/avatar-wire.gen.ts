// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/argil__avatars__audio-to-video.json
//   data/fal/openapi/fal-ai__bytedance__omnihuman__v1.5.json
//   data/fal/openapi/fal-ai__echomimic-v3.json
//   data/fal/openapi/fal-ai__kling-video__ai-avatar__v2__pro.json
//   data/fal/openapi/fal-ai__kling-video__ai-avatar__v2__standard.json
//   data/fal/openapi/fal-ai__longcat-single-avatar__image-audio-to-video.json
//   data/fal/openapi/fal-ai__sync-lipsync__v3__image-to-video.json
//   data/fal/openapi/veed__avatars__audio-to-video.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.avatar` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalFile,
  FalVideo,
} from "./shared.gen";

/**
 * The request body `argil/avatars/audio-to-video` accepts.
 *
 * Argil Avatars — the second preset-performer route, and the second witness that `image`
 * is a per-model fact in this category.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `Argil/avatars/audio-to-video` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/Argil/avatars/audio-to-video/api
 */
export interface ArgilAvatarsAudioToVideoInput {
  avatar: "Mia outdoor (UGC)" | "Lara (Masterclass)" | "Ines (UGC)" | "Maria (Masterclass)" | "Emma (UGC)" | "Sienna (Masterclass)" | "Elena (UGC)" | "Jasmine (Masterclass)" | "Amara (Masterclass)" | "Ryan podcast (UGC)" | "Tyler (Masterclass)" | "Jayse (Masterclass)" | "Paul (Masterclass)" | "Matteo (UGC)" | "Daniel car (UGC)" | "Dario (Masterclass)" | "Viva (Masterclass)" | "Chen (Masterclass)" | "Alex (Masterclass)" | "Vanessa (UGC)" | "Laurent (UGC)" | "Noemie car (UGC)" | "Brandon (UGC)" | "Byron (Masterclass)" | "Calista (Masterclass)" | "Milo (Masterclass)" | "Fabien (Masterclass)" | "Rose (UGC)";
  /** Carries a audio reference — an https URL or a `data:` URI. */
  audio_url: string;
  /**
   * Enabling the remove background feature will result in a 50% increase in the price.
   * Default: `false`.
   */
  remove_background?: boolean;
}

/**
 * The result document `argil/avatars/audio-to-video` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface ArgilAvatarsAudioToVideoOutput {
  video?: FalVideo | null;
  /** Default: `false`. */
  moderation_flagged?: boolean;
  moderation_transcription?: string | null;
  moderation_error?: string | null;
}

/**
 * The request body `fal-ai/bytedance/omnihuman/v1.5` accepts.
 *
 * OmniHuman 1.5 — a still, an audio track, and an optional `mask_url` limiting what moves.
 *
 * Docs: https://fal.ai/models/fal-ai/bytedance/omnihuman/v1.5/api
 */
export interface FalAiBytedanceOmnihumanV15Input {
  /** The text prompt used to guide the video generation. */
  prompt?: string | null;
  /**
   * The URL of the image used to generate the video. Carries a image reference — an https
   * URL or a `data:` URI.
   */
  image_url: string;
  /**
   * The URL of the mask image to apply to the image. Only the person in the white area of
   * the mask will speak. Carries a image reference — an https URL or a `data:` URI.
   */
  mask_url?: string | null;
  /**
   * The URL of the audio file to generate the video. Audio must be under 30s long for 1080p
   * generation and under 60s long for 720p generation. Carries a audio reference — an https
   * URL or a `data:` URI.
   */
  audio_url: string;
  /** Generate a video at a faster rate with a slight quality trade-off. Default: `false`. */
  turbo_mode?: boolean;
  /**
   * The resolution of the generated video. Defaults to 1080p. 720p generation is faster and
   * higher in quality. 1080p generation is limited to 30s audio and 720p generation is
   * limited to 60s audio. Default: `"1080p"`.
   */
  resolution?: "720p" | "1080p";
}

/**
 * The result document `fal-ai/bytedance/omnihuman/v1.5` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiBytedanceOmnihumanV15Output {
  /** Generated video file. */
  video: FalFile;
  /** Duration of audio input/video output as used for billing. */
  duration: number;
}

/**
 * The request body `fal-ai/echomimic-v3` accepts.
 *
 * EchoMimic v3. The one avatar endpoint whose `prompt` is REQUIRED, which is why the
 * category's prompt is a per-model extra rather than a canonical word — three of the eight
 * rows have no prompt at all.
 *
 * Docs: https://fal.ai/models/fal-ai/echomimic-v3/api
 */
export interface FalAiEchomimicV3Input {
  /**
   * The URL of the image to use as a reference for the video generation. Carries a image
   * reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /**
   * The URL of the audio to use as a reference for the video generation. Carries a audio
   * reference — an https URL or a `data:` URI.
   */
  audio_url: string;
  /** The prompt to use for the video generation. */
  prompt: string;
  /** The negative prompt to use for the video generation. Default: `""`. */
  negative_prompt?: string;
  /** The number of frames to generate at once. Default: `121`. */
  num_frames_per_generation?: number;
  /** The guidance scale to use for the video generation. Default: `4.5`. */
  guidance_scale?: number;
  /** The audio guidance scale to use for the video generation. Default: `2.5`. */
  audio_guidance_scale?: number;
  /** The seed to use for the video generation. */
  seed?: number | null;
}

/**
 * The result document `fal-ai/echomimic-v3` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiEchomimicV3Output {
  /** The generated video file. */
  video: FalFile;
}

/**
 * The request body `fal-ai/kling-video/ai-avatar/v2/pro` accepts.
 *
 * Kling AI Avatar v2 Pro, at 2x the Standard rate.
 *
 * Docs: https://fal.ai/models/fal-ai/kling-video/ai-avatar/v2/pro/api
 */
export interface FalAiKlingVideoAiAvatarV2ProInput {
  /**
   * The URL of the image to use as your avatar. Carries a image reference — an https URL or
   * a `data:` URI.
   */
  image_url: string;
  /** The URL of the audio file. Carries a audio reference — an https URL or a `data:` URI. */
  audio_url: string;
  /** The prompt to use for the video generation. Default: `"."`. */
  prompt?: string;
}

/**
 * The result document `fal-ai/kling-video/ai-avatar/v2/pro` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKlingVideoAiAvatarV2ProOutput {
  /** The generated video. */
  video: FalFile;
  /** Duration of the output video in seconds. */
  duration: number;
}

/**
 * The request body `fal-ai/kling-video/ai-avatar/v2/standard` accepts.
 *
 * Kling AI Avatar v2 Standard. Its `prompt` defaults to ".", which is fal's way of
 * spelling "required by the wire, meaningless to the caller".
 *
 * Docs: https://fal.ai/models/fal-ai/kling-video/ai-avatar/v2/standard/api
 */
export interface FalAiKlingVideoAiAvatarV2StandardInput {
  /**
   * The URL of the image to use as your avatar. Carries a image reference — an https URL or
   * a `data:` URI.
   */
  image_url: string;
  /** The URL of the audio file. Carries a audio reference — an https URL or a `data:` URI. */
  audio_url: string;
  /** The prompt to use for the video generation. Default: `"."`. */
  prompt?: string;
}

/**
 * The result document `fal-ai/kling-video/ai-avatar/v2/standard` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKlingVideoAiAvatarV2StandardOutput {
  /** The generated video. */
  video: FalFile;
  /** Duration of the output video in seconds. */
  duration: number;
}

/**
 * The request body `fal-ai/longcat-single-avatar/image-audio-to-video` accepts.
 *
 * LongCat single-avatar, image + audio arm. Its /audio-to-video sibling is the still-less
 * arm and is not curated — one route per shape is enough.
 *
 * Docs: https://fal.ai/models/fal-ai/longcat-single-avatar/image-audio-to-video/api
 */
export interface FalAiLongcatSingleAvatarImageAudioToVideoInput {
  /**
   * The URL of the image to animate. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  image_url: string;
  /**
   * The URL of the audio file to drive the avatar. Carries a audio reference — an https URL
   * or a `data:` URI.
   */
  audio_url: string;
  /**
   * The prompt to guide the video generation. Default: `"A person is talking naturally with
   * natural expressions and movements."`.
   */
  prompt?: string;
  /**
   * The negative prompt to avoid in the video generation. Default: `"Close-up, Bright tones,
   * overexposed, static, blurred details, subtitles, style, works, paintings, images,
   * static, overall gray, worst quality, low quality, JPEG compression residue, ugly,
   * incomplete, extra fingers, poorly drawn hands, poorly drawn faces, deformed, disfigured,
   * misshapen limbs, fused fingers, still picture, messy background, three legs, many people
   * in the background, walking backwards"`.
   */
  negative_prompt?: string;
  /** The number of inference steps to use. Default: `50`. */
  num_inference_steps?: number;
  /** The text guidance scale for classifier-free guidance. Default: `4`. */
  text_guidance_scale?: number;
  /**
   * The audio guidance scale. Higher values may lead to exaggerated mouth movements.
   * Default: `4`.
   */
  audio_guidance_scale?: number;
  /**
   * Resolution of the generated video (480p or 720p). Billing is per video-second (16
   * frames): 480p is 1 unit per second and 720p is 4 units per second. Default: `"480p"`.
   */
  resolution?: "480p" | "720p";
  /**
   * Number of video segments to generate. Each segment adds ~5 seconds of video. First
   * segment is ~5.8s, additional segments are 5s each. Default: `1`.
   */
  num_segments?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /**
   * Whether to enable safety checker. Disabling it requires account authorization;
   * unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * Whether to enable prompt expansion using an LLM to enhance the prompt for better video
   * quality. Default: `false`.
   */
  enable_prompt_expansion?: boolean;
}

/**
 * The result document `fal-ai/longcat-single-avatar/image-audio-to-video` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiLongcatSingleAvatarImageAudioToVideoOutput {
  /** The generated video file. */
  video: FalFile;
  /** The seed used for generation. */
  seed: number;
}

/**
 * The request body `fal-ai/sync-lipsync/v3/image-to-video` accepts.
 *
 * The still-driven twin of sync-lipsync/v3 — image in, video out. The split that makes
 * lipsync and avatar separate packs rather than one `sources` arm.
 *
 * Docs: https://fal.ai/models/fal-ai/sync-lipsync/v3/image-to-video/api
 */
export interface FalAiSyncLipsyncV3ImageToVideoInput {
  /**
   * URL of the input image. sync-3 animates the face in the image so it lip-syncs to the
   * provided audio (image-to-video). Supported image formats: JPEG, PNG and WebP. Carries a
   * image reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /**
   * URL of the input audio. The output video matches its duration. Carries a audio reference
   * — an https URL or a `data:` URI.
   */
  audio_url: string;
}

/**
 * The result document `fal-ai/sync-lipsync/v3/image-to-video` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiSyncLipsyncV3ImageToVideoOutput {
  /** The generated video. */
  video: FalFile;
}

/**
 * The request body `veed/avatars/audio-to-video` accepts.
 *
 * VEED Avatars. Takes NO image: the performer is a catalogued `avatar_id` from a 28-value
 * enum. The row that forces `image` to be narrowed per model rather than required by the
 * vocabulary.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `VEED/avatars/audio-to-video` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/VEED/avatars/audio-to-video/api
 */
export interface VeedAvatarsAudioToVideoInput {
  /** The avatar to use for the video. */
  avatar_id: "emily_vertical_primary" | "emily_vertical_secondary" | "marcus_vertical_primary" | "marcus_vertical_secondary" | "mira_vertical_primary" | "mira_vertical_secondary" | "jasmine_vertical_primary" | "jasmine_vertical_secondary" | "jasmine_vertical_walking" | "aisha_vertical_walking" | "elena_vertical_primary" | "elena_vertical_secondary" | "any_male_vertical_primary" | "any_female_vertical_primary" | "any_male_vertical_secondary" | "any_female_vertical_secondary" | "any_female_vertical_walking" | "emily_primary" | "emily_side" | "marcus_primary" | "marcus_side" | "aisha_walking" | "elena_primary" | "elena_side" | "any_male_primary" | "any_female_primary" | "any_male_side" | "any_female_side";
  /** Carries a audio reference — an https URL or a `data:` URI. */
  audio_url: string;
}

/**
 * The result document `veed/avatars/audio-to-video` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface VeedAvatarsAudioToVideoOutput {
  video: FalFile;
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 8 object types re-instantiates at every comparison
 * site, while a keyed lookup resolves in one. The per-endpoint narrowing a validator
 * exposes indexes into this, so adding an endpoint costs one line here and nothing at any
 * call site.
 */
export interface FalAvatarBodyById {
  "argil/avatars/audio-to-video": ArgilAvatarsAudioToVideoInput;
  "fal-ai/bytedance/omnihuman/v1.5": FalAiBytedanceOmnihumanV15Input;
  "fal-ai/echomimic-v3": FalAiEchomimicV3Input;
  "fal-ai/kling-video/ai-avatar/v2/pro": FalAiKlingVideoAiAvatarV2ProInput;
  "fal-ai/kling-video/ai-avatar/v2/standard": FalAiKlingVideoAiAvatarV2StandardInput;
  "fal-ai/longcat-single-avatar/image-audio-to-video": FalAiLongcatSingleAvatarImageAudioToVideoInput;
  "fal-ai/sync-lipsync/v3/image-to-video": FalAiSyncLipsyncV3ImageToVideoInput;
  "veed/avatars/audio-to-video": VeedAvatarsAudioToVideoInput;
}

/** Endpoint id → its result document type. */
export interface FalAvatarResultById {
  "argil/avatars/audio-to-video": ArgilAvatarsAudioToVideoOutput;
  "fal-ai/bytedance/omnihuman/v1.5": FalAiBytedanceOmnihumanV15Output;
  "fal-ai/echomimic-v3": FalAiEchomimicV3Output;
  "fal-ai/kling-video/ai-avatar/v2/pro": FalAiKlingVideoAiAvatarV2ProOutput;
  "fal-ai/kling-video/ai-avatar/v2/standard": FalAiKlingVideoAiAvatarV2StandardOutput;
  "fal-ai/longcat-single-avatar/image-audio-to-video": FalAiLongcatSingleAvatarImageAudioToVideoOutput;
  "fal-ai/sync-lipsync/v3/image-to-video": FalAiSyncLipsyncV3ImageToVideoOutput;
  "veed/avatars/audio-to-video": VeedAvatarsAudioToVideoOutput;
}
