// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__nano-banana-2__edit.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The per-endpoint intermediate representation every `fal.imageEdit` check reads.
 *
 * This is the file that makes one union schema enough. The schema next door asks “is this
 * the right shape?”; these rows carry what each endpoint actually requires, ranges,
 * enumerates and treats as media, so the ONE hand-written check battery can narrow to the
 * endpoint at run time and compose a message that cites that endpoint's own documentation.
 *
 * Note what is NOT here: no per-endpoint deny table. The `props` keys ARE the allow-list —
 * an O(N×M) table of everything each endpoint refuses would be hundreds of kilobytes
 * saying what the keys already say.
 */

import type { FalEndpointShape } from "../shape-types";

// Enum vocabularies, hoisted and deduplicated by content: the same list is
// named by both tables below, and often by several endpoints.
const E_205460 = ["jpeg", "png", "webp"] as const;
const E_5ae5df = ["0.5K", "1K", "2K", "4K"] as const;
const E_652908 = ["minimal", "high"] as const;
const E_a008c0 = ["auto", "21:9", "16:9", "3:2", "4:3", "5:4", "1:1", "4:5", "3:4", "2:3", "9:16", "4:1", "1:4", "8:1", "1:8"] as const;
const E_f908b4 = ["1", "2", "3", "4", "5", "6"] as const;

export const FAL_IMAGE_EDIT_SHAPES = {
  "fal-ai/nano-banana-2/edit": {
    order: ["prompt", "num_images", "seed", "aspect_ratio", "output_format", "safety_tolerance", "sync_mode", "image_urls", "system_prompt", "resolution", "video_url", "audio_url", "pdf_url", "limit_generations", "enable_web_search", "thinking_level"],
    props: {
      prompt: { t: "string", req: true, minLen: 3, maxLen: 50000 },
      num_images: { t: "integer", def: true, min: 1, max: 4 },
      seed: { t: "integer", nul: true },
      aspect_ratio: { t: "string", nul: true, def: true, enum: E_a008c0 },
      output_format: { t: "string", def: true, enum: E_205460 },
      safety_tolerance: { t: "string", def: true, enum: E_f908b4 },
      sync_mode: { t: "boolean", def: true },
      image_urls: { t: "array", items: { t: "string" } },
      system_prompt: { t: "string", def: true, maxLen: 50000 },
      resolution: { t: "string", def: true, enum: E_5ae5df },
      video_url: { t: "string", nul: true },
      audio_url: { t: "string", nul: true },
      pdf_url: { t: "string", nul: true },
      limit_generations: { t: "boolean", def: true },
      enable_web_search: { t: "boolean", def: true },
      thinking_level: { t: "string", nul: true, enum: E_652908 },
    },
  },
} as const satisfies Record<string, FalEndpointShape>;

/**
 * Each endpoint's closed vocabularies, keyed by parameter.
 *
 * The same arrays the shapes above point at, addressed the way a picker wants them:
 * `unmodel/fal/values` publishes these so a UI can offer exactly what the endpoint
 * accepts.
 */
export const FAL_IMAGE_EDIT_CONSTRAINTS = {
  "fal-ai/nano-banana-2/edit": {
    aspect_ratio: E_a008c0,
    output_format: E_205460,
    safety_tolerance: E_f908b4,
    resolution: E_5ae5df,
    thinking_level: E_652908,
  },
} as const;
