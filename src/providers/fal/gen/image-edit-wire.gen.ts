// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/alibaba__qwen-image-3__edit.json
//   data/fal/openapi/bria__fibo-edit__relight.json
//   data/fal/openapi/bytedance__seedream__v5__pro__edit.json
//   data/fal/openapi/fal-ai__bytedance__seedream__v4.5__edit.json
//   data/fal/openapi/fal-ai__flux-2-pro__edit.json
//   data/fal/openapi/fal-ai__flux-2__edit.json
//   data/fal/openapi/fal-ai__flux-kontext__dev.json
//   data/fal/openapi/fal-ai__flux-pro__kontext.json
//   data/fal/openapi/fal-ai__flux-pro__kontext__max.json
//   data/fal/openapi/fal-ai__flux-pro__kontext__max__multi.json
//   data/fal/openapi/fal-ai__flux-pro__v1__fill.json
//   data/fal/openapi/fal-ai__flux__dev__image-to-image.json
//   data/fal/openapi/fal-ai__gpt-image-1.5__edit.json
//   data/fal/openapi/fal-ai__nano-banana-2__edit.json
//   data/fal/openapi/fal-ai__nano-banana-pro__edit.json
//   data/fal/openapi/fal-ai__nano-banana__edit.json
//   data/fal/openapi/fal-ai__qwen-image-edit-2511.json
//   data/fal/openapi/openai__gpt-image-2__edit.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.imageEdit` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalFile,
  FalImageFile,
  FalImageSize,
  FalImage_28cfa5,
  FalImage_d44dd8,
} from "./shared.gen";

/**
 * The request body `alibaba/qwen-image-3/edit` accepts.
 *
 * Qwen Image 3 editing, conditional on a 1K/2K `resolution`.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/qwen-image-3/edit` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/qwen-image-3/edit/api
 */
export interface AlibabaQwenImage3EditInput {
  /**
   * Text prompt describing the desired edit. Supports Chinese and English. Max 5000
   * characters.
   */
  prompt: string;
  /** Content to avoid in the generated image. Max 500 characters. Default: `""`. */
  negative_prompt?: string | null;
  /**
   * The size of the generated image. If not provided, the model determines the resolution
   * automatically. Total number of pixels must be between 512x512 and 2048x2048.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | null;
  /** Enable automatic LLM prompt rewriting for better results. Default: `true`. */
  enable_prompt_expansion?: boolean;
  /** Random seed for reproducibility (0-2147483647). */
  seed?: number | null;
  /**
   * Enable content moderation for input and output. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * Reference images for editing (1-3 images required). Order matters: reference as 'image
   * 1', 'image 2', 'image 3' in prompt. Resolution: 384-2048px each dimension. Max size:
   * 10MB each. Formats: JPEG, JPG, PNG (no alpha), WEBP. Carries a image reference — an
   * https URL or a `data:` URI.
   */
  image_urls: string[];
}

/**
 * The result document `alibaba/qwen-image-3/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface AlibabaQwenImage3EditOutput {
  /** Generated images. */
  images: FalFile[];
  /** The seed used for generation. */
  seed: number;
}

/**
 * The request body `bria/fibo-edit/relight` accepts.
 *
 * Fibo Edit [Relight]. Provider-native only: its request is prompt-less by construction
 * (`image_url`, `light_direction`, `light_type` are required), while the unified
 * image-edit vocabulary requires a prompt and would therefore send a field this endpoint
 * does not declare.
 *
 * Docs: https://fal.ai/models/bria/fibo-edit/relight/api
 */
export interface BriaFiboEditRelightInput {
  /** The source image. Carries a image reference — an https URL or a `data:` URI. */
  image_url: string;
  /** Where the light comes from. */
  light_direction: "front" | "side" | "bottom" | "top-down" | null;
  /** The quality/style/time of day. */
  light_type: "midday" | "blue hour light" | "low-angle sunlight" | "sunrise light" | "spotlight on subject" | "overcast light" | "soft overcast daylight lighting" | "cloud-filtered lighting" | "fog-diffused lighting" | "moonlight lighting" | "starlight nighttime" | "soft bokeh lighting" | "harsh studio lighting";
}

/**
 * The result document `bria/fibo-edit/relight` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface BriaFiboEditRelightOutput {
  /** Generated image. */
  image: FalImage_28cfa5;
  /** Generated images. Default: `[]`. */
  images?: FalImage_28cfa5[];
  /** Current instruction. */
  structured_instruction: Record<string, unknown>;
}

/**
 * The request body `bytedance/seedream/v5/pro/edit` accepts.
 *
 * Seedream 5.0 Pro editing. Priced by output area PLUS an additive surcharge per extra
 * input image.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/seedream-5-pro/edit` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/seedream-5-pro/edit/api
 */
export interface BytedanceSeedreamV5ProEditInput {
  /** The text prompt used to edit the image. */
  prompt: string;
  /**
   * The size of the generated image. Total pixels must be between 1024x1024 and 2048x2048,
   * with aspect ratio between 1/16 and 16. Default: `"auto_2K"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto_1K" | "auto_2K";
  /** Number of separate model generations to run with the prompt. Default: `1`. */
  num_images?: number;
  /** The file format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * List of URLs of input reference images. Up to 10 images are supported; if more are sent,
   * only the last 10 are used. Carries a image reference — an https URL or a `data:` URI.
   */
  image_urls: string[];
}

/**
 * The result document `bytedance/seedream/v5/pro/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface BytedanceSeedreamV5ProEditOutput {
  /** Generated images. */
  images: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/bytedance/seedream/v4.5/edit` accepts.
 *
 * Seedream 4.5 editing, flat per-image.
 *
 * Docs: https://fal.ai/models/fal-ai/bytedance/seedream/v4.5/edit/api
 */
export interface FalAiBytedanceSeedreamV45EditInput {
  /** The text prompt used to edit the image. */
  prompt: string;
  /**
   * The size of the generated image. Width and height must be between 1920 and 4096, or
   * total number of pixels must be between 2560*1440 and 4096*4096. Default:
   * `{"height":2048,"width":2048}`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto_2K" | "auto_4K";
  /** Number of separate model generations to be run with the prompt. Default: `1`. */
  num_images?: number;
  /**
   * If set to a number greater than one, enables multi-image generation. The model will
   * potentially return up to `max_images` images every generation, and in total,
   * `num_images` generations will be carried out. In total, the number of images generated
   * will be between `num_images` and `max_images*num_images`. The total number of images
   * (image inputs + image outputs) must not exceed 15. Default: `1`.
   */
  max_images?: number;
  /** Random seed to control the stochasticity of image generation. */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * List of URLs of input images for editing. Presently, up to 10 image inputs are allowed.
   * If over 10 images are sent, only the last 10 will be used. Carries a image reference —
   * an https URL or a `data:` URI.
   */
  image_urls: string[];
}

/**
 * The result document `fal-ai/bytedance/seedream/v4.5/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiBytedanceSeedreamV45EditOutput {
  /** Generated images. */
  images: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/flux-2-pro/edit` accepts.
 *
 * FLUX.2 [pro] editing, tiered per megapixel across input AND output.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-pro/edit/api
 */
export interface FalAiFlux2ProEditInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The size of the generated image. If `auto`, the size will be determined by the model.
   * Default: `"auto"`.
   */
  image_size?: FalImageSize | "auto" | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The seed to use for the generation. */
  seed?: number | null;
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5";
  /**
   * Whether to enable the safety checker. Disabling it requires account authorization;
   * unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * List of URLs of input images for editing. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_urls: string[];
}

/**
 * The result document `fal-ai/flux-2-pro/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2ProEditOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The seed used for the generation. */
  seed: number;
}

/**
 * The request body `fal-ai/flux-2/edit` accepts.
 *
 * FLUX.2 [dev] editing — billed on input and output megapixels, with inputs resized to 1MP
 * first.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/flux-2-dev/edit` its OpenAPI document is written against. unmodel submits
 * to the published id — that is the one fal documents and the one this catalog is keyed
 * on.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-dev/edit/api
 */
export interface FalAiFlux2EditInput {
  /** The prompt to edit the image. */
  prompt: string;
  /**
   * Guidance Scale is a measure of how close you want the model to stick to your prompt when
   * looking for a related image to show you. Default: `2.5`.
   */
  guidance_scale?: number;
  /** The seed to use for the generation. If not provided, a random seed will be used. */
  seed?: number | null;
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The size of the image to generate. The width and height must be between 512 and 2048
   * pixels.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | null;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The acceleration level to use for the image generation. Default: `"regular"`. */
  acceleration?: "none" | "regular" | "high";
  /** If set to true, the prompt will be expanded for better results. Default: `false`. */
  enable_prompt_expansion?: boolean;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The URLs of the images for editing. A maximum of 4 images are allowed, if more are
   * provided, only the first 4 will be used. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_urls: string[];
}

/**
 * The result document `fal-ai/flux-2/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2EditOutput {
  /** The edited images. */
  images: FalImageFile[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-kontext/dev` accepts.
 *
 * The open-weights Kontext.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/flux-kontext-lora/dev` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-kontext-lora/dev/api
 */
export interface FalAiFluxKontextDevInput {
  /** The prompt to edit the image. */
  prompt: string;
  /**
   * The URL of the image to edit. Max width: 14142px, Max height: 14142px, Timeout: 20s.
   * Carries a image reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `2.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** Output format. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
  /**
   * Determines how the output resolution is set for image editing. - `auto`: The model
   * selects an optimal resolution from a predefined set that best matches the input image's
   * aspect ratio. This is the recommended setting for most use cases as it's what the model
   * was trained on. - `match_input`: The model will attempt to use the same resolution as
   * the input image. The resolution will be adjusted to be compatible with the model's
   * requirements (e.g. dimensions must be multiples of 16 and within supported limits).
   * Apart from these, a few aspect ratios are also supported. Default: `"match_input"`.
   */
  resolution_mode?: "auto" | "match_input" | "1:1" | "16:9" | "21:9" | "3:2" | "2:3" | "4:5" | "5:4" | "3:4" | "4:3" | "9:16" | "9:21";
}

/**
 * The result document `fal-ai/flux-kontext/dev` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxKontextDevOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/kontext` accepts.
 *
 * FLUX.1 Kontext [pro] — single-image instruction editing. unmodel also serves Kontext
 * first-party at `unmodel/black-forest-labs`.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/kontext/api
 */
export interface FalAiFluxProKontextInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
  /** The aspect ratio of the generated image. */
  aspect_ratio?: "21:9" | "16:9" | "4:3" | "3:2" | "1:1" | "2:3" | "3:4" | "9:16" | "9:21" | null;
  /**
   * Image prompt for the omni model. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  image_url: string;
}

/**
 * The result document `fal-ai/flux-pro/kontext` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProKontextOutput {
  /** The generated image files info. */
  images: FalImage_28cfa5[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/kontext/max` accepts.
 *
 * FLUX.1 Kontext [max].
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/kontext/max/api
 */
export interface FalAiFluxProKontextMaxInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
  /** The aspect ratio of the generated image. */
  aspect_ratio?: "21:9" | "16:9" | "4:3" | "3:2" | "1:1" | "2:3" | "3:4" | "9:16" | "9:21" | null;
  /**
   * Image prompt for the omni model. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  image_url: string;
}

/**
 * The result document `fal-ai/flux-pro/kontext/max` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProKontextMaxOutput {
  /** The generated image files info. */
  images: FalImage_28cfa5[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/kontext/max/multi` accepts.
 *
 * Kontext [max] multi — takes `image_urls` (a LIST) where its siblings take a single
 * `image_url`. The multi-image arm the media naming rule has to classify as an array of
 * image refs.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/kontext/max/multi/api
 */
export interface FalAiFluxProKontextMaxMultiInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
  /** The aspect ratio of the generated image. */
  aspect_ratio?: "21:9" | "16:9" | "4:3" | "3:2" | "1:1" | "2:3" | "3:4" | "9:16" | "9:21" | null;
  /**
   * Image prompt for the omni model. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  image_urls: string[];
}

/**
 * The result document `fal-ai/flux-pro/kontext/max/multi` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProKontextMaxMultiOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/v1/fill` accepts.
 *
 * FLUX.1 [pro] fill — inpainting, so it carries a `mask_url` alongside `image_url`. The
 * second media kind in this category.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/v1/fill/api
 */
export interface FalAiFluxProV1FillInput {
  /** The prompt to fill the masked part of the image. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
  /**
   * The image URL to generate an image from. Needs to match the dimensions of the mask.
   * Carries a image reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /**
   * The mask URL to inpaint the image. Needs to match the dimensions of the input image.
   * Carries a image reference — an https URL or a `data:` URI.
   */
  mask_url: string;
}

/**
 * The result document `fal-ai/flux-pro/v1/fill` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProV1FillOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux/dev/image-to-image` accepts.
 *
 * FLUX.1 [dev] image-to-image — a `strength` re-render rather than an instruction edit.
 *
 * Docs: https://fal.ai/models/fal-ai/flux/dev/image-to-image/api
 */
export interface FalAiFluxDevImageToImageInput {
  /**
   * The URL of the image to generate an image from. Carries a image reference — an https URL
   * or a `data:` URI.
   */
  image_url: string;
  /**
   * The strength of the initial image. Higher strength values are better for this model.
   * Default: `0.95`.
   */
  strength?: number;
  /** The number of inference steps to perform. Default: `40`. */
  num_inference_steps?: number;
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/flux/dev/image-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxDevImageToImageOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/gpt-image-1.5/edit` accepts.
 *
 * The edit arm of gpt-image-1.5.
 *
 * Docs: https://fal.ai/models/fal-ai/gpt-image-1.5/edit/api
 */
export interface FalAiGptImage15EditInput {
  /** The prompt for image generation. */
  prompt: string;
  /**
   * The URLs of the images to use as a reference for the generation. Carries a image
   * reference — an https URL or a `data:` URI.
   */
  image_urls: string[];
  /** Aspect ratio for the generated image. Default: `"auto"`. */
  image_size?: "auto" | "1024x1024" | "1536x1024" | "1024x1536";
  /** Background for the generated image. Default: `"auto"`. */
  background?: "auto" | "transparent" | "opaque";
  /** Quality for the generated image. Default: `"high"`. */
  quality?: "low" | "medium" | "high";
  /** Input fidelity for the generated image. Default: `"high"`. */
  input_fidelity?: "low" | "high";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Output format for the images. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The URL of the mask image to use for the generation. This indicates what part of the
   * image to edit. Carries a image reference — an https URL or a `data:` URI.
   */
  mask_image_url?: string | null;
}

/**
 * The result document `fal-ai/gpt-image-1.5/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiGptImage15EditOutput {
  /** The generated images. */
  images: FalImageFile[];
}

/**
 * The request body `fal-ai/nano-banana-2/edit` accepts.
 *
 * aspectRatioEnum + resolutionEnum, and a multi-image `image_urls` array.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana-2/edit/api
 */
export interface FalAiNanoBanana2EditInput {
  /** The prompt for image editing. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /**
   * The aspect ratio of the generated image. Supports extreme ratios: 4:1, 1:4, 8:1, 1:8.
   * Default: `"auto"`.
   */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | "4:1" | "1:4" | "8:1" | "1:8" | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The URLs of the images to use for image-to-image generation or image editing. Optional
   * when at least one of `video_url`, `audio_url`, or `pdf_url` is provided. Carries a image
   * reference — an https URL or a `data:` URI.
   */
  image_urls?: string[];
  /**
   * Optional system instruction that steers the model's persona and output style across the
   * request. Leave blank to omit; when provided, it is sent as the system instruction to
   * Gemini (or as a system message on OpenAI-compatible providers). Default: `""`.
   */
  system_prompt?: string;
  /** The resolution of the image to generate. Default: `"1K"`. */
  resolution?: "0.5K" | "1K" | "2K" | "4K";
  /**
   * Optional video to use as input context. Accepts an http(s)/data URL to a video file
   * (downloaded and sent inline, max 15MB) or a YouTube URL (passed through to the model
   * without downloading). Carries a video reference — an https URL or a `data:` URI.
   */
  video_url?: string | null;
  /**
   * Optional audio file to use as input context. Accepts an http(s)/data URL (downloaded and
   * sent inline, max 15MB). Carries a audio reference — an https URL or a `data:` URI.
   */
  audio_url?: string | null;
  /**
   * Optional PDF document to use as input context. Accepts an http(s)/data URL (downloaded
   * and sent inline, max 15MB).
   */
  pdf_url?: string | null;
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate and ignore any intermediate images generated by the model. This
   * may affect generation quality. Default: `true`.
   */
  limit_generations?: boolean;
  /**
   * Enable web search for the image generation task. This will allow the model to use the
   * latest information from the web to generate the image. Default: `false`.
   */
  enable_web_search?: boolean;
  /**
   * When set, enables model thinking with the given level ('minimal' or 'high') and includes
   * thoughts in the generation. Omit to disable.
   */
  thinking_level?: "minimal" | "high" | null;
}

/**
 * The result document `fal-ai/nano-banana-2/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBanana2EditOutput {
  /** The edited images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/nano-banana-pro/edit` accepts.
 *
 * The edit arm of nano-banana-pro, conditional on `resolution`.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana-pro/edit/api
 */
export interface FalAiNanoBananaProEditInput {
  /** The prompt for image editing. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /** The aspect ratio of the generated image. Default: `"auto"`. */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The URLs of the images to use for image-to-image generation or image editing. Carries a
   * image reference — an https URL or a `data:` URI.
   */
  image_urls: string[];
  /**
   * Optional system instruction that steers the model's persona and output style across the
   * request. Leave blank to omit; when provided, it is sent as the system instruction to
   * Gemini (or as a system message on OpenAI-compatible providers). Default: `""`.
   */
  system_prompt?: string;
  /** The resolution of the image to generate. Default: `"1K"`. */
  resolution?: "1K" | "2K" | "4K";
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate. Default: `true`.
   */
  limit_generations?: boolean;
  /**
   * Enable web search for the image generation task. This will allow the model to use the
   * latest information from the web to generate the image. Default: `false`.
   */
  enable_web_search?: boolean;
}

/**
 * The result document `fal-ai/nano-banana-pro/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBananaProEditOutput {
  /** The edited images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/nano-banana/edit` accepts.
 *
 * The edit arm of the original Nano Banana, flat per-image.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana/edit/api
 */
export interface FalAiNanoBananaEditInput {
  /** The prompt for image editing. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /** The aspect ratio of the generated image. Default: `"auto"`. */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The URLs of the images to use for image-to-image generation or image editing. Carries a
   * image reference — an https URL or a `data:` URI.
   */
  image_urls: string[];
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate. Default: `true`.
   */
  limit_generations?: boolean;
}

/**
 * The result document `fal-ai/nano-banana/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBananaEditOutput {
  /** The edited images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/qwen-image-edit-2511` accepts.
 *
 * Qwen-Image-Edit (2511).
 *
 * Docs: https://fal.ai/models/fal-ai/qwen-image-edit-2511/api
 */
export interface FalAiQwenImageEdit2511Input {
  /** The prompt to edit the image with. */
  prompt: string;
  /** The negative prompt to generate an image from. Default: `""`. */
  negative_prompt?: string;
  /** The size of the generated image. If None, uses the input image dimensions. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | null;
  /**
   * The URLs of the images to edit. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  image_urls: string[];
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /** The guidance scale to use for the image generation. Default: `4.5`. */
  guidance_scale?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /** If `True`, the media will be returned as a data URI. Default: `false`. */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /** The acceleration level to use. Default: `"regular"`. */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/qwen-image-edit-2511` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiQwenImageEdit2511Output {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `openai/gpt-image-2/edit` accepts.
 *
 * The edit arm of gpt-image-2. Its per-size table INCLUDES one input image, which is why
 * its figures differ from the text-to-image sibling's.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/gpt-image-2/edit` its OpenAPI document is written against. unmodel submits
 * to the published id — that is the one fal documents and the one this catalog is keyed
 * on.
 *
 * Docs: https://fal.ai/models/fal-ai/gpt-image-2/edit/api
 */
export interface OpenaiGptImage2EditInput {
  /** The prompt for image generation. */
  prompt: string;
  /**
   * The URLs of the images to use as a reference for the generation. A maximum of 16 images
   * are allowed. Carries a image reference — an https URL or a `data:` URI.
   */
  image_urls: string[];
  /**
   * The size of the generated image. Use 'auto' to infer from input images. Default:
   * `"auto"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto";
  /** Background for the generated image. Default: `"auto"`. */
  background?: "auto" | "transparent" | "opaque";
  /**
   * Quality for the generated image. Use 'auto' to let the model pick the best quality for
   * the prompt. Default: `"high"`.
   */
  quality?: "auto" | "low" | "medium" | "high";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Output format for the images. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The URL of the mask image to use for the generation. This indicates what part of the
   * image to edit. Carries a image reference — an https URL or a `data:` URI.
   */
  mask_url?: string | null;
}

/**
 * The result document `openai/gpt-image-2/edit` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface OpenaiGptImage2EditOutput {
  /** The generated images. */
  images: FalImageFile[];
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 18 object types re-instantiates at every
 * comparison site, while a keyed lookup resolves in one. The per-endpoint narrowing a
 * validator exposes indexes into this, so adding an endpoint costs one line here and
 * nothing at any call site.
 */
export interface FalImageEditBodyById {
  "alibaba/qwen-image-3/edit": AlibabaQwenImage3EditInput;
  "bria/fibo-edit/relight": BriaFiboEditRelightInput;
  "bytedance/seedream/v5/pro/edit": BytedanceSeedreamV5ProEditInput;
  "fal-ai/bytedance/seedream/v4.5/edit": FalAiBytedanceSeedreamV45EditInput;
  "fal-ai/flux-2-pro/edit": FalAiFlux2ProEditInput;
  "fal-ai/flux-2/edit": FalAiFlux2EditInput;
  "fal-ai/flux-kontext/dev": FalAiFluxKontextDevInput;
  "fal-ai/flux-pro/kontext": FalAiFluxProKontextInput;
  "fal-ai/flux-pro/kontext/max": FalAiFluxProKontextMaxInput;
  "fal-ai/flux-pro/kontext/max/multi": FalAiFluxProKontextMaxMultiInput;
  "fal-ai/flux-pro/v1/fill": FalAiFluxProV1FillInput;
  "fal-ai/flux/dev/image-to-image": FalAiFluxDevImageToImageInput;
  "fal-ai/gpt-image-1.5/edit": FalAiGptImage15EditInput;
  "fal-ai/nano-banana-2/edit": FalAiNanoBanana2EditInput;
  "fal-ai/nano-banana-pro/edit": FalAiNanoBananaProEditInput;
  "fal-ai/nano-banana/edit": FalAiNanoBananaEditInput;
  "fal-ai/qwen-image-edit-2511": FalAiQwenImageEdit2511Input;
  "openai/gpt-image-2/edit": OpenaiGptImage2EditInput;
}

/** Endpoint id → its result document type. */
export interface FalImageEditResultById {
  "alibaba/qwen-image-3/edit": AlibabaQwenImage3EditOutput;
  "bria/fibo-edit/relight": BriaFiboEditRelightOutput;
  "bytedance/seedream/v5/pro/edit": BytedanceSeedreamV5ProEditOutput;
  "fal-ai/bytedance/seedream/v4.5/edit": FalAiBytedanceSeedreamV45EditOutput;
  "fal-ai/flux-2-pro/edit": FalAiFlux2ProEditOutput;
  "fal-ai/flux-2/edit": FalAiFlux2EditOutput;
  "fal-ai/flux-kontext/dev": FalAiFluxKontextDevOutput;
  "fal-ai/flux-pro/kontext": FalAiFluxProKontextOutput;
  "fal-ai/flux-pro/kontext/max": FalAiFluxProKontextMaxOutput;
  "fal-ai/flux-pro/kontext/max/multi": FalAiFluxProKontextMaxMultiOutput;
  "fal-ai/flux-pro/v1/fill": FalAiFluxProV1FillOutput;
  "fal-ai/flux/dev/image-to-image": FalAiFluxDevImageToImageOutput;
  "fal-ai/gpt-image-1.5/edit": FalAiGptImage15EditOutput;
  "fal-ai/nano-banana-2/edit": FalAiNanoBanana2EditOutput;
  "fal-ai/nano-banana-pro/edit": FalAiNanoBananaProEditOutput;
  "fal-ai/nano-banana/edit": FalAiNanoBananaEditOutput;
  "fal-ai/qwen-image-edit-2511": FalAiQwenImageEdit2511Output;
  "openai/gpt-image-2/edit": OpenaiGptImage2EditOutput;
}
