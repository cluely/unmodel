// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__flux__dev.json
//   data/fal/openapi/fal-ai__flux__schnell.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * How each `fal.image` endpoint lets a caller state geometry and duration, and which wire
 * keys it takes.
 *
 * `classes` is what the unified adapter branches on. One branch per shape class, never one
 * per endpoint: at a hundred endpoints a per-endpoint switch is both unreadable and a d.ts
 * liability, and the classes are exhaustive by construction — an endpoint whose geometry
 * parameters fit none of them fails codegen rather than falling through.
 *
 * `keys` is fal's own parameter list, in fal's own order. Which of those keys is a
 * canonical unified word and which is a per-model extra is a HAND decision that lands with
 * each category's adapter; this file states the fact, not the mapping.
 */

import type { FalParamShape } from "../shape-types";

/** fal-ai/flux/dev. */
const ROW_4ff0de = {
  classes: ["imageSizeUnion"],
  keys: ["prompt", "image_size", "num_inference_steps", "seed", "guidance_scale", "sync_mode", "num_images", "enable_safety_checker", "output_format", "acceleration"],
} as const;

/** fal-ai/flux/schnell. */
const ROW_973af6 = {
  classes: ["imageSizeUnion"],
  keys: ["num_inference_steps", "prompt", "image_size", "seed", "guidance_scale", "sync_mode", "num_images", "enable_safety_checker", "output_format", "acceleration"],
} as const;

export const FAL_IMAGE_PARAM_SHAPES = {
  "fal-ai/flux/dev": ROW_4ff0de,
  "fal-ai/flux/schnell": ROW_973af6,
} as const satisfies Record<string, FalParamShape>;
