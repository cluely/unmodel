// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/bytedance__seedream__v5__pro__text-to-image.json
//   data/fal/openapi/fal-ai__bytedance__seedream__v4.5__text-to-image.json
//   data/fal/openapi/fal-ai__flux-2.json
//   data/fal/openapi/fal-ai__flux-2-max.json
//   data/fal/openapi/fal-ai__flux-2-pro.json
//   data/fal/openapi/fal-ai__flux-2__flash.json
//   data/fal/openapi/fal-ai__flux-lora.json
//   data/fal/openapi/fal-ai__flux-pro__v1.1.json
//   data/fal/openapi/fal-ai__flux-pro__v1.1-ultra.json
//   data/fal/openapi/fal-ai__flux__dev.json
//   data/fal/openapi/fal-ai__flux__schnell.json
//   data/fal/openapi/fal-ai__gpt-image-1.5.json
//   data/fal/openapi/fal-ai__ideogram__v3.json
//   data/fal/openapi/fal-ai__kling-image__v3__text-to-image.json
//   data/fal/openapi/fal-ai__nano-banana.json
//   data/fal/openapi/fal-ai__nano-banana-2.json
//   data/fal/openapi/fal-ai__nano-banana-pro.json
//   data/fal/openapi/fal-ai__qwen-image.json
//   data/fal/openapi/fal-ai__recraft__v3__text-to-image.json
//   data/fal/openapi/fal-ai__recraft__v4__text-to-image.json
//   data/fal/openapi/fal-ai__z-image__turbo.json
//   data/fal/openapi/google__nano-banana-2-lite.json
//   data/fal/openapi/ideogram__v4.json
//   data/fal/openapi/krea__v2__large__text-to-image.json
//   data/fal/openapi/krea__v2__medium__text-to-image.json
//   data/fal/openapi/openai__gpt-image-2.json
//   data/fal/openapi/reve__2.1__text-to-image.json
//   data/fal/openapi/xai__grok-imagine-image.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.image` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalColorPalette,
  FalElementInput,
  FalFile,
  FalImageFile,
  FalImageSize,
  FalImageStyleReference,
  FalImage_28cfa5,
  FalImage_d44dd8,
  FalLoraWeight,
  FalMoodboard,
  FalRGBColor,
  FalStyle,
} from "./shared.gen";

/**
 * The request body `bytedance/seedream/v5/pro/text-to-image` accepts.
 *
 * Seedream 5.0 Pro. Priced by output pixel AREA in two bands — a conditional whose tier
 * the request settles only once a size is given.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/seedream-5-pro/text-to-image` its OpenAPI document is written against.
 * unmodel submits to the published id — that is the one fal documents and the one this
 * catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/seedream-5-pro/text-to-image/api
 */
export interface BytedanceSeedreamV5ProTextToImageInput {
  /** The text prompt used to generate the image. */
  prompt: string;
  /**
   * The size of the generated image. Total pixels must be between 1024x1024 and 2048x2048,
   * with aspect ratio between 1/16 and 16. Default: `"auto_2K"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto_1K" | "auto_2K";
  /** Number of separate model generations to run with the prompt. Default: `1`. */
  num_images?: number;
  /** The file format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
}

/**
 * The result document `bytedance/seedream/v5/pro/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface BytedanceSeedreamV5ProTextToImageOutput {
  /** Generated images. */
  images: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/bytedance/seedream/v4.5/text-to-image` accepts.
 *
 * Seedream 4.5, flat per-image.
 *
 * Docs: https://fal.ai/models/fal-ai/bytedance/seedream/v4.5/text-to-image/api
 */
export interface FalAiBytedanceSeedreamV45TextToImageInput {
  /** The text prompt used to generate the image. */
  prompt: string;
  /**
   * The size of the generated image. Width and height must be between 1920 and 4096, or
   * total number of pixels must be between 2560*1440 and 4096*4096. Default:
   * `{"height":2048,"width":2048}`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto_2K" | "auto_4K";
  /** Number of separate model generations to be run with the prompt. Default: `1`. */
  num_images?: number;
  /**
   * If set to a number greater than one, enables multi-image generation. The model will
   * potentially return up to `max_images` images every generation, and in total,
   * `num_images` generations will be carried out. In total, the number of images generated
   * will be between `num_images` and `max_images*num_images`. Default: `1`.
   */
  max_images?: number;
  /** Random seed to control the stochasticity of image generation. */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
}

/**
 * The result document `fal-ai/bytedance/seedream/v4.5/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiBytedanceSeedreamV45TextToImageOutput {
  /** Generated images. */
  images: FalImage_28cfa5[];
  /** Seed used for generation. */
  seed: number;
}

/**
 * The request body `fal-ai/flux-2` accepts.
 *
 * FLUX.2 [dev], the open-weights generation.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/flux-2-dev` its OpenAPI document is written against. unmodel submits to
 * the published id — that is the one fal documents and the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-dev/api
 */
export interface FalAiFlux2Input {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * Guidance Scale is a measure of how close you want the model to stick to your prompt when
   * looking for a related image to show you. Default: `2.5`.
   */
  guidance_scale?: number;
  /** The seed to use for the generation. If not provided, a random seed will be used. */
  seed?: number | null;
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The size of the image to generate. The width and height must be between 512 and 2048
   * pixels. Default: `"landscape_4_3"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The acceleration level to use for the image generation. Default: `"regular"`. */
  acceleration?: "none" | "regular" | "high";
  /** If set to true, the prompt will be expanded for better results. Default: `false`. */
  enable_prompt_expansion?: boolean;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
}

/**
 * The result document `fal-ai/flux-2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2Output {
  /** The generated images. */
  images: FalImageFile[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-2-max` accepts.
 *
 * FLUX.2 [max]. Tiered like flux-2-pro, at 2.3x the rate.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-max/api
 */
export interface FalAiFlux2MaxInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The seed to use for the generation. */
  seed?: number | null;
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5";
  /**
   * Whether to enable the safety checker. Disabling it requires account authorization;
   * unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `fal-ai/flux-2-max` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2MaxOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The seed used for the generation. */
  seed: number;
}

/**
 * The request body `fal-ai/flux-2-pro` accepts.
 *
 * FLUX.2 [pro] — the anyOf[$ref ImageSize, string enum] `image_size` union, and a TIERED
 * price (first megapixel apart from each extra) that never reaches ModelCost.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-pro/api
 */
export interface FalAiFlux2ProInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The seed to use for the generation. */
  seed?: number | null;
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5";
  /**
   * Whether to enable the safety checker. Disabling it requires account authorization;
   * unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `fal-ai/flux-2-pro` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2ProOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The seed used for the generation. */
  seed: number;
}

/**
 * The request body `fal-ai/flux-2/flash` accepts.
 *
 * The cheapest FLUX.2 arm at $0.005/MP — the low end of the per-megapixel band.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/flux-2-turbo-flash/flash` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-2-turbo-flash/flash/api
 */
export interface FalAiFlux2FlashInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * Guidance Scale is a measure of how close you want the model to stick to your prompt when
   * looking for a related image to show you. Default: `2.5`.
   */
  guidance_scale?: number;
  /** The seed to use for the generation. If not provided, a random seed will be used. */
  seed?: number | null;
  /**
   * The size of the image to generate. The width and height must be between 512 and 2048
   * pixels. Default: `"landscape_4_3"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** If set to true, the prompt will be expanded for better results. Default: `false`. */
  enable_prompt_expansion?: boolean;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
}

/**
 * The result document `fal-ai/flux-2/flash` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFlux2FlashOutput {
  /** The generated images. */
  images: FalImageFile[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-lora` accepts.
 *
 * FLUX.1 [dev] with LoRA weights — carries a `loras` array of URL+scale objects.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-lora/api
 */
export interface FalAiFluxLoraInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The LoRAs to use for the image generation. You can use any number of LoRAs and they will
   * be merged together to generate the final image. Default: `[]`.
   */
  loras?: FalLoraWeight[];
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * The number of images to generate. This is always set to 1 for streaming output. Default:
   * `1`.
   */
  num_images?: number;
  /** If set to true, the safety checker will be enabled. Default: `true`. */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * Acceleration level for image generation. 'regular' balances speed and quality. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular";
}

/**
 * The result document `fal-ai/flux-lora` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxLoraOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/v1.1` accepts.
 *
 * FLUX1.1 [pro].
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/v1.1/api
 */
export interface FalAiFluxProV11Input {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
}

/**
 * The result document `fal-ai/flux-pro/v1.1` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProV11Output {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux-pro/v1.1-ultra` accepts.
 *
 * FLUX1.1 [pro] ultra — sizes by `aspect_ratio` with no pixel field, so it is the
 * aspectRatioEnum arm of this category rather than an imageSizeUnion one.
 *
 * Docs: https://fal.ai/models/fal-ai/flux-pro/v1.1-ultra/api
 */
export interface FalAiFluxProV11UltraInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The safety tolerance level for the generated image. 1 being the most strict and 5 being
   * the most permissive. Default: `"2"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /** Whether to enhance the prompt for better results. Default: `false`. */
  enhance_prompt?: boolean;
  /**
   * The image URL to generate an image from. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_url?: string | null;
  /** The strength of the image prompt, between 0 and 1. Default: `0.1`. */
  image_prompt_strength?: number;
  /** The aspect ratio of the generated image. Default: `"16:9"`. */
  aspect_ratio?: "21:9" | "16:9" | "4:3" | "3:2" | "1:1" | "2:3" | "3:4" | "9:16" | "9:21" | (string & {});
  /** Generate less processed, more natural-looking images. Default: `false`. */
  raw?: boolean;
}

/**
 * The result document `fal-ai/flux-pro/v1.1-ultra` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxProV11UltraOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux/dev` accepts.
 *
 * The anyOf[$ref ImageSize, string enum] `image_size` union — the imageSizeUnion shape
 * class.
 *
 * Docs: https://fal.ai/models/fal-ai/flux/dev/api
 */
export interface FalAiFluxDevInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/flux/dev` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxDevOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux/schnell` accepts.
 *
 * Same shape as flux/dev with a much tighter num_inference_steps ceiling (12 vs 50) — the
 * per-endpoint range narrowing the IR exists for.
 *
 * Docs: https://fal.ai/models/fal-ai/flux/schnell/api
 */
export interface FalAiFluxSchnellInput {
  /** The number of inference steps to perform. Default: `4`. */
  num_inference_steps?: number;
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/flux/schnell` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxSchnellOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/gpt-image-1.5` accepts.
 *
 * GPT Image 1.5. Same size x quality conditional shape as gpt-image-2.
 *
 * Docs: https://fal.ai/models/fal-ai/gpt-image-1.5/api
 */
export interface FalAiGptImage15Input {
  /** The prompt for image generation. */
  prompt: string;
  /** Aspect ratio for the generated image. Default: `"1024x1024"`. */
  image_size?: "1024x1024" | "1536x1024" | "1024x1536";
  /** Background for the generated image. Default: `"auto"`. */
  background?: "auto" | "transparent" | "opaque";
  /** Quality for the generated image. Default: `"high"`. */
  quality?: "low" | "medium" | "high";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Output format for the images. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `fal-ai/gpt-image-1.5` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiGptImage15Output {
  /** The generated images. */
  images: FalImageFile[];
}

/**
 * The request body `fal-ai/ideogram/v3` accepts.
 *
 * Ideogram V3. `rendering_speed` selects one of three per-image rates.
 *
 * Docs: https://fal.ai/models/fal-ai/ideogram/v3/api
 */
export interface FalAiIdeogramV3Input {
  /**
   * A set of images to use as style references (maximum total size 10MB across all style
   * references). The images should be in JPEG, PNG or WebP format. Carries a image reference
   * — an https URL or a `data:` URI.
   */
  image_urls?: string[] | null;
  /** The rendering speed to use. Default: `"BALANCED"`. */
  rendering_speed?: "TURBO" | "BALANCED" | "QUALITY";
  /**
   * A color palette for generation, must EITHER be specified via one of the presets (name)
   * or explicitly via hexadecimal representations of the color with optional weights
   * (members)
   */
  color_palette?: FalColorPalette | null;
  /**
   * A list of 8 character hexadecimal codes representing the style of the image. Cannot be
   * used in conjunction with style_reference_images or style.
   */
  style_codes?: string[] | null;
  /** The style type to generate with. Cannot be used with style_codes. */
  style?: "AUTO" | "GENERAL" | "REALISTIC" | "DESIGN" | null;
  /**
   * Determine if MagicPrompt should be used in generating the request or not. Default:
   * `true`.
   */
  expand_prompt?: boolean;
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Seed for the random number generator. */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** Style preset for generation. The chosen style preset will guide the generation. */
  style_preset?: "80S_ILLUSTRATION" | "90S_NOSTALGIA" | "ABSTRACT_ORGANIC" | "ANALOG_NOSTALGIA" | "ART_BRUT" | "ART_DECO" | "ART_POSTER" | "AURA" | "AVANT_GARDE" | "BAUHAUS" | "BLUEPRINT" | "BLURRY_MOTION" | "BRIGHT_ART" | "C4D_CARTOON" | "CHILDRENS_BOOK" | "COLLAGE" | "COLORING_BOOK_I" | "COLORING_BOOK_II" | "CUBISM" | "DARK_AURA" | "DOODLE" | "DOUBLE_EXPOSURE" | "DRAMATIC_CINEMA" | "EDITORIAL" | "EMOTIONAL_MINIMAL" | "ETHEREAL_PARTY" | "EXPIRED_FILM" | "FLAT_ART" | "FLAT_VECTOR" | "FOREST_REVERIE" | "GEO_MINIMALIST" | "GLASS_PRISM" | "GOLDEN_HOUR" | "GRAFFITI_I" | "GRAFFITI_II" | "HALFTONE_PRINT" | "HIGH_CONTRAST" | "HIPPIE_ERA" | "ICONIC" | "JAPANDI_FUSION" | "JAZZY" | "LONG_EXPOSURE" | "MAGAZINE_EDITORIAL" | "MINIMAL_ILLUSTRATION" | "MIXED_MEDIA" | "MONOCHROME" | "NIGHTLIFE" | "OIL_PAINTING" | "OLD_CARTOONS" | "PAINT_GESTURE" | "POP_ART" | "RETRO_ETCHING" | "RIVIERA_POP" | "SPOTLIGHT_80S" | "STYLIZED_RED" | "SURREAL_COLLAGE" | "TRAVEL_POSTER" | "VINTAGE_GEO" | "VINTAGE_POSTER" | "WATERCOLOR" | "WEIRD" | "WOODBLOCK_PRINT" | null;
  prompt: string;
  /** The resolution of the generated image. Default: `"square_hd"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | null;
  /**
   * Description of what to exclude from an image. Descriptions in the prompt take precedence
   * to descriptions in the negative prompt. Default: `""`.
   */
  negative_prompt?: string;
}

/**
 * The result document `fal-ai/ideogram/v3` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiIdeogramV3Output {
  images: FalFile[];
  /** Seed used for the random number generator. */
  seed: number;
}

/**
 * The request body `fal-ai/kling-image/v3/text-to-image` accepts.
 *
 * Kling Image v3.
 *
 * Docs: https://fal.ai/models/fal-ai/kling-image/v3/text-to-image/api
 */
export interface FalAiKlingImageV3TextToImageInput {
  /** Text prompt for image generation. Max 2500 characters. */
  prompt: string;
  /**
   * Negative text prompt. It is recommended to supplement negative prompt information
   * through negative sentences directly within positive prompts.
   */
  negative_prompt?: string | null;
  /**
   * Optional: Elements (characters/objects) to include in the image for face control. Each
   * element can have a frontal image and optionally reference images.
   */
  elements?: FalElementInput[] | null;
  /** Image generation resolution. 1K: standard, 2K: high-res. Default: `"1K"`. */
  resolution?: "1K" | "2K";
  /** Number of images to generate (1-9). Default: `1`. */
  num_images?: number;
  /** Aspect ratio of generated images. Default: `"16:9"`. */
  aspect_ratio?: "16:9" | "9:16" | "1:1" | "4:3" | "3:4" | "3:2" | "2:3" | "21:9";
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /** If `True`, the media will be returned as a data URI. Default: `false`. */
  sync_mode?: boolean;
}

/**
 * The result document `fal-ai/kling-image/v3/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKlingImageV3TextToImageOutput {
  /** Generated images. */
  images: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/nano-banana` accepts.
 *
 * The original Nano Banana (Gemini 2.5 Flash Image), flat per-image.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana/api
 */
export interface FalAiNanoBananaInput {
  /** The text prompt to generate an image from. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /** The aspect ratio of the generated image. Default: `"1:1"`. */
  aspect_ratio?: "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16";
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate. Default: `true`.
   */
  limit_generations?: boolean;
}

/**
 * The result document `fal-ai/nano-banana` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBananaOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/nano-banana-2` accepts.
 *
 * Google's Gemini 3 Pro Image. `resolution` multiplies the rate and two booleans add
 * surcharges — the conditional-pricing case that keeps rates out of ModelCost.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana-2/api
 */
export interface FalAiNanoBanana2Input {
  /** The text prompt to generate an image from. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /**
   * The aspect ratio of the generated image. Supports extreme ratios: 4:1, 1:4, 8:1, 1:8.
   * Use "auto" to let the model decide based on the prompt. Default: `"auto"`.
   */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | "4:1" | "1:4" | "8:1" | "1:8" | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * Optional system instruction that steers the model's persona and output style across the
   * request. Leave blank to omit; when provided, it is sent as the system instruction to
   * Gemini (or as a system message on OpenAI-compatible providers). Default: `""`.
   */
  system_prompt?: string;
  /** The resolution of the image to generate. Default: `"1K"`. */
  resolution?: "0.5K" | "1K" | "2K" | "4K";
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate and ignore any intermediate images generated by the model. This
   * may affect generation quality. Default: `true`.
   */
  limit_generations?: boolean;
  /**
   * Enable web search for the image generation task. This will allow the model to use the
   * latest information from the web to generate the image. Default: `false`.
   */
  enable_web_search?: boolean;
  /**
   * When set, enables model thinking with the given level ('minimal' or 'high') and includes
   * thoughts in the generation. Omit to disable.
   */
  thinking_level?: "minimal" | "high" | null;
}

/**
 * The result document `fal-ai/nano-banana-2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBanana2Output {
  /** The generated images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/nano-banana-pro` accepts.
 *
 * Nano Banana Pro. Same conditional shape as nano-banana-2 with a narrower `resolution`
 * enum (no 0.5K).
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana-pro/api
 */
export interface FalAiNanoBananaProInput {
  /** The text prompt to generate an image from. */
  prompt: string;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /** The aspect ratio of the generated image. Default: `"1:1"`. */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * Optional system instruction that steers the model's persona and output style across the
   * request. Leave blank to omit; when provided, it is sent as the system instruction to
   * Gemini (or as a system message on OpenAI-compatible providers). Default: `""`.
   */
  system_prompt?: string;
  /** The resolution of the image to generate. Default: `"1K"`. */
  resolution?: "1K" | "2K" | "4K";
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate. Default: `true`.
   */
  limit_generations?: boolean;
  /**
   * Enable web search for the image generation task. This will allow the model to use the
   * latest information from the web to generate the image. Default: `false`.
   */
  enable_web_search?: boolean;
}

/**
 * The result document `fal-ai/nano-banana-pro` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiNanoBananaProOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `fal-ai/qwen-image` accepts.
 *
 * Qwen-Image, per megapixel.
 *
 * Docs: https://fal.ai/models/fal-ai/qwen-image/api
 */
export interface FalAiQwenImageInput {
  /** The prompt to generate the image with. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of inference steps to perform. Default: `30`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `2.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png";
  /** The negative prompt for the generation. Default: `" "`. */
  negative_prompt?: string;
  /**
   * Acceleration level for image generation. Options: 'none', 'regular', 'high'. Higher
   * acceleration increases speed. 'regular' balances speed and quality. 'high' is
   * recommended for images without text. Default: `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
  /**
   * The LoRAs to use for the image generation. You can use up to 3 LoRAs and they will be
   * merged together to generate the final image. Default: `[]`.
   */
  loras?: FalLoraWeight[];
  /**
   * Enable turbo mode for faster generation with high quality. When enabled, uses optimized
   * settings (10 steps, CFG=1.2). Default: `false`.
   */
  use_turbo?: boolean;
}

/**
 * The result document `fal-ai/qwen-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiQwenImageOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/recraft/v3/text-to-image` accepts.
 *
 * Recraft V3. A vector `style` doubles the rate, so the tier key is a style name rather
 * than a size or a speed.
 *
 * Docs: https://fal.ai/models/fal-ai/recraft/v3/text-to-image/api
 */
export interface FalAiRecraftV3TextToImageInput {
  prompt: string;
  /** Default: `"square_hd"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /**
   * The style of the generated images. Vector images cost 2X as much. Default:
   * `"realistic_image"`.
   */
  style?: "any" | "realistic_image" | "digital_illustration" | "vector_illustration" | "realistic_image/b_and_w" | "realistic_image/hard_flash" | "realistic_image/hdr" | "realistic_image/natural_light" | "realistic_image/studio_portrait" | "realistic_image/enterprise" | "realistic_image/motion_blur" | "realistic_image/evening_light" | "realistic_image/faded_nostalgia" | "realistic_image/forest_life" | "realistic_image/mystic_naturalism" | "realistic_image/natural_tones" | "realistic_image/organic_calm" | "realistic_image/real_life_glow" | "realistic_image/retro_realism" | "realistic_image/retro_snapshot" | "realistic_image/urban_drama" | "realistic_image/village_realism" | "realistic_image/warm_folk" | "digital_illustration/pixel_art" | "digital_illustration/hand_drawn" | "digital_illustration/grain" | "digital_illustration/infantile_sketch" | "digital_illustration/2d_art_poster" | "digital_illustration/handmade_3d" | "digital_illustration/hand_drawn_outline" | "digital_illustration/engraving_color" | "digital_illustration/2d_art_poster_2" | "digital_illustration/antiquarian" | "digital_illustration/bold_fantasy" | "digital_illustration/child_book" | "digital_illustration/child_books" | "digital_illustration/cover" | "digital_illustration/crosshatch" | "digital_illustration/digital_engraving" | "digital_illustration/expressionism" | "digital_illustration/freehand_details" | "digital_illustration/grain_20" | "digital_illustration/graphic_intensity" | "digital_illustration/hard_comics" | "digital_illustration/long_shadow" | "digital_illustration/modern_folk" | "digital_illustration/multicolor" | "digital_illustration/neon_calm" | "digital_illustration/noir" | "digital_illustration/nostalgic_pastel" | "digital_illustration/outline_details" | "digital_illustration/pastel_gradient" | "digital_illustration/pastel_sketch" | "digital_illustration/pop_art" | "digital_illustration/pop_renaissance" | "digital_illustration/street_art" | "digital_illustration/tablet_sketch" | "digital_illustration/urban_glow" | "digital_illustration/urban_sketching" | "digital_illustration/vanilla_dreams" | "digital_illustration/young_adult_book" | "digital_illustration/young_adult_book_2" | "vector_illustration/bold_stroke" | "vector_illustration/chemistry" | "vector_illustration/colored_stencil" | "vector_illustration/contour_pop_art" | "vector_illustration/cosmics" | "vector_illustration/cutout" | "vector_illustration/depressive" | "vector_illustration/editorial" | "vector_illustration/emotional_flat" | "vector_illustration/infographical" | "vector_illustration/marker_outline" | "vector_illustration/mosaic" | "vector_illustration/naivector" | "vector_illustration/roundish_flat" | "vector_illustration/segmented_colors" | "vector_illustration/sharp_contrast" | "vector_illustration/thin" | "vector_illustration/vector_photo" | "vector_illustration/vivid_shapes" | "vector_illustration/engraving" | "vector_illustration/line_art" | "vector_illustration/line_circuit" | "vector_illustration/linocut";
  /** An array of preferable colors. Default: `[]`. */
  colors?: FalRGBColor[];
  /** The ID of the custom style reference (optional) */
  style_id?: string | null;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `false`.
   */
  enable_safety_checker?: boolean;
}

/**
 * The result document `fal-ai/recraft/v3/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiRecraftV3TextToImageOutput {
  images: FalFile[];
}

/**
 * The request body `fal-ai/recraft/v4/text-to-image` accepts.
 *
 * Recraft V4, flat per-image.
 *
 * Docs: https://fal.ai/models/fal-ai/recraft/v4/text-to-image/api
 */
export interface FalAiRecraftV4TextToImageInput {
  prompt: string;
  /** Default: `"square_hd"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** An array of preferable colors. Default: `[]`. */
  colors?: FalRGBColor[];
  /** The preferable background color of the generated images. */
  background_color?: FalRGBColor | null;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
}

/**
 * The result document `fal-ai/recraft/v4/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiRecraftV4TextToImageOutput {
  images: FalFile[];
}

/**
 * The request body `fal-ai/z-image/turbo` accepts.
 *
 * Z-Image Turbo — the cheapest row in the category at $0.005/MP, and the one per-megapixel
 * page that states no rounding rule.
 *
 * Docs: https://fal.ai/models/fal-ai/z-image/turbo/api
 */
export interface FalAiZImageTurboInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of inference steps to perform. Default: `8`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /** The acceleration level to use. Default: `"regular"`. */
  acceleration?: "none" | "regular" | "high";
  /**
   * Whether to enable prompt expansion. Note: this will increase the price by 0.0025 credits
   * per request. Default: `false`.
   */
  enable_prompt_expansion?: boolean;
}

/**
 * The result document `fal-ai/z-image/turbo` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiZImageTurboOutput {
  /** The generated image files info. */
  images: FalImageFile[];
  /** The timings of the generation process. */
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `google/nano-banana-2-lite` accepts.
 *
 * Token-priced rather than per-image — carries an `unpriced` reason, not a rate.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/nano-banana-lite` its OpenAPI document is written against. unmodel submits
 * to the published id — that is the one fal documents and the one this catalog is keyed
 * on.
 *
 * Docs: https://fal.ai/models/fal-ai/nano-banana-lite/api
 */
export interface GoogleNanoBanana2LiteInput {
  /** The text prompt to generate an image from. */
  prompt: string;
  /**
   * Optional system instruction that steers the model's persona and output style across the
   * request. Leave blank to omit; when provided, it is sent as the system instruction to
   * Gemini (or as a system message on OpenAI-compatible providers). Default: `""`.
   */
  system_prompt?: string;
  /**
   * The aspect ratio of the generated image. Supports extreme ratios: 4:1, 1:4, 8:1, 1:8.
   * Use "auto" to let the model decide based on the prompt. Default: `"auto"`.
   */
  aspect_ratio?: "auto" | "21:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | "4:1" | "1:4" | "8:1" | "1:8" | null;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed for the random number generator. */
  seed?: number | null;
  /** The format of the generated image. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * The safety tolerance level for content moderation. 1 is the most strict (blocks most
   * content), 6 is the least strict. Default: `"4"`.
   */
  safety_tolerance?: "1" | "2" | "3" | "4" | "5" | "6";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /**
   * Experimental parameter to limit the number of generations from each round of prompting
   * to 1. Set to `True` to to disregard any instructions in the prompt regarding the number
   * of images to generate and ignore any intermediate images generated by the model. This
   * may affect generation quality. Default: `true`.
   */
  limit_generations?: boolean;
  /**
   * When set, enables model thinking with the given level ('minimal' or 'high') and includes
   * thoughts in the generation. Omit to disable.
   */
  thinking_level?: "minimal" | "high" | null;
}

/**
 * The result document `google/nano-banana-2-lite` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface GoogleNanoBanana2LiteOutput {
  /** The generated images. */
  images: FalImageFile[];
  /** The description of the generated images. */
  description: string;
}

/**
 * The request body `ideogram/v4` accepts.
 *
 * Ideogram V4. Like v3, but each `rendering_speed` tier is a PER-MEGAPIXEL rate — the same
 * tier key meaning a different unit one version apart.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/ideogram-v4` its OpenAPI document is written against. unmodel submits to
 * the published id — that is the one fal documents and the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/ideogram-v4/api
 */
export interface IdeogramV4Input {
  /** The prompt to generate an image from. */
  prompt: string;
  /**
   * Which model expands the prompt. 'None' disables prompt expansion and skips its fee;
   * 'Medium' is fast; 'Large' uses Ideogram's Magic Prompt for the highest quality. Default:
   * `"Medium"`.
   */
  expansion_model?: "None" | "Medium" | "Large";
  /** The resolution of the generated image. Default: `"square_hd"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /**
   * The rendering speed to use. Faster speeds use fewer denoising steps. Default:
   * `"BALANCED"`.
   */
  rendering_speed?: "TURBO" | "BALANCED" | "QUALITY";
  /** The acceleration level to use for the image generation. Default: `"none"`. */
  acceleration?: "none" | "low" | "regular" | "high";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** The seed to use for generation. If not provided, a random seed will be used. */
  seed?: number | null;
  /** If `True`, the image is returned as a data URI and is not stored. Default: `false`. */
  sync_mode?: boolean;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
}

/**
 * The result document `ideogram/v4` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface IdeogramV4Output {
  /** The generated images. */
  images: FalImageFile[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `krea/v2/large/text-to-image` accepts.
 *
 * Krea 2 large. unmodel also serves Krea first-party (`unmodel/krea`); this is the same
 * model behind fal's queue.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/krea/v2/large/text-to-image` its OpenAPI document is written against.
 * unmodel submits to the published id — that is the one fal documents and the one this
 * catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/krea/v2/large/text-to-image/api
 */
export interface KreaV2LargeTextToImageInput {
  /** Text description of the image to generate. */
  prompt: string;
  /** Aspect ratio of the generated image. Default: `"1:1"`. */
  aspect_ratio?: "1:1" | "4:3" | "3:2" | "16:9" | "2.35:1" | "4:5" | "2:3" | "9:16";
  /**
   * Controls how loosely the model interprets the prompt. Higher values produce more
   * creative results that may drift from the prompt; lower values stay closer to the prompt.
   * Default: `"medium"`.
   */
  creativity?: "raw" | "low" | "medium" | "high";
  /** Random seed for reproducible generation. */
  seed?: number | null;
  /**
   * Optional list of reference images that guide the style of the generated image. At most
   * 10 entries. Default: `[]`.
   */
  image_style_references?: FalImageStyleReference[];
  /**
   * Optional list of Krea preset styles (LoRA-based) to apply. Each entry references a style
   * by `id` from the Krea styles catalog. At most 10 entries. Default: `[]`.
   */
  styles?: FalStyle[];
  /**
   * Optional moodboard to apply to the generation. Krea currently accepts at most one
   * moodboard per request. Each entry references a moodboard by `id` (UUID). Default: `[]`.
   */
  moodboards?: FalMoodboard[];
}

/**
 * The result document `krea/v2/large/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface KreaV2LargeTextToImageOutput {
  /** The generated images. */
  images: FalImage_28cfa5[];
  /** The seed used to generate the image. */
  seed: number;
}

/**
 * The request body `krea/v2/medium/text-to-image` accepts.
 *
 * Krea 2 medium.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/krea/v2/medium/text-to-image` its OpenAPI document is written against.
 * unmodel submits to the published id — that is the one fal documents and the one this
 * catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/krea/v2/medium/text-to-image/api
 */
export interface KreaV2MediumTextToImageInput {
  /** Text description of the image to generate. */
  prompt: string;
  /** Aspect ratio of the generated image. Default: `"1:1"`. */
  aspect_ratio?: "1:1" | "4:3" | "3:2" | "16:9" | "2.35:1" | "4:5" | "2:3" | "9:16";
  /**
   * Controls how loosely the model interprets the prompt. Higher values produce more
   * creative results that may drift from the prompt; lower values stay closer to the prompt.
   * Default: `"medium"`.
   */
  creativity?: "raw" | "low" | "medium" | "high";
  /** Random seed for reproducible generation. */
  seed?: number | null;
  /**
   * Optional list of reference images that guide the style of the generated image. At most
   * 10 entries. Default: `[]`.
   */
  image_style_references?: FalImageStyleReference[];
  /**
   * Optional list of Krea preset styles (LoRA-based) to apply. Each entry references a style
   * by `id` from the Krea styles catalog. At most 10 entries. Default: `[]`.
   */
  styles?: FalStyle[];
  /**
   * Optional moodboard to apply to the generation. Krea currently accepts at most one
   * moodboard per request. Each entry references a moodboard by `id` (UUID). Default: `[]`.
   */
  moodboards?: FalMoodboard[];
}

/**
 * The result document `krea/v2/medium/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface KreaV2MediumTextToImageOutput {
  /** The generated images. */
  images: FalImage_28cfa5[];
  /** The seed used to generate the image. */
  seed: number;
}

/**
 * The request body `openai/gpt-image-2` accepts.
 *
 * ChatGPT Images 2.0. Priced per size x quality PLUS unbounded token charges, so its
 * estimate is undefined however the request is written.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/gpt-image-2` its OpenAPI document is written against. unmodel submits to
 * the published id — that is the one fal documents and the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/gpt-image-2/api
 */
export interface OpenaiGptImage2Input {
  /** The prompt for image generation. */
  prompt: string;
  /**
   * The size of the generated image. Supports preset names, explicit {width, height}, or
   * 'auto' to let the model pick the best size. Concrete sizes must have both dimensions as
   * multiples of 16, max edge 3840px, aspect ratio <= 3:1, total pixels between 655,360 and
   * 8,294,400. Default: `"landscape_4_3"`.
   */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9" | "auto";
  /**
   * Quality for the generated image. Use 'auto' to let the model pick the best quality for
   * the prompt. Default: `"high"`.
   */
  quality?: "auto" | "low" | "medium" | "high";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Output format for the images. Default: `"png"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `openai/gpt-image-2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface OpenaiGptImage2Output {
  /** The generated images. */
  images: FalImageFile[];
}

/**
 * The request body `reve/2.1/text-to-image` accepts.
 *
 * Reve 2.1.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/reve/v2/text-to-image` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/reve/v2/text-to-image/api
 */
export interface Reve21TextToImageInput {
  /** The text description of the desired image. */
  prompt: string;
  /**
   * The desired aspect ratio of the generated image. With `auto`, the model picks an
   * appropriate aspect ratio for the request. Default: `"auto"`.
   */
  aspect_ratio?: "4:1" | "3:1" | "21:9" | "2:1" | "17:9" | "16:9" | "3:2" | "4:3" | "5:4" | "1:1" | "4:5" | "3:4" | "2:3" | "9:16" | "1:2" | "1:3" | "1:4" | "auto";
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Output format for the generated image. Default: `"png"`. */
  output_format?: "png" | "jpeg" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `reve/2.1/text-to-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Reve21TextToImageOutput {
  /** The generated images. */
  images: FalImage_28cfa5[];
}

/**
 * The request body `xai/grok-imagine-image` accepts.
 *
 * xAI's Grok Imagine, flat per-image.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/xai` its OpenAPI document is written against. unmodel submits to the
 * published id — that is the one fal documents and the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/xai/api
 */
export interface XaiGrokImagineImageInput {
  /** Text description of the desired image. */
  prompt: string;
  /** Number of images to generate. Default: `1`. */
  num_images?: number;
  /** Aspect ratio of the generated image. Default: `"1:1"`. */
  aspect_ratio?: "2:1" | "20:9" | "19.5:9" | "16:9" | "4:3" | "3:2" | "1:1" | "2:3" | "3:4" | "9:16" | "9:19.5" | "9:20" | "1:2";
  /**
   * Resolution of the generated image. `1k` for standard resolution, `2k` for high
   * resolution. Default: `"1k"`.
   */
  resolution?: "1k" | "2k";
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png" | "webp";
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
}

/**
 * The result document `xai/grok-imagine-image` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface XaiGrokImagineImageOutput {
  /** The URL of the generated image. */
  images: FalImageFile[];
  /** The enhanced prompt that was used to generate the image. */
  revised_prompt?: string | null;
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 28 object types re-instantiates at every
 * comparison site, while a keyed lookup resolves in one. The per-endpoint narrowing a
 * validator exposes indexes into this, so adding an endpoint costs one line here and
 * nothing at any call site.
 */
export interface FalImageBodyById {
  "bytedance/seedream/v5/pro/text-to-image": BytedanceSeedreamV5ProTextToImageInput;
  "fal-ai/bytedance/seedream/v4.5/text-to-image": FalAiBytedanceSeedreamV45TextToImageInput;
  "fal-ai/flux-2": FalAiFlux2Input;
  "fal-ai/flux-2-max": FalAiFlux2MaxInput;
  "fal-ai/flux-2-pro": FalAiFlux2ProInput;
  "fal-ai/flux-2/flash": FalAiFlux2FlashInput;
  "fal-ai/flux-lora": FalAiFluxLoraInput;
  "fal-ai/flux-pro/v1.1": FalAiFluxProV11Input;
  "fal-ai/flux-pro/v1.1-ultra": FalAiFluxProV11UltraInput;
  "fal-ai/flux/dev": FalAiFluxDevInput;
  "fal-ai/flux/schnell": FalAiFluxSchnellInput;
  "fal-ai/gpt-image-1.5": FalAiGptImage15Input;
  "fal-ai/ideogram/v3": FalAiIdeogramV3Input;
  "fal-ai/kling-image/v3/text-to-image": FalAiKlingImageV3TextToImageInput;
  "fal-ai/nano-banana": FalAiNanoBananaInput;
  "fal-ai/nano-banana-2": FalAiNanoBanana2Input;
  "fal-ai/nano-banana-pro": FalAiNanoBananaProInput;
  "fal-ai/qwen-image": FalAiQwenImageInput;
  "fal-ai/recraft/v3/text-to-image": FalAiRecraftV3TextToImageInput;
  "fal-ai/recraft/v4/text-to-image": FalAiRecraftV4TextToImageInput;
  "fal-ai/z-image/turbo": FalAiZImageTurboInput;
  "google/nano-banana-2-lite": GoogleNanoBanana2LiteInput;
  "ideogram/v4": IdeogramV4Input;
  "krea/v2/large/text-to-image": KreaV2LargeTextToImageInput;
  "krea/v2/medium/text-to-image": KreaV2MediumTextToImageInput;
  "openai/gpt-image-2": OpenaiGptImage2Input;
  "reve/2.1/text-to-image": Reve21TextToImageInput;
  "xai/grok-imagine-image": XaiGrokImagineImageInput;
}

/** Endpoint id → its result document type. */
export interface FalImageResultById {
  "bytedance/seedream/v5/pro/text-to-image": BytedanceSeedreamV5ProTextToImageOutput;
  "fal-ai/bytedance/seedream/v4.5/text-to-image": FalAiBytedanceSeedreamV45TextToImageOutput;
  "fal-ai/flux-2": FalAiFlux2Output;
  "fal-ai/flux-2-max": FalAiFlux2MaxOutput;
  "fal-ai/flux-2-pro": FalAiFlux2ProOutput;
  "fal-ai/flux-2/flash": FalAiFlux2FlashOutput;
  "fal-ai/flux-lora": FalAiFluxLoraOutput;
  "fal-ai/flux-pro/v1.1": FalAiFluxProV11Output;
  "fal-ai/flux-pro/v1.1-ultra": FalAiFluxProV11UltraOutput;
  "fal-ai/flux/dev": FalAiFluxDevOutput;
  "fal-ai/flux/schnell": FalAiFluxSchnellOutput;
  "fal-ai/gpt-image-1.5": FalAiGptImage15Output;
  "fal-ai/ideogram/v3": FalAiIdeogramV3Output;
  "fal-ai/kling-image/v3/text-to-image": FalAiKlingImageV3TextToImageOutput;
  "fal-ai/nano-banana": FalAiNanoBananaOutput;
  "fal-ai/nano-banana-2": FalAiNanoBanana2Output;
  "fal-ai/nano-banana-pro": FalAiNanoBananaProOutput;
  "fal-ai/qwen-image": FalAiQwenImageOutput;
  "fal-ai/recraft/v3/text-to-image": FalAiRecraftV3TextToImageOutput;
  "fal-ai/recraft/v4/text-to-image": FalAiRecraftV4TextToImageOutput;
  "fal-ai/z-image/turbo": FalAiZImageTurboOutput;
  "google/nano-banana-2-lite": GoogleNanoBanana2LiteOutput;
  "ideogram/v4": IdeogramV4Output;
  "krea/v2/large/text-to-image": KreaV2LargeTextToImageOutput;
  "krea/v2/medium/text-to-image": KreaV2MediumTextToImageOutput;
  "openai/gpt-image-2": OpenaiGptImage2Output;
  "reve/2.1/text-to-image": Reve21TextToImageOutput;
  "xai/grok-imagine-image": XaiGrokImagineImageOutput;
}
