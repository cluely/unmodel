// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__flux__dev.json
//   data/fal/openapi/fal-ai__flux__schnell.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.image` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalImageSize,
  FalImage_d44dd8,
} from "./shared.gen";

/**
 * The request body `fal-ai/flux/dev` accepts.
 *
 * The anyOf[$ref ImageSize, string enum] `image_size` union — the imageSizeUnion shape
 * class.
 *
 * Docs: https://fal.ai/models/fal-ai/flux/dev/api
 */
export interface FalAiFluxDevInput {
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /** The number of inference steps to perform. Default: `28`. */
  num_inference_steps?: number;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/flux/dev` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxDevOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * The request body `fal-ai/flux/schnell` accepts.
 *
 * Same shape as flux/dev with a much tighter num_inference_steps ceiling (12 vs 50) — the
 * per-endpoint range narrowing the IR exists for.
 *
 * Docs: https://fal.ai/models/fal-ai/flux/schnell/api
 */
export interface FalAiFluxSchnellInput {
  /** The number of inference steps to perform. Default: `4`. */
  num_inference_steps?: number;
  /** The prompt to generate an image from. */
  prompt: string;
  /** The size of the generated image. Default: `"landscape_4_3"`. */
  image_size?: FalImageSize | "square_hd" | "square" | "portrait_4_3" | "portrait_16_9" | "landscape_4_3" | "landscape_16_9";
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /**
   * The CFG (Classifier Free Guidance) scale is a measure of how close you want the model to
   * stick to your prompt when looking for a related image to show you. Default: `3.5`.
   */
  guidance_scale?: number;
  /**
   * If `True`, the media will be returned as a data URI and the output data won't be
   * available in the request history. Default: `false`.
   */
  sync_mode?: boolean;
  /** The number of images to generate. Default: `1`. */
  num_images?: number;
  /**
   * If set to true, the safety checker will be enabled. Disabling it requires account
   * authorization; unauthorized requests are always checked, and images flagged as unsafe
   * are returned as black images. Default: `true`.
   */
  enable_safety_checker?: boolean;
  /** The format of the generated image. Default: `"jpeg"`. */
  output_format?: "jpeg" | "png";
  /**
   * The speed of the generation. The higher the speed, the faster the generation. Default:
   * `"none"`.
   */
  acceleration?: "none" | "regular" | "high";
}

/**
 * The result document `fal-ai/flux/schnell` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiFluxSchnellOutput {
  /** The generated image files info. */
  images: FalImage_d44dd8[];
  timings: Record<string, number>;
  /**
   * Seed of the generated Image. It will be the same value of the one passed in the input or
   * the randomly generated that was used in case none was passed.
   */
  seed: number;
  /** Whether the generated images contain NSFW concepts. */
  has_nsfw_concepts: boolean[];
  /** The prompt used for generating the image. */
  prompt: string;
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 2 object types re-instantiates at every comparison
 * site, while a keyed lookup resolves in one. The per-endpoint narrowing a validator
 * exposes indexes into this, so adding an endpoint costs one line here and nothing at any
 * call site.
 */
export interface FalImageBodyById {
  "fal-ai/flux/dev": FalAiFluxDevInput;
  "fal-ai/flux/schnell": FalAiFluxSchnellInput;
}

/** Endpoint id → its result document type. */
export interface FalImageResultById {
  "fal-ai/flux/dev": FalAiFluxDevOutput;
  "fal-ai/flux/schnell": FalAiFluxSchnellOutput;
}
