// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__kling-video__lipsync__audio-to-video.json
//   data/fal/openapi/fal-ai__latentsync.json
//   data/fal/openapi/fal-ai__pixverse__lipsync.json
//   data/fal/openapi/fal-ai__sync-lipsync__v2.json
//   data/fal/openapi/fal-ai__sync-lipsync__v2__pro.json
//   data/fal/openapi/fal-ai__sync-lipsync__v3.json
//   data/fal/openapi/veed__lipsync.json
//   data/fal/openapi/veed__lipsync__v2.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The ONE request schema for every `fal.lipsync` endpoint.
 *
 * One `z.looseObject` for the whole category, not 8 per-endpoint schemas: zod objects are
 * built eagerly, and a hundred of them would be constructed on import for the one the
 * caller actually used.
 *
 * Every field is optional here and every bound is absent. That is not laxity, it is the
 * division of labour — this schema is the SHAPE gate (is `prompt` a string?), and what
 * each endpoint requires, ranges and enumerates lives in `FAL_LIPSYNC_SHAPES`, where the
 * hand check battery can turn a violation into a message that names the endpoint and cites
 * its own documentation.
 *
 * `looseObject` because fal ships new parameters between refreshes and a caller who reads
 * the release notes before we do should not be blocked; unknown keys are reported as
 * warnings by the hand battery instead.
 */

import { z } from "zod";

const falSync3ActiveSpeakerDetectionSchema = z.looseObject({ auto_detect: z.boolean().nullable().optional(), v3: z.boolean().nullable().optional(), use_v2: z.boolean().nullable().optional(), frame_number: z.number().nullable().optional(), coordinates: z.array(z.number()).nullable().optional(), bounding_boxes: z.array(z.array(z.number()).nullable()).nullable().optional(), bounding_boxes_url: z.string().nullable().optional(), face_image: z.string().nullable().optional() });
const falSync3GenerationOptionsSchema = z.looseObject({ sync_mode: z.enum(["cut_off", "loop", "bounce", "silence", "remap"]).nullable().optional(), model_mode: z.enum(["lips", "face", "head", "lipsync", "emotion", "talking_head"]).nullable().optional(), prompt: z.enum(["happy", "sad", "angry", "disgusted", "surprised", "neutral"]).nullable().optional(), temperature: z.number().nullable().optional(), active_speaker_detection: falSync3ActiveSpeakerDetectionSchema.nullable().optional(), occlusion_detection_enabled: z.boolean().nullable().optional() });

export const falLipsyncInputSchema = z.looseObject({
  /**
   * The endpoint to submit to — unmodel's route selector, not a fal body field.
   *
   * Stripped in `finalize` and interpolated into `.request.url`; fal never receives it. It
   * is declared here only so the pipeline does not report the one required parameter as an
   * unknown one. The selector is `endpoint` rather than `model` because `model` is a REAL
   * wire field on several fal endpoints.
   */
  endpoint: z.string(),
  /**
   * Every `fal.lipsync` endpoint types `audio_url` as string, but they disagree on the
   * details (fal-ai/kling-video/lipsync/audio-to-video, fal-ai/latentsync,
   * fal-ai/pixverse/lipsync, fal-ai/sync-lipsync/v2, fal-ai/sync-lipsync/v2/pro,
   * fal-ai/sync-lipsync/v3, veed/lipsync, veed/lipsync/v2), so the union takes the bare type
   * and the exact vocabulary is enforced per endpoint from FAL_LIPSYNC_SHAPES.
   */
  audio_url: z.string().nullable().optional(),
  guidance_scale: z.number().optional(),
  loop_mode: z.enum(["pingpong", "loop"]).nullable().optional(),
  model: z.enum(["lipsync-2", "lipsync-2-pro"]).optional(),
  options: falSync3GenerationOptionsSchema.nullable().optional(),
  seed: z.number().nullable().optional(),
  sync_mode: z.enum(["cut_off", "loop", "bounce", "silence", "remap"]).optional(),
  text: z.string().nullable().optional(),
  /**
   * Every `fal.lipsync` endpoint types `video_url` as string, but they disagree on the
   * details (fal-ai/kling-video/lipsync/audio-to-video, fal-ai/latentsync,
   * fal-ai/pixverse/lipsync, fal-ai/sync-lipsync/v2, fal-ai/sync-lipsync/v2/pro,
   * fal-ai/sync-lipsync/v3, veed/lipsync, veed/lipsync/v2), so the union takes the bare type
   * and the exact vocabulary is enforced per endpoint from FAL_LIPSYNC_SHAPES.
   */
  video_url: z.string().optional(),
  voice_id: z.enum(["Emily", "James", "Isabella", "Liam", "Chloe", "Adrian", "Harper", "Ava", "Sophia", "Julia", "Mason", "Jack", "Oliver", "Ethan", "Auto"]).optional(),
});
