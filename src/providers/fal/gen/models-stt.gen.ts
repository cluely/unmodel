// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__wizper.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The catalog slice for `fal.stt`.
 *
 * fal is not in models.dev, so these rows follow src/providers/HAND_CATALOGS.md — with one
 * difference worth stating: the SHAPE is generated from fal's own published schema and
 * listing, while the PRICING above each row is hand-transcribed from a public model page,
 * quote and date included. fal publishes no machine-readable rate anywhere.
 *
 * `limit.context: 0` throughout: none of these models has a token context window, and 0 is
 * what tells the pipeline to skip the context check rather than to refuse every request.
 */

import type { ModelInfo } from "../../../core/catalog-types";

export const sttModels = {
  /**
   * UNPRICED. Billed per COMPUTE second, not per unit of input or output: the price of a
   * transcription depends on how long fal's GPU took, which no request body can predict. The
   * page's own compute-second figure reads $0, which is an unpopulated billing template
   * rather than a free tier — quoting it as a rate would be worse than saying nothing.
   * Estimates for this endpoint return {} with this caveat.
   *
   * Source: https://fal.ai/models/fal-ai/wizper — verified 2026-08-24. Quote: “Your request
   * will cost $0 per compute second.”
   */
  "fal-ai/wizper": {
    id: "fal-ai/wizper",
    name: "Wizper (Whisper v3 -- fal.ai edition)",
    attachment: false,
    reasoning: false,
    toolCall: false,
    openWeights: false,
    lastUpdated: "2026-04-21",
    modalities: { input: ["audio"], output: ["text"] },
    limit: { context: 0 },
  },
} as const satisfies Record<string, ModelInfo>;

export type FalSttModelId = keyof typeof sttModels;
