// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/cassetteai__sound-effects-generator.json
//   data/fal/openapi/fal-ai__elevenlabs__sound-effects__v2.json
//   data/fal/openapi/fal-ai__stable-audio-3__small__sfx__base__text-to-audio.json
//   data/fal/openapi/fal-ai__stable-audio-3__small__sfx__text-to-audio.json
//   data/fal/openapi/mirelo-ai__sfx1.6__text-to-audio.json
//   data/fal/openapi/sonilo__v1.1__text-to-sound-effects.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The ONE request schema for every `fal.sfx` endpoint.
 *
 * One `z.looseObject` for the whole category, not 6 per-endpoint schemas: zod objects are
 * built eagerly, and a hundred of them would be constructed on import for the one the
 * caller actually used.
 *
 * Every field is optional here and every bound is absent. That is not laxity, it is the
 * division of labour — this schema is the SHAPE gate (is `prompt` a string?), and what
 * each endpoint requires, ranges and enumerates lives in `FAL_SFX_SHAPES`, where the hand
 * check battery can turn a violation into a message that names the endpoint and cites its
 * own documentation.
 *
 * `looseObject` because fal ships new parameters between refreshes and a caller who reads
 * the release notes before we do should not be blocked; unknown keys are reported as
 * warnings by the hand battery instead.
 */

import { z } from "zod";

export const falSfxInputSchema = z.looseObject({
  /**
   * The endpoint to submit to — unmodel's route selector, not a fal body field.
   *
   * Stripped in `finalize` and interpolated into `.request.url`; fal never receives it. It
   * is declared here only so the pipeline does not report the one required parameter as an
   * unknown one. The selector is `endpoint` rather than `model` because `model` is a REAL
   * wire field on several fal endpoints.
   */
  endpoint: z.string(),
  ambience: z.boolean().optional(),
  audio_format: z.enum(["wav", "mp3", "aac", "flac"]).optional(),
  bitrate: z.string().optional(),
  double_output: z.boolean().optional(),
  /**
   * Every `fal.sfx` endpoint types `duration` as number, but they disagree on the details
   * (cassetteai/sound-effects-generator, fal-ai/stable-audio-3/small/sfx/base/text-to-audio,
   * fal-ai/stable-audio-3/small/sfx/text-to-audio, mirelo-ai/sfx1.6/text-to-audio,
   * sonilo/v1.1/text-to-sound-effects), so the union takes the bare type and the exact
   * vocabulary is enforced per endpoint from FAL_SFX_SHAPES.
   */
  duration: z.number().optional(),
  duration_seconds: z.number().nullable().optional(),
  enable_prompt_expansion: z.boolean().optional(),
  enable_safety_checker: z.boolean().optional(),
  guidance_scale: z.number().optional(),
  loop: z.boolean().optional(),
  negative_prompt: z.string().optional(),
  num_inference_steps: z.number().optional(),
  num_samples: z.number().optional(),
  /**
   * Every `fal.sfx` endpoint types `output_format` as string, but they disagree on the
   * details (fal-ai/elevenlabs/sound-effects/v2,
   * fal-ai/stable-audio-3/small/sfx/base/text-to-audio,
   * fal-ai/stable-audio-3/small/sfx/text-to-audio), so the union takes the bare type and the
   * exact vocabulary is enforced per endpoint from FAL_SFX_SHAPES.
   */
  output_format: z.string().optional(),
  prompt: z.string().optional(),
  prompt_influence: z.number().optional(),
  /**
   * Every `fal.sfx` endpoint types `seed` as number, but they disagree on the details
   * (fal-ai/stable-audio-3/small/sfx/base/text-to-audio,
   * fal-ai/stable-audio-3/small/sfx/text-to-audio, mirelo-ai/sfx1.6/text-to-audio), so the
   * union takes the bare type and the exact vocabulary is enforced per endpoint from
   * FAL_SFX_SHAPES.
   */
  seed: z.number().nullable().optional(),
  sync_mode: z.boolean().optional(),
  text: z.string().optional(),
  text_prompt: z.string().optional(),
  upload_audio_format: z.enum(["wav", "mp3", "aac", "flac"]).optional(),
});
