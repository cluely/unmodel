// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__cohere-transcribe.json
//   data/fal/openapi/fal-ai__elevenlabs__speech-to-text.json
//   data/fal/openapi/fal-ai__elevenlabs__speech-to-text__scribe-v2.json
//   data/fal/openapi/fal-ai__speech-to-text.json
//   data/fal/openapi/fal-ai__speech-to-text__turbo.json
//   data/fal/openapi/fal-ai__wizper.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The ONE request schema for every `fal.stt` endpoint.
 *
 * One `z.looseObject` for the whole category, not 6 per-endpoint schemas: zod objects are
 * built eagerly, and a hundred of them would be constructed on import for the one the
 * caller actually used.
 *
 * Every field is optional here and every bound is absent. That is not laxity, it is the
 * division of labour — this schema is the SHAPE gate (is `prompt` a string?), and what
 * each endpoint requires, ranges and enumerates lives in `FAL_STT_SHAPES`, where the hand
 * check battery can turn a violation into a message that names the endpoint and cites its
 * own documentation.
 *
 * `looseObject` because fal ships new parameters between refreshes and a caller who reads
 * the release notes before we do should not be blocked; unknown keys are reported as
 * warnings by the hand battery instead.
 */

import { z } from "zod";

export const falSttInputSchema = z.looseObject({
  /**
   * The endpoint to submit to — unmodel's route selector, not a fal body field.
   *
   * Stripped in `finalize` and interpolated into `.request.url`; fal never receives it. It
   * is declared here only so the pipeline does not report the one required parameter as an
   * unknown one. The selector is `endpoint` rather than `model` because `model` is a REAL
   * wire field on several fal endpoints.
   */
  endpoint: z.string(),
  audio_url: z.string().optional(),
  chunk_level: z.enum(["segment"]).optional(),
  diarize: z.boolean().optional(),
  keyterms: z.array(z.string()).optional(),
  /**
   * Every `fal.stt` endpoint types `language` as string, but they disagree on the details
   * (fal-ai/cohere-transcribe, fal-ai/wizper), so the union takes the bare type and the
   * exact vocabulary is enforced per endpoint from FAL_STT_SHAPES.
   */
  language: z.string().nullable().optional(),
  language_code: z.string().nullable().optional(),
  max_new_tokens: z.number().optional(),
  max_segment_len: z.number().optional(),
  merge_chunks: z.boolean().optional(),
  punctuation: z.boolean().optional(),
  tag_audio_events: z.boolean().optional(),
  task: z.enum(["transcribe", "translate"]).optional(),
  use_pnc: z.boolean().optional(),
  version: z.enum(["3"]).optional(),
});
