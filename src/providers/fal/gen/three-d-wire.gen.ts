// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__hunyuan-3d__v3.1__pro__image-to-3d.json
//   data/fal/openapi/fal-ai__hunyuan-3d__v3.1__pro__text-to-3d.json
//   data/fal/openapi/fal-ai__hunyuan-3d__v3.1__rapid__image-to-3d.json
//   data/fal/openapi/fal-ai__hunyuan3d__v2.json
//   data/fal/openapi/fal-ai__hunyuan3d__v2__turbo.json
//   data/fal/openapi/fal-ai__hyper3d__rodin__v2.5.json
//   data/fal/openapi/fal-ai__hyper3d__rodin__v2.5__text-to-3d.json
//   data/fal/openapi/fal-ai__trellis.json
//   data/fal/openapi/fal-ai__trellis-2.json
//   data/fal/openapi/fal-ai__triposr.json
//   data/fal/openapi/hitem3d__hi3d__v3.0__image-to-3d.json
//   data/fal/openapi/meshy__v7__image-to-3d.json
//   data/fal/openapi/meshy__v7__text-to-3d.json
//   data/fal/openapi/tripo3d__h3.1__image-to-3d.json
//   data/fal/openapi/tripo3d__h3.1__text-to-3d.json
//   data/fal/openapi/tripo3d__p1__image-to-3d.json
//   data/fal/openapi/tripo3d__p1__text-to-3d.json
//   data/fal/openapi/tripo3d__tripo__v2.5__image-to-3d.json
//   data/fal/openapi/tripo3d__tripo__v2.5__multiview-to-3d.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.threeD` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalBasicAnimations,
  FalFile,
  FalImage_28cfa5,
  FalModelUrls_15e05d,
  FalModelUrls_3cc361,
  FalModelUrls_a3b9bc,
  FalRodinGen25Addons,
  FalRodinGen25BbboxField,
  FalTextureFiles,
} from "./shared.gen";

/**
 * The request body `fal-ai/hunyuan-3d/v3.1/pro/image-to-3d` accepts.
 *
 * Tencent's Hunyuan3D 3.1 Pro, image-driven. Its extra views are seven NAMED siblings
 * (`back_image_url`, `top_image_url`, `left_front_image_url`, …) beside the required
 * `input_image_url`, so the canonical `image` is the front view and the rest are extras.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/hunyuan-3d-pro-image-to-3d/v3.1/pro/image-to-3d` its OpenAPI document is
 * written against. unmodel submits to the published id — that is the one fal documents and
 * the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/hunyuan-3d-pro-image-to-3d/v3.1/pro/image-to-3d/api
 */
export interface FalAiHunyuan3dV31ProImageTo3dInput {
  /**
   * Front view image URL. Resolution: 128-5000px, max 8MB, formats: JPG/PNG/WEBP. Tips:
   * simple background, single object, object >50% of frame. Carries a image reference — an
   * https URL or a `data:` URI.
   */
  input_image_url: string;
  /**
   * Optional back/rear view image URL (JPG/PNG recommended). Carries a image reference — an
   * https URL or a `data:` URI.
   */
  back_image_url?: string | null;
  /**
   * Optional left side view image URL (JPG/PNG recommended). Carries a image reference — an
   * https URL or a `data:` URI.
   */
  left_image_url?: string | null;
  /**
   * Optional right side view image URL (JPG/PNG recommended). Carries a image reference — an
   * https URL or a `data:` URI.
   */
  right_image_url?: string | null;
  /**
   * Optional top view image URL (v3.1 exclusive, JPG/PNG recommended). Carries a image
   * reference — an https URL or a `data:` URI.
   */
  top_image_url?: string | null;
  /**
   * Optional bottom view image URL (v3.1 exclusive, JPG/PNG recommended). Carries a image
   * reference — an https URL or a `data:` URI.
   */
  bottom_image_url?: string | null;
  /**
   * Optional left-front 45 degree angle view image URL (v3.1 exclusive, JPG/PNG
   * recommended). Carries a image reference — an https URL or a `data:` URI.
   */
  left_front_image_url?: string | null;
  /**
   * Optional right-front 45 degree angle view image URL (v3.1 exclusive, JPG/PNG
   * recommended). Carries a image reference — an https URL or a `data:` URI.
   */
  right_front_image_url?: string | null;
  /**
   * Generation task type. Normal: textured model. Geometry: geometry-only white model (no
   * textures). LowPoly/Sketch are not available in v3.1. Default: `"Normal"`.
   */
  generate_type?: "Normal" | "Geometry";
  /**
   * Enable PBR material generation (metallic, roughness, normal textures). Ignored when
   * generate_type is Geometry. Default: `false`.
   */
  enable_pbr?: boolean;
  /** Target polygon face count. Range: 40,000-1,500,000. Default: 500,000. Default: `500000`. */
  face_count?: number;
}

/**
 * The result document `fal-ai/hunyuan-3d/v3.1/pro/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHunyuan3dV31ProImageTo3dOutput {
  /** Generated 3D object in GLB format. */
  model_glb: FalFile;
  /** Preview thumbnail of the generated model. */
  thumbnail?: FalFile | null;
  /** URLs for different 3D model formats. */
  model_urls: FalModelUrls_15e05d;
  /** The seed used for generation. */
  seed?: number | null;
}

/**
 * The request body `fal-ai/hunyuan-3d/v3.1/pro/text-to-3d` accepts.
 *
 * The text-driven half of the same model. `generate_type` picks a textured mesh or a white
 * one — the switch every vendor here spells differently, and the reason `texture` is not
 * vocabulary.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/hunyuan-3d-pro-text-to-3d/v3.1/pro/text-to-3d` its OpenAPI document is
 * written against. unmodel submits to the published id — that is the one fal documents and
 * the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/hunyuan-3d-pro-text-to-3d/v3.1/pro/text-to-3d/api
 */
export interface FalAiHunyuan3dV31ProTextTo3dInput {
  /** Text description of the 3D content to generate. Max 1024 UTF-8 characters. */
  prompt: string;
  /**
   * Generation task type. Normal: textured model. Geometry: geometry-only white model (no
   * textures). LowPoly/Sketch are not available in v3.1. Default: `"Normal"`.
   */
  generate_type?: "Normal" | "Geometry";
  /**
   * Enable PBR material generation (metallic, roughness, normal textures). Ignored when
   * generate_type is Geometry. Default: `false`.
   */
  enable_pbr?: boolean;
  /** Target polygon face count. Range: 40,000-1,500,000. Default: 500,000. Default: `500000`. */
  face_count?: number;
}

/**
 * The result document `fal-ai/hunyuan-3d/v3.1/pro/text-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHunyuan3dV31ProTextTo3dOutput {
  /** Generated 3D object in GLB format. */
  model_glb: FalFile;
  /** Preview thumbnail of the generated model. */
  thumbnail?: FalFile | null;
  /** URLs for different 3D model formats. */
  model_urls: FalModelUrls_15e05d;
  /** The seed used for generation. */
  seed?: number | null;
}

/**
 * The request body `fal-ai/hunyuan-3d/v3.1/rapid/image-to-3d` accepts.
 *
 * The fast tier: three parameters in all. Its response carries `material_mtl` and
 * `texture` beside the mesh, which is the one place in this category where the OBJ
 * sidecars are visible on the wire.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/hunyuan-3d-rapid-image-to-3d/v3.1/rapid/image-to-3d` its OpenAPI document
 * is written against. unmodel submits to the published id — that is the one fal documents
 * and the one this catalog is keyed on.
 *
 * Docs:
 * https://fal.ai/models/fal-ai/hunyuan-3d-rapid-image-to-3d/v3.1/rapid/image-to-3d/api
 */
export interface FalAiHunyuan3dV31RapidImageTo3dInput {
  /**
   * Front view image URL. Resolution: 128-5000px, max 8MB (recommended ≤6MB for base64
   * encoding), formats: JPG/PNG/WEBP. Tips: simple background, single object, object >50% of
   * frame. Carries a image reference — an https URL or a `data:` URI.
   */
  input_image_url: string;
  /**
   * Enable PBR material generation (metallic, roughness, normal textures). Does not take
   * effect when enable_geometry is True. Default: `false`.
   */
  enable_pbr?: boolean;
  /**
   * Generate geometry-only white model without textures. When enabled, enable_pbr is ignored
   * and OBJ is not supported (default output is GLB). Default: `false`.
   */
  enable_geometry?: boolean;
}

/**
 * The result document `fal-ai/hunyuan-3d/v3.1/rapid/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHunyuan3dV31RapidImageTo3dOutput {
  /** Generated 3D model file. Contains GLB if available, otherwise OBJ. */
  model_glb?: FalFile | null;
  /** MTL material file for the OBJ model. */
  material_mtl?: FalFile | null;
  /** Texture image for the 3D model. */
  texture?: FalFile | null;
  /** Preview thumbnail of the generated model. */
  thumbnail?: FalFile | null;
  /** URLs for different 3D model formats. */
  model_urls: FalModelUrls_15e05d;
}

/**
 * The request body `fal-ai/hunyuan3d/v2` accepts.
 *
 * Hunyuan3D 2.0, the octree-resolution generation. `textured_mesh` triples the price,
 * which is the conditional rate its pricing row carries.
 *
 * Docs: https://fal.ai/models/fal-ai/hunyuan3d/v2/api
 */
export interface FalAiHunyuan3dV2Input {
  /**
   * URL of image to use while generating the 3D model. Carries a image reference — an https
   * URL or a `data:` URI.
   */
  input_image_url: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /** Number of inference steps to perform. Default: `50`. */
  num_inference_steps?: number;
  /** Guidance scale for the model. Default: `7.5`. */
  guidance_scale?: number;
  /** Octree resolution for the model. Default: `256`. */
  octree_resolution?: number;
  /**
   * If set true, textured mesh will be generated and the price charged would be 3 times that
   * of white mesh. Default: `false`.
   */
  textured_mesh?: boolean;
}

/**
 * The result document `fal-ai/hunyuan3d/v2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHunyuan3dV2Output {
  /** Generated 3D object file. */
  model_mesh: FalFile;
  /** Seed value used for generation. */
  seed: number;
}

/**
 * The request body `fal-ai/hunyuan3d/v2/turbo` accepts.
 *
 * The turbo arm of the same schema — identical parameter surface, so it shares a generated
 * row. fal publishes NO rate for it: see data/fal/pricing.json.
 *
 * Docs: https://fal.ai/models/fal-ai/hunyuan3d/v2/turbo/api
 */
export interface FalAiHunyuan3dV2TurboInput {
  /**
   * URL of image to use while generating the 3D model. Carries a image reference — an https
   * URL or a `data:` URI.
   */
  input_image_url: string;
  /**
   * The same seed and the same prompt given to the same version of the model will output the
   * same image every time.
   */
  seed?: number | null;
  /** Number of inference steps to perform. Default: `50`. */
  num_inference_steps?: number;
  /** Guidance scale for the model. Default: `7.5`. */
  guidance_scale?: number;
  /** Octree resolution for the model. Default: `256`. */
  octree_resolution?: number;
  /**
   * If set true, textured mesh will be generated and the price charged would be 3 times that
   * of white mesh. Default: `false`.
   */
  textured_mesh?: boolean;
}

/**
 * The result document `fal-ai/hunyuan3d/v2/turbo` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHunyuan3dV2TurboOutput {
  /** Generated 3D object file. */
  model_mesh: FalFile;
  /** Seed value used for generation. */
  seed: number;
}

/**
 * The request body `fal-ai/hyper3d/rodin/v2.5` accepts.
 *
 * Hyper3D Rodin v2.5, and the only endpoint in this category that reads BOTH moods:
 * `prompt` and `image_urls` are both optional and the prompt steers an image-driven
 * generation. That is the both-arm of the row's `inputs`, and it is why
 * `ThreeDModelNarrowing` has three populated arms rather than two. `image_urls` is a LIST
 * — the `imageWireList` flag.
 *
 * Docs: https://fal.ai/models/fal-ai/hyper3d/rodin/v2.5/api
 */
export interface FalAiHyper3dRodinV25Input {
  /**
   * A textual prompt to guide model generation. Optional for Image-to-3D mode - if empty,
   * the model auto-generates a prompt from the images. Default: `""`.
   */
  prompt?: string;
  /**
   * URL of images to use while generating the 3D model. Required for Image-to-3D mode. Up to
   * 5 images allowed. Carries a image reference — an https URL or a `data:` URI.
   */
  image_urls?: string[];
  /**
   * Generation tier. Higher tiers produce more detailed meshes but cost more. Extreme-High
   * enables the highest-detail mode and bills at double the base rate. See more info in the
   * [documentation](https://developer.hyper3d.ai/api-specification/rodin-gen2.5#use-gen-2.5-generation-with-following-gen-2.5-tiers).
   * Selecting Gen-2.5-Minimum will automatically configure to a minimum mode, where 50K
   * Triangle is selected, taking priority over quality_mesh_option. Default:
   * `"Gen-2.5-High"`.
   */
  tier?: "Gen-2.5-Minimum" | "Gen-2.5-Extreme-Low" | "Gen-2.5-Low" | "Gen-2.5-Medium" | "Gen-2.5-High" | "Gen-2.5-Extreme-High";
  /**
   * When enabled, preserves the transparency channel from input images during 3D generation.
   * Default: `false`.
   */
  use_original_alpha?: boolean;
  /** Seed value for randomization, ranging from 0 to 65535. Optional. */
  seed?: number | null;
  /**
   * Format of the geometry file. Possible values: glb, usdz, fbx, obj, stl. Default is glb.
   * Default: `"glb"`.
   */
  geometry_file_format?: "glb" | "usdz" | "fbx" | "obj" | "stl";
  /**
   * Material type. PBR: physically-based materials with realistic lighting. Shaded: baked
   * lighting with no PBR maps. All: both PBR and Shaded variants. None: geometry only with
   * no textures. Default: `"All"`.
   */
  material?: "PBR" | "Shaded" | "All" | "None";
  /**
   * Combined quality and mesh type selection. Quad = smooth surfaces, Triangle = detailed
   * geometry. Maps to `mesh_mode` (Quad/Raw) and `quality_override` (face count) on the
   * Hyper3D API. Valid face counts depend on the selected tier. Up to 1M Triangle is
   * supported for tiers Extreme-Low, Low, and Medium. Up to 2M Triangle (as low as 20K
   * Triangle) is supported for tiers High and Extreme-High. Auto option will choose the
   * advised options for each tier: 1. Gen-2.5-Minimum: 50K Triangle 2. Gen-2.5-Extreme-Low:
   * 50K Triangle 3. Gen-2.5-Low: 50K Triangle 4. Gen-2.5-Medium: 500K Triangle 5.
   * Gen-2.5-High: 500K Triangle Gen-2.5-Extreme-High: 1M Triangle. Default: `"Auto"`.
   */
  quality_mesh_option?: "Auto" | "4K Quad" | "8K Quad" | "18K Quad" | "50K Quad" | "100K Quad" | "200K Quad" | "2K Triangle" | "20K Triangle" | "50K Triangle" | "150K Triangle" | "500K Triangle" | "1M Triangle" | "2M Triangle";
  /**
   * Texture generation quality. When unset, the API picks a default appropriate for the
   * tier.
   */
  texture_mode?: "legacy" | "extreme-low" | "low" | "medium" | "high" | null;
  /**
   * If enabled, the generation will have enhanced generative robustness while ensuring
   * output consistency. This corresponds to `geometry_instruct_mode` being equal to
   * `creative` on the Hyper3D API. Default: `false`.
   */
  enable_creative_mode?: boolean;
  /** Enables enhanced texture post-processing for higher visual fidelity. Default: `false`. */
  hd_texture?: boolean;
  /** Removes baked lighting/highlights from the generated textures. Default: `false`. */
  texture_delight?: boolean;
  /**
   * Generate finer micro-scale geometric detail. Only effective with
   * `tier="Gen-2.5-Extreme-High"`. Default: `false`.
   */
  is_micro?: boolean;
  /**
   * Generate characters in T-pose or A-pose format, making them easier to rig and animate in
   * 3D software. Default: `false`.
   */
  TAPose?: boolean;
  /** This is a controlnet that controls the maximum sized of the generated model. */
  bbox_condition?: FalRodinGen25BbboxField | null;
  /** Optional add-ons. Single values are accepted as a shorthand for a list with one item. */
  addons?: FalRodinGen25Addons | null;
  /**
   * Generate a preview render image of the 3D model along with the model files. Default:
   * `false`.
   */
  preview_render?: boolean;
}

/**
 * The result document `fal-ai/hyper3d/rodin/v2.5` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHyper3dRodinV25Output {
  /** Generated 3D object file. */
  model_mesh: FalFile;
  /** Additional generated 3D object files returned by Hyper3D besides model_mesh. */
  model_meshes?: FalFile[];
  /** Seed value used for generation. */
  seed: number;
  /** Generated textures for the 3D object. */
  textures: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/hyper3d/rodin/v2.5/text-to-3d` accepts.
 *
 * The text-only sibling of the route above: same dials, no image field at all. The pair is
 * the clearest statement in the roster that `inputs` is a fact about a ROUTE rather than
 * about a vendor.
 *
 * Docs: https://fal.ai/models/fal-ai/hyper3d/rodin/v2.5/text-to-3d/api
 */
export interface FalAiHyper3dRodinV25TextTo3dInput {
  /** A textual prompt to guide model generation. Required for Text-to-3D mode. */
  prompt: string;
  /**
   * Generation tier. Higher tiers produce more detailed meshes but cost more. Extreme-High
   * enables the highest-detail mode and bills at double the base rate. See more info in the
   * [documentation](https://developer.hyper3d.ai/api-specification/rodin-gen2.5#use-gen-2.5-generation-with-following-gen-2.5-tiers).
   * Selecting Gen-2.5-Minimum will automatically configure to a minimum mode, where 50K
   * Triangle is selected, taking priority over quality_mesh_option. Default:
   * `"Gen-2.5-High"`.
   */
  tier?: "Gen-2.5-Minimum" | "Gen-2.5-Extreme-Low" | "Gen-2.5-Low" | "Gen-2.5-Medium" | "Gen-2.5-High" | "Gen-2.5-Extreme-High";
  /** Seed value for randomization, ranging from 0 to 65535. Optional. */
  seed?: number | null;
  /**
   * Format of the geometry file. Possible values: glb, usdz, fbx, obj, stl. Default is glb.
   * Default: `"glb"`.
   */
  geometry_file_format?: "glb" | "usdz" | "fbx" | "obj" | "stl";
  /**
   * Material type. PBR: physically-based materials with realistic lighting. Shaded: baked
   * lighting with no PBR maps. All: both PBR and Shaded variants. None: geometry only with
   * no textures. Default: `"All"`.
   */
  material?: "PBR" | "Shaded" | "All" | "None";
  /**
   * Combined quality and mesh type selection. Quad = smooth surfaces, Triangle = detailed
   * geometry. Maps to `mesh_mode` (Quad/Raw) and `quality_override` (face count) on the
   * Hyper3D API. Valid face counts depend on the selected tier. Up to 1M Triangle is
   * supported for tiers Extreme-Low, Low, and Medium. Up to 2M Triangle (as low as 20K
   * Triangle) is supported for tiers High and Extreme-High. Auto option will choose the
   * advised options for each tier: 1. Gen-2.5-Minimum: 50K Triangle 2. Gen-2.5-Extreme-Low:
   * 50K Triangle 3. Gen-2.5-Low: 50K Triangle 4. Gen-2.5-Medium: 500K Triangle 5.
   * Gen-2.5-High: 500K Triangle Gen-2.5-Extreme-High: 1M Triangle. Default: `"Auto"`.
   */
  quality_mesh_option?: "Auto" | "4K Quad" | "8K Quad" | "18K Quad" | "50K Quad" | "100K Quad" | "200K Quad" | "2K Triangle" | "20K Triangle" | "50K Triangle" | "150K Triangle" | "500K Triangle" | "1M Triangle" | "2M Triangle";
  /**
   * Texture generation quality. When unset, the API picks a default appropriate for the
   * tier.
   */
  texture_mode?: "legacy" | "extreme-low" | "low" | "medium" | "high" | null;
  /**
   * If enabled, the generation will have enhanced generative robustness while ensuring
   * output consistency. This corresponds to `geometry_instruct_mode` being equal to
   * `creative` on the Hyper3D API. Default: `false`.
   */
  enable_creative_mode?: boolean;
  /** Enables enhanced texture post-processing for higher visual fidelity. Default: `false`. */
  hd_texture?: boolean;
  /** Removes baked lighting/highlights from the generated textures. Default: `false`. */
  texture_delight?: boolean;
  /**
   * Generate finer micro-scale geometric detail. Only effective with
   * `tier="Gen-2.5-Extreme-High"`. Default: `false`.
   */
  is_micro?: boolean;
  /**
   * Generate characters in T-pose or A-pose format, making them easier to rig and animate in
   * 3D software. Default: `false`.
   */
  TAPose?: boolean;
  /** This is a controlnet that controls the maximum sized of the generated model. */
  bbox_condition?: FalRodinGen25BbboxField | null;
  /** Optional add-ons. */
  addons?: FalRodinGen25Addons | null;
}

/**
 * The result document `fal-ai/hyper3d/rodin/v2.5/text-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiHyper3dRodinV25TextTo3dOutput {
  /** Generated 3D object file. */
  model_mesh: FalFile;
  /** Additional generated 3D object files returned by Hyper3D besides model_mesh. */
  model_meshes?: FalFile[];
  /** Seed value used for generation. */
  seed: number;
  /** Generated textures for the 3D object. */
  textures: FalImage_28cfa5[];
}

/**
 * The request body `fal-ai/trellis` accepts.
 *
 * Microsoft TRELLIS — structured-latent 3D generation from one image, and the cheapest row
 * in the category at $0.02.
 *
 * Docs: https://fal.ai/models/fal-ai/trellis/api
 */
export interface FalAiTrellisInput {
  /**
   * URL of the input image to convert to 3D. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_url: string;
  /** Random seed for reproducibility. */
  seed?: number | null;
  /** Guidance strength for sparse structure generation. Default: `7.5`. */
  ss_guidance_strength?: number;
  /** Sampling steps for sparse structure generation. Default: `12`. */
  ss_sampling_steps?: number;
  /** Guidance strength for structured latent generation. Default: `3`. */
  slat_guidance_strength?: number;
  /** Sampling steps for structured latent generation. Default: `12`. */
  slat_sampling_steps?: number;
  /** Mesh simplification factor. Default: `0.95`. */
  mesh_simplify?: number;
  /** Texture resolution. Default: `1024`. */
  texture_size?: 512 | 1024 | 2048;
}

/**
 * The result document `fal-ai/trellis` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiTrellisOutput {
  /** Generated 3D mesh file. */
  model_mesh: FalFile;
  /** Processing timings. */
  timings: Record<string, number>;
}

/**
 * The request body `fal-ai/trellis-2` accepts.
 *
 * TRELLIS 2, with thirty parameters — the widest per-endpoint extras surface in this
 * category, all of them sampler and UV-unwrap dials that no second vendor spells the same
 * way.
 *
 * Docs: https://fal.ai/models/fal-ai/trellis-2/api
 */
export interface FalAiTrellis2Input {
  /** Random seed for reproducibility. */
  seed?: number | null;
  /** Output resolution; higher is slower but more detailed. Default: `1024`. */
  resolution?: 512 | 1024 | 1536;
  /**
   * How closely the initial 3D structure follows the input image. Higher values produce more
   * faithful but potentially noisier results. Default: `7.5`.
   */
  ss_guidance_strength?: number;
  /**
   * Dampens artifacts from high guidance in stage 1. Lower values allow stronger guidance
   * effects, higher values stabilize the output. Default: `0.7`.
   */
  ss_guidance_rescale?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance starts for stage 1
   * (sparse structure). Guidance is only applied between interval_start and interval_end;
   * outside this window the model runs unguided. Lower values apply guidance earlier.
   * Default: `0.6`.
   */
  ss_guidance_interval_start?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance ends for stage 1
   * (sparse structure). Must be >= ss_guidance_interval_start. Default: `1`.
   */
  ss_guidance_interval_end?: number;
  /**
   * Number of denoising steps for the initial structure. More steps = slower but potentially
   * higher quality. Default: `12`.
   */
  ss_sampling_steps?: number;
  /**
   * Controls noise schedule sharpness for structure generation. Higher values produce
   * sharper transitions. Default: `5`.
   */
  ss_rescale_t?: number;
  /**
   * How closely the detailed geometry follows the input image. Higher values add more detail
   * but may introduce noise. Default: `7.5`.
   */
  shape_slat_guidance_strength?: number;
  /**
   * Dampens artifacts from high guidance in the shape stage. Increase if you see noisy
   * geometry. Default: `0.5`.
   */
  shape_slat_guidance_rescale?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance starts for stage 2
   * (shape refinement). Guidance is only applied between interval_start and interval_end.
   * Default: `0.6`.
   */
  shape_slat_guidance_interval_start?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance ends for stage 2
   * (shape refinement). Must be >= shape_slat_guidance_interval_start. Default: `1`.
   */
  shape_slat_guidance_interval_end?: number;
  /**
   * Number of denoising steps for shape refinement. More steps = slower but potentially
   * smoother geometry. Default: `12`.
   */
  shape_slat_sampling_steps?: number;
  /**
   * Controls noise schedule sharpness for shape refinement. Higher values produce sharper
   * geometric details. Default: `3`.
   */
  shape_slat_rescale_t?: number;
  /**
   * How closely the texture follows the input image colors. Higher values produce more vivid
   * but potentially oversaturated textures. Default: `1`.
   */
  tex_slat_guidance_strength?: number;
  /**
   * Dampens artifacts from high guidance in the texture stage. Increase if textures look
   * noisy or have color banding. Default: `0`.
   */
  tex_slat_guidance_rescale?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance starts for stage 3
   * (texture). Guidance is only applied between interval_start and interval_end. Default:
   * `0.6`.
   */
  tex_slat_guidance_interval_start?: number;
  /**
   * Fraction of the denoising process at which classifier-free guidance ends for stage 3
   * (texture). Must be >= tex_slat_guidance_interval_start. Default: `0.9`.
   */
  tex_slat_guidance_interval_end?: number;
  /**
   * Number of denoising steps for texture generation. More steps = slower but potentially
   * cleaner textures. Default: `12`.
   */
  tex_slat_sampling_steps?: number;
  /**
   * Controls noise schedule sharpness for texture generation. Higher values produce sharper
   * texture details. Default: `3`.
   */
  tex_slat_rescale_t?: number;
  /**
   * Target number of vertices in the final mesh. Lower values produce smaller files but less
   * detail. 500k is good for most uses, reduce to 20k-50k for web/mobile. Default: `500000`.
   */
  decimation_target?: number;
  /**
   * Resolution of the texture image baked onto the mesh. Higher values capture finer surface
   * details but produce larger files. Default: `2048`.
   */
  texture_size?: 1024 | 2048 | 4096;
  /**
   * Rebuild the mesh topology for cleaner triangles. Slower but usually produces better
   * results for downstream use (animation, 3D printing, etc). Default: `true`.
   */
  remesh?: boolean;
  /**
   * Controls how far remeshing can move vertices from the original surface. Higher values
   * allow more smoothing but may lose fine details. Default: `1`.
   */
  remesh_band?: number;
  /**
   * How much to project remeshed vertices back onto the original surface. 0 = no projection
   * (smoother), 1 = full projection (preserves detail). Default: `0`.
   */
  remesh_project?: number;
  /**
   * Maximum angle (in degrees) between adjacent face normals allowed within a single UV
   * chart. Lower values produce more charts with less distortion; higher values produce
   * fewer, larger charts with potentially more stretching. Default: `90`.
   */
  uv_unwrap_angle_threshold_deg?: number;
  /**
   * Number of local refinement passes during UV chart computation. Higher values produce
   * cleaner chart boundaries at the cost of additional UV-unwrap time. Default: `0`.
   */
  uv_unwrap_refine_iterations?: number;
  /**
   * Number of global clustering passes during UV chart computation. More passes produce more
   * balanced charts. Default: `1`.
   */
  uv_unwrap_global_iterations?: number;
  /**
   * Smoothing strength applied to UV chart boundaries. Higher values reduce visible seams
   * but may introduce UV distortion. Default: `1`.
   */
  uv_unwrap_smooth_strength?: number;
  /**
   * URL of the input image to convert to 3D. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_url: string;
}

/**
 * The result document `fal-ai/trellis-2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiTrellis2Output {
  /** Generated 3D GLB file. */
  model_glb: FalFile;
}

/**
 * The request body `fal-ai/triposr` accepts.
 *
 * Stability's TripoSR. Sub-second feed-forward reconstruction; its `output_format` is a
 * two-value glb/obj enum, one of the four spellings that kept `format` out of the
 * vocabulary.
 *
 * Docs: https://fal.ai/models/fal-ai/triposr/api
 */
export interface FalAiTriposrInput {
  /**
   * Path for the image file to be processed. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  image_url: string;
  /** Output format for the 3D model. Default: `"glb"`. */
  output_format?: "glb" | "obj";
  /** Whether to remove the background from the input image. Default: `true`. */
  do_remove_background?: boolean;
  /** Ratio of the foreground image to the original image. Default: `0.9`. */
  foreground_ratio?: number;
  /** Resolution of the marching cubes. Above 512 is not recommended. Default: `256`. */
  mc_resolution?: number;
}

/**
 * The result document `fal-ai/triposr` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiTriposrOutput {
  /** Generated 3D object file. */
  model_mesh: FalFile;
  /** Inference timings. */
  timings: Record<string, number>;
  /** Directory containing textures for the remeshed model. */
  remeshing_dir?: FalFile | null;
}

/**
 * The request body `hitem3d/hi3d/v3.0/image-to-3d` accepts.
 *
 * Hi3D v3.0. `model` is a REAL wire field — a `const "hi3dv3.0"` naming the checkpoint —
 * and stays on the wire while the route is still selected by the `endpoint` pseudo-param.
 * Allow-listed under R6, reviewed 2026-08-25 against the live schema.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/hitem3d/v3.0/image-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/hitem3d/v3.0/image-to-3d/api
 */
export interface Hitem3dHi3dV30ImageTo3dInput {
  /** Hi3D v3.0 model version used by this endpoint. Default: `"hi3dv3.0"`. */
  model?: "hi3dv3.0";
  /**
   * Generation resolution. 2048quality is the faster default; 2048master provides the
   * highest quality. Default: `"2048quality"`.
   */
  resolution?: "2048quality" | "2048master";
  /**
   * Whether to generate textures in addition to geometry. When false, only the geometry mesh
   * is generated (and billed). Default: `true`.
   */
  enable_texture?: boolean;
  /**
   * Generate PBR material maps together with the texture. Only supported (and billed) for
   * v2.0/v2.1 and v3.0 models when enable_texture is true; ignored otherwise. Default:
   * `true`.
   */
  enable_pbr?: boolean;
  /**
   * Target face count. The partner recommends 2,000,000 for 2048quality and 5,000,000 for
   * 2048master.
   */
  face_count?: number | null;
  /** File format of the generated model. Default: `"glb"`. */
  export_format?: "glb" | "obj" | "stl" | "fbx" | "usdz";
  /** If set to true, input images are checked for safety before processing. Default: `true`. */
  enable_safety_checker?: boolean;
  /**
   * URL of the input image. PNG, JPEG and WebP formats are supported, up to 20MB. Carries a
   * image reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /** De-shading strength. Default: `0.5`. */
  shading?: number;
}

/**
 * The result document `hitem3d/hi3d/v3.0/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Hitem3dHi3dV30ImageTo3dOutput {
  /** Generated 3D model file. */
  model_mesh: FalFile;
  /** Preview image of the generated model, when available. */
  thumbnail?: FalFile | null;
}

/**
 * The request body `meshy/v7/image-to-3d` accepts.
 *
 * Meshy 7, image-driven, and the only row here whose extras reach past geometry into
 * RIGGING and animation (`enable_rigging`, `animation_action_id`). Those are 3d-to-3d
 * operations bolted onto a generation call; unmodel serves the generation and leaves the
 * rest as extras.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/meshy/v7/image-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/meshy/v7/image-to-3d/api
 */
export interface MeshyV7ImageTo3dInput {
  /**
   * Image URL or base64 data URI for 3D model creation. Supports .jpg, .jpeg, and .png
   * formats. Also supports AVIF and HEIF formats which will be automatically converted.
   * Carries a image reference — an https URL or a `data:` URI.
   */
  image_url: string;
  /**
   * Type of 3D mesh generation. 'standard' produces a regular high-detail mesh; 'lowpoly'
   * produces a low-poly mesh optimized for cleaner polygons; 'smart-topology' uses Meshy-T2
   * for clean, natively separated parts. When set to 'lowpoly', the remesh controls
   * (topology, target_polycount, should_remesh) are ignored by Meshy. Default: `"standard"`.
   */
  model_type?: "standard" | "lowpoly" | "smart-topology";
  /**
   * Specify the topology of the generated model. Quad for smooth surfaces, Triangle for
   * detailed geometry. Default: `"triangle"`.
   */
  topology?: "quad" | "triangle";
  /**
   * Target number of polygons in the generated model. When model_type is 'smart-topology'
   * and this is left unset, Meshy's lower smart-topology default (4,000) is used instead;
   * smart topology accepts at most 15,000. Default: `30000`.
   */
  target_polycount?: number;
  /**
   * Controls symmetry behavior during model generation. Off disables symmetry, Auto
   * determines it automatically, On enforces symmetry. Default: `"auto"`.
   */
  symmetry_mode?: "off" | "auto" | "on";
  /** Whether to enable the remesh phase. Default: `true`. */
  should_remesh?: boolean;
  /** Whether to generate textures. Default: `true`. */
  should_texture?: boolean;
  /**
   * Generate PBR Maps (metallic, roughness, normal) in addition to base color. Default:
   * `false`.
   */
  enable_pbr?: boolean;
  /**
   * Deprecated: use pose_mode instead. When true, generates a T-pose model. Default:
   * `false`.
   */
  is_a_t_pose?: boolean;
  /**
   * Pose mode for the generated model. 'a-pose' generates an A-pose, 't-pose' generates a
   * T-pose, empty string for no specific pose. Default: `""`.
   */
  pose_mode?: "a-pose" | "t-pose" | "";
  /** Text prompt to guide the texturing process. */
  texture_prompt?: string | null;
  /**
   * 2D image to guide the texturing process. Carries a image reference — an https URL or a
   * `data:` URI.
   */
  texture_image_url?: string | null;
  /**
   * Automatically rig the generated model as a humanoid character. Includes basic walking
   * and running animations. Best results with humanoid characters that have clearly defined
   * limbs. Default: `false`.
   */
  enable_rigging?: boolean;
  /**
   * Approximate height of the character in meters. Only used when enable_rigging is true.
   * Default: `1.7`.
   */
  rigging_height_meters?: number;
  /**
   * Apply an animation preset to the rigged model. Requires enable_rigging to be true.
   * Default: `false`.
   */
  enable_animation?: boolean;
  /**
   * Animation preset ID from Meshy's library. Only used when enable_animation is true; in
   * that case it must be in [0, 696] (``0`` is the "Idle" preset) and is otherwise rejected
   * with a 422. See https://docs.meshy.ai/en/api/animation-library for available action IDs.
   * Default: `92`.
   */
  animation_action_id?: number;
  /**
   * If set to true, input data will be checked for safety before processing. Default:
   * `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * Enable higher-fidelity geometry with finer surface detail. Only available for standard
   * generation (ultra requires Meshy-7; it cannot be combined with smart topology or
   * lowpoly). Default: `false`.
   */
  ultra_mode?: boolean;
}

/**
 * The result document `meshy/v7/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface MeshyV7ImageTo3dOutput {
  /** Animated 3D model in GLB format. Only present when enable_animation is true. */
  animation_glb?: FalFile | null;
  /** Animated 3D model in FBX format. Only present when enable_animation is true. */
  animation_fbx?: FalFile | null;
  /** Generated 3D object in GLB format. */
  model_glb: FalFile;
  /** Preview thumbnail of the generated model. */
  thumbnail?: FalFile | null;
  /** URLs for different 3D model formats. */
  model_urls: FalModelUrls_a3b9bc;
  /** Array of texture file objects, matching Meshy API structure. */
  texture_urls?: FalTextureFiles[];
  /** The seed used for generation (if available) */
  seed?: number | null;
  /** Rigged character in GLB format. Only present when enable_rigging is true. */
  rigged_character_glb?: FalFile | null;
  /** Rigged character in FBX format. Only present when enable_rigging is true. */
  rigged_character_fbx?: FalFile | null;
  /** Basic walking and running animations. Only present when enable_rigging is true. */
  basic_animations?: FalBasicAnimations | null;
  /** Rigging task ID. Only present when enable_rigging is true. */
  rig_task_id?: string | null;
}

/**
 * The request body `meshy/v7/text-to-3d` accepts.
 *
 * The text-driven half of Meshy 7. Note it publishes both `prompt` and `texture_prompt` —
 * two texts meaning different things, which is why the canonical `prompt` is named per row
 * rather than guessed.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/meshy/v7/text-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/meshy/v7/text-to-3d/api
 */
export interface MeshyV7TextTo3dInput {
  /** Describe what kind of object the 3D model is. Maximum 600 characters. */
  prompt: string;
  /**
   * Generation mode. 'preview' returns untextured geometry only, 'full' returns textured
   * model (preview + refine). Default: `"full"`.
   */
  mode?: "preview" | "full";
  /** Seed for reproducible results. Same prompt and seed usually generate the same result. */
  seed?: number | null;
  /**
   * Type of 3D mesh generation. 'standard' produces a regular high-detail mesh; 'lowpoly'
   * produces a low-poly mesh optimized for cleaner polygons; 'smart-topology' uses Meshy-T2
   * for clean, natively separated parts on the geometry (preview) step. When set to
   * 'lowpoly', the remesh controls (topology, target_polycount, should_remesh) are ignored
   * by Meshy. Default: `"standard"`.
   */
  model_type?: "standard" | "lowpoly" | "smart-topology";
  /**
   * Specify the topology of the generated model. Quad for smooth surfaces, Triangle for
   * detailed geometry. Default: `"triangle"`.
   */
  topology?: "quad" | "triangle";
  /**
   * Target number of polygons in the generated model. When model_type is 'smart-topology'
   * and this is left unset, Meshy's lower smart-topology default (4,000) is used instead;
   * smart topology accepts at most 15,000. Default: `30000`.
   */
  target_polycount?: number;
  /**
   * Whether to enable the remesh phase. When false, returns unprocessed triangular mesh.
   * Default: `true`.
   */
  should_remesh?: boolean;
  /** Controls symmetry behavior during model generation. Default: `"auto"`. */
  symmetry_mode?: "off" | "auto" | "on";
  /**
   * Generate PBR Maps (metallic, roughness, normal) in addition to base color. Default:
   * `false`.
   */
  enable_pbr?: boolean;
  /**
   * Deprecated: use pose_mode instead. When true, generates a T-pose model. Default:
   * `false`.
   */
  is_a_t_pose?: boolean;
  /**
   * Pose mode for the generated model. 'a-pose' generates an A-pose, 't-pose' generates a
   * T-pose, empty string for no specific pose. Default: `""`.
   */
  pose_mode?: "a-pose" | "t-pose" | "";
  /**
   * Whether to enable prompt expansion. This will use a large language model to expand the
   * prompt with additional details while maintaining the original meaning. Default: `false`.
   */
  enable_prompt_expansion?: boolean;
  /** Additional text prompt to guide the texturing process (only used in 'full' mode) */
  texture_prompt?: string | null;
  /**
   * 2D image to guide the texturing process (only used in 'full' mode) Carries a image
   * reference — an https URL or a `data:` URI.
   */
  texture_image_url?: string | null;
  /**
   * Automatically rig the generated model as a humanoid character. Includes basic walking
   * and running animations. Best results with humanoid characters that have clearly defined
   * limbs. Default: `false`.
   */
  enable_rigging?: boolean;
  /**
   * Approximate height of the character in meters. Only used when enable_rigging is true.
   * Default: `1.7`.
   */
  rigging_height_meters?: number;
  /**
   * Apply an animation preset to the rigged model. Requires enable_rigging to be true.
   * Default: `false`.
   */
  enable_animation?: boolean;
  /**
   * Animation preset ID from Meshy's library. Only used when enable_animation is true; in
   * that case it must be in [0, 696] (``0`` is the "Idle" preset) and is otherwise rejected
   * with a 422. See https://docs.meshy.ai/en/api/animation-library for available action IDs.
   * Default: `92`.
   */
  animation_action_id?: number;
  /**
   * If set to true, input data will be checked for safety before processing. Default:
   * `true`.
   */
  enable_safety_checker?: boolean;
  /**
   * Enable higher-fidelity geometry with finer surface detail on the preview (geometry)
   * step. Only available for standard generation (ultra requires Meshy-7; it cannot be
   * combined with smart topology or lowpoly). Default: `false`.
   */
  ultra_mode?: boolean;
}

/**
 * The result document `meshy/v7/text-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface MeshyV7TextTo3dOutput {
  /** Animated 3D model in GLB format. Only present when enable_animation is true. */
  animation_glb?: FalFile | null;
  /** Animated 3D model in FBX format. Only present when enable_animation is true. */
  animation_fbx?: FalFile | null;
  /** Generated 3D object in GLB format. */
  model_glb: FalFile;
  /** Preview thumbnail of the generated model. */
  thumbnail?: FalFile | null;
  /** URLs for different 3D model formats. */
  model_urls: FalModelUrls_a3b9bc;
  /** Array of texture file objects. */
  texture_urls?: FalTextureFiles[];
  /** The seed used for generation. */
  seed?: number | null;
  /** The text prompt used for generation. */
  prompt: string;
  /** The actual prompt used if prompt expansion was enabled. */
  actual_prompt?: string | null;
  /** Rigged character in GLB format. Only present when enable_rigging is true. */
  rigged_character_glb?: FalFile | null;
  /** Rigged character in FBX format. Only present when enable_rigging is true. */
  rigged_character_fbx?: FalFile | null;
  /** Basic walking and running animations. Only present when enable_rigging is true. */
  basic_animations?: FalBasicAnimations | null;
  /** Rigging task ID. Only present when enable_rigging is true. */
  rig_task_id?: string | null;
}

/**
 * The request body `tripo3d/h3.1/image-to-3d` accepts.
 *
 * Tripo H3.1 through fal — the same model the NATIVE tripo3d provider serves as
 * `v3.1-20260211`, which is what makes this category's second witness a real comparison.
 * fal renames Tripo's polymorphic `input` to a plain `image_url` and drops
 * `smart_low_poly`, `generate_parts` and `compress`: exactly the kind of disagreement
 * `extras` exists for.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/tripo3d/h3.1/image-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/tripo3d/h3.1/image-to-3d/api
 */
export interface Tripo3dH31ImageTo3dInput {
  /**
   * URL of the input image for 3D model creation. Carries a image reference — an https URL
   * or a `data:` URI.
   */
  image_url: string;
  /**
   * Target number of faces for the generated mesh. If not set, the model adaptively
   * determines the count.
   */
  face_limit?: number | null;
  /** Whether to generate textures for the model. Default: `true`. */
  texture?: boolean;
  /**
   * Whether to generate PBR (Physically Based Rendering) materials. If true, texture is also
   * enabled. Default: `true`.
   */
  pbr?: boolean;
  /** Seed for geometry generation reproducibility. */
  model_seed?: number | null;
  /** Seed for texture generation reproducibility. */
  texture_seed?: number | null;
  /**
   * Quality level for textures. 'detailed' produces higher-resolution textures. Default:
   * `"standard"`.
   */
  texture_quality?: "standard" | "detailed";
  /** Quality level for geometry. Default: `"standard"`. */
  geometry_quality?: "standard" | "detailed";
  /**
   * How textures are aligned. 'original_image' aligns to input image, 'geometry' aligns to
   * generated geometry. Default: `"original_image"`.
   */
  texture_alignment?: "original_image" | "geometry";
  /** Auto-scale the model to real-world dimensions (meters). Default: `false`. */
  auto_size?: boolean;
  /**
   * Model orientation. 'align_image' auto-rotates to match the input image. Default:
   * `"default"`.
   */
  orientation?: "default" | "align_image";
  /**
   * Generate quad (4-sided) mesh topology instead of triangles. When Tripo emits FBX bytes
   * for quad output, the returned model file is FBX. Default: `false`.
   */
  quad?: boolean;
}

/**
 * The result document `tripo3d/h3.1/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dH31ImageTo3dOutput {
  /** Generated 3D model file. May be GLB or FBX depending on Tripo output. */
  model_mesh: FalFile;
  /** URLs for different 3D model variants. */
  model_urls: FalModelUrls_3cc361;
  /** Preview render of the generated 3D model. */
  rendered_image?: FalFile | null;
}

/**
 * The request body `tripo3d/h3.1/text-to-3d` accepts.
 *
 * The text-driven half of the same model. Publishes THREE seeds — `model_seed`,
 * `image_seed`, `texture_seed` — and the canonical `seed` maps to the geometry one; the
 * other two are extras.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/tripo3d/h3.1/text-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/tripo3d/h3.1/text-to-3d/api
 */
export interface Tripo3dH31TextTo3dInput {
  /** Text description of the 3D object to generate. Maximum 1024 characters. */
  prompt: string;
  /** Text describing features to avoid in the generated model. */
  negative_prompt?: string | null;
  /**
   * Target number of faces for the generated mesh. If not set, the model adaptively
   * determines the count.
   */
  face_limit?: number | null;
  /** Whether to generate textures for the model. Default: `true`. */
  texture?: boolean;
  /**
   * Whether to generate PBR (Physically Based Rendering) materials. If true, texture is also
   * enabled. Default: `true`.
   */
  pbr?: boolean;
  /** Seed for geometry generation reproducibility. */
  model_seed?: number | null;
  /** Seed for the text-to-image step. */
  image_seed?: number | null;
  /** Seed for texture generation reproducibility. */
  texture_seed?: number | null;
  /**
   * Quality level for textures. 'detailed' produces higher-resolution textures. Default:
   * `"standard"`.
   */
  texture_quality?: "standard" | "detailed";
  /** Quality level for geometry. Default: `"standard"`. */
  geometry_quality?: "standard" | "detailed";
  /** Auto-scale the model to real-world dimensions (meters). Default: `false`. */
  auto_size?: boolean;
  /**
   * Generate quad (4-sided) mesh topology instead of triangles. When Tripo emits FBX bytes
   * for quad output, the returned model file is FBX. Default: `false`.
   */
  quad?: boolean;
}

/**
 * The result document `tripo3d/h3.1/text-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dH31TextTo3dOutput {
  /** Generated 3D model file. May be GLB or FBX depending on Tripo output. */
  model_mesh: FalFile;
  /** URLs for different 3D model variants. */
  model_urls: FalModelUrls_3cc361;
  /** Preview render of the generated 3D model. */
  rendered_image?: FalFile | null;
}

/**
 * The request body `tripo3d/p1/image-to-3d` accepts.
 *
 * Tripo P1, the low-poly series: four parameters, and a `face_limit` in the thousands
 * where the H series takes millions. The narrowest surface in the category.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/tripo3d/p1/image-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/tripo3d/p1/image-to-3d/api
 */
export interface Tripo3dP1ImageTo3dInput {
  /**
   * URL of the input image for 3D model creation. Carries a image reference — an https URL
   * or a `data:` URI.
   */
  image_url: string;
  /**
   * Target number of faces for the generated mesh. If not set, the model adaptively
   * determines the count.
   */
  face_limit?: number | null;
  /** Whether to generate textures for the model. Default: `true`. */
  texture?: boolean;
  /** Seed for geometry generation reproducibility. */
  model_seed?: number | null;
}

/**
 * The result document `tripo3d/p1/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dP1ImageTo3dOutput {
  /** Generated 3D model file. May be GLB or FBX depending on Tripo output. */
  model_mesh: FalFile;
  /** URLs for different 3D model variants. */
  model_urls: FalModelUrls_3cc361;
  /** Preview render of the generated 3D model. */
  rendered_image?: FalFile | null;
}

/**
 * The request body `tripo3d/p1/text-to-3d` accepts.
 *
 * The text-driven half of P1.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/tripo3d/p1/text-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/tripo3d/p1/text-to-3d/api
 */
export interface Tripo3dP1TextTo3dInput {
  /** Text description of the 3D object to generate. Maximum 1024 characters. */
  prompt: string;
  /**
   * Target number of faces for the generated mesh. If not set, the model adaptively
   * determines the count.
   */
  face_limit?: number | null;
  /** Whether to generate textures for the model. Default: `true`. */
  texture?: boolean;
  /** Seed for geometry generation reproducibility. */
  model_seed?: number | null;
}

/**
 * The result document `tripo3d/p1/text-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dP1TextTo3dOutput {
  /** Generated 3D model file. May be GLB or FBX depending on Tripo output. */
  model_mesh: FalFile;
  /** URLs for different 3D model variants. */
  model_urls: FalModelUrls_3cc361;
  /** Preview render of the generated 3D model. */
  rendered_image?: FalFile | null;
}

/**
 * The request body `tripo3d/tripo/v2.5/image-to-3d` accepts.
 *
 * Tripo v2.5, the legacy generation, kept because it is still the default many callers
 * have pinned and because its `style` enum is the one deprecated field in this roster.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `Tripo3d/tripo/v2.5/image-to-3d` its OpenAPI document is written against. unmodel
 * submits to the published id — that is the one fal documents and the one this catalog is
 * keyed on.
 *
 * Docs: https://fal.ai/models/Tripo3d/tripo/v2.5/image-to-3d/api
 */
export interface Tripo3dTripoV25ImageTo3dInput {
  /**
   * This is the random seed for model generation. The seed controls the geometry generation
   * process, ensuring identical models when the same seed is used. This parameter is an
   * integer and is randomly chosen if not set.
   */
  seed?: number | null;
  /**
   * Limits the number of faces on the output model. If this option is not set, the face
   * limit will be adaptively determined.
   */
  face_limit?: number | null;
  /**
   * A boolean option to enable pbr. The default value is True, set False to get a model
   * without pbr. If this option is set to True, texture will be ignored and used as True.
   * Default: `false`.
   */
  pbr?: boolean | null;
  /**
   * An option to enable texturing. Default is 'standard', set 'no' to get a model without
   * any textures, and set 'HD' to get a model with hd quality textures. Default:
   * `"standard"`.
   */
  texture?: "no" | "standard" | "HD";
  /**
   * This is the random seed for texture generation. Using the same seed will produce
   * identical textures. This parameter is an integer and is randomly chosen if not set. If
   * you want a model with different textures, please use same seed and different
   * texture_seed.
   */
  texture_seed?: number | null;
  /**
   * Automatically scale the model to real-world dimensions, with the unit in meters. The
   * default value is False. Default: `false`.
   */
  auto_size?: boolean | null;
  /**
   * [DEPRECATED] Defines the artistic style or transformation to be applied to the 3D model,
   * altering its appearance according to preset options (extra $0.05 per generation). Omit
   * this option to keep the original style and apperance.
   */
  style?: "person:person2cartoon" | "object:clay" | "object:steampunk" | "animal:venom" | "object:barbie" | "object:christmas" | "gold" | "ancient_bronze" | null;
  /**
   * Set True to enable quad mesh output (extra $0.05 per generation). If quad=True and
   * face_limit is not set, the default face_limit will be 10000. Note: Enabling this option
   * will force the output to be an FBX model. Default: `false`.
   */
  quad?: boolean | null;
  /**
   * Determines the prioritization of texture alignment in the 3D model. The default value is
   * original_image. Default: `"original_image"`.
   */
  texture_alignment?: "original_image" | "geometry" | null;
  /**
   * Set orientation=align_image to automatically rotate the model to align the original
   * image. The default value is default. Default: `"default"`.
   */
  orientation?: "default" | "align_image" | null;
  /**
   * URL of the image to use for model generation. Carries a image reference — an https URL
   * or a `data:` URI.
   */
  image_url: string;
}

/**
 * The result document `tripo3d/tripo/v2.5/image-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dTripoV25ImageTo3dOutput {
  /** The task id of the 3D model generation. */
  task_id: string;
  /** Model. */
  model_mesh?: FalFile | null;
  /** Base model. */
  base_model?: FalFile | null;
  /** Pbr model. */
  pbr_model?: FalFile | null;
  /** A preview image of the model. */
  rendered_image?: FalFile | null;
}

/**
 * The request body `tripo3d/tripo/v2.5/multiview-to-3d` accepts.
 *
 * The multiview arm that unmodel CAN serve: `front_image_url` is required and the other
 * three angles are optional siblings, so the canonical `image` is the front view and the
 * rest ride as extras. Contrast tripo3d/h3.1/multiview-to-3d in `excluded`.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `Tripo3d/tripo/v2.5/multiview-to-3d` its OpenAPI document is written against.
 * unmodel submits to the published id — that is the one fal documents and the one this
 * catalog is keyed on.
 *
 * Docs: https://fal.ai/models/Tripo3d/tripo/v2.5/multiview-to-3d/api
 */
export interface Tripo3dTripoV25MultiviewTo3dInput {
  /**
   * This is the random seed for model generation. The seed controls the geometry generation
   * process, ensuring identical models when the same seed is used. This parameter is an
   * integer and is randomly chosen if not set.
   */
  seed?: number | null;
  /**
   * Limits the number of faces on the output model. If this option is not set, the face
   * limit will be adaptively determined.
   */
  face_limit?: number | null;
  /**
   * A boolean option to enable pbr. The default value is True, set False to get a model
   * without pbr. If this option is set to True, texture will be ignored and used as True.
   * Default: `false`.
   */
  pbr?: boolean | null;
  /**
   * An option to enable texturing. Default is 'standard', set 'no' to get a model without
   * any textures, and set 'HD' to get a model with hd quality textures. Default:
   * `"standard"`.
   */
  texture?: "no" | "standard" | "HD";
  /**
   * This is the random seed for texture generation. Using the same seed will produce
   * identical textures. This parameter is an integer and is randomly chosen if not set. If
   * you want a model with different textures, please use same seed and different
   * texture_seed.
   */
  texture_seed?: number | null;
  /**
   * Automatically scale the model to real-world dimensions, with the unit in meters. The
   * default value is False. Default: `false`.
   */
  auto_size?: boolean | null;
  /**
   * [DEPRECATED] Defines the artistic style or transformation to be applied to the 3D model,
   * altering its appearance according to preset options (extra $0.05 per generation). Omit
   * this option to keep the original style and apperance.
   */
  style?: "person:person2cartoon" | "object:clay" | "object:steampunk" | "animal:venom" | "object:barbie" | "object:christmas" | "gold" | "ancient_bronze" | null;
  /**
   * Set True to enable quad mesh output (extra $0.05 per generation). If quad=True and
   * face_limit is not set, the default face_limit will be 10000. Note: Enabling this option
   * will force the output to be an FBX model. Default: `false`.
   */
  quad?: boolean | null;
  /**
   * Determines the prioritization of texture alignment in the 3D model. The default value is
   * original_image. Default: `"original_image"`.
   */
  texture_alignment?: "original_image" | "geometry" | null;
  /**
   * Set orientation=align_image to automatically rotate the model to align the original
   * image. The default value is default. Default: `"default"`.
   */
  orientation?: "default" | "align_image" | null;
  /**
   * Front view image of the object. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  front_image_url: string;
  /**
   * Left view image of the object. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  left_image_url?: string | null;
  /**
   * Back view image of the object. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  back_image_url?: string | null;
  /**
   * Right view image of the object. Carries a image reference — an https URL or a `data:`
   * URI.
   */
  right_image_url?: string | null;
}

/**
 * The result document `tripo3d/tripo/v2.5/multiview-to-3d` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface Tripo3dTripoV25MultiviewTo3dOutput {
  /** The task id of the 3D model generation. */
  task_id: string;
  /** Model. */
  model_mesh?: FalFile | null;
  /** Base model. */
  base_model?: FalFile | null;
  /** Pbr model. */
  pbr_model?: FalFile | null;
  /** A preview image of the model. */
  rendered_image?: FalFile | null;
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 19 object types re-instantiates at every
 * comparison site, while a keyed lookup resolves in one. The per-endpoint narrowing a
 * validator exposes indexes into this, so adding an endpoint costs one line here and
 * nothing at any call site.
 */
export interface FalThreeDBodyById {
  "fal-ai/hunyuan-3d/v3.1/pro/image-to-3d": FalAiHunyuan3dV31ProImageTo3dInput;
  "fal-ai/hunyuan-3d/v3.1/pro/text-to-3d": FalAiHunyuan3dV31ProTextTo3dInput;
  "fal-ai/hunyuan-3d/v3.1/rapid/image-to-3d": FalAiHunyuan3dV31RapidImageTo3dInput;
  "fal-ai/hunyuan3d/v2": FalAiHunyuan3dV2Input;
  "fal-ai/hunyuan3d/v2/turbo": FalAiHunyuan3dV2TurboInput;
  "fal-ai/hyper3d/rodin/v2.5": FalAiHyper3dRodinV25Input;
  "fal-ai/hyper3d/rodin/v2.5/text-to-3d": FalAiHyper3dRodinV25TextTo3dInput;
  "fal-ai/trellis": FalAiTrellisInput;
  "fal-ai/trellis-2": FalAiTrellis2Input;
  "fal-ai/triposr": FalAiTriposrInput;
  "hitem3d/hi3d/v3.0/image-to-3d": Hitem3dHi3dV30ImageTo3dInput;
  "meshy/v7/image-to-3d": MeshyV7ImageTo3dInput;
  "meshy/v7/text-to-3d": MeshyV7TextTo3dInput;
  "tripo3d/h3.1/image-to-3d": Tripo3dH31ImageTo3dInput;
  "tripo3d/h3.1/text-to-3d": Tripo3dH31TextTo3dInput;
  "tripo3d/p1/image-to-3d": Tripo3dP1ImageTo3dInput;
  "tripo3d/p1/text-to-3d": Tripo3dP1TextTo3dInput;
  "tripo3d/tripo/v2.5/image-to-3d": Tripo3dTripoV25ImageTo3dInput;
  "tripo3d/tripo/v2.5/multiview-to-3d": Tripo3dTripoV25MultiviewTo3dInput;
}

/** Endpoint id → its result document type. */
export interface FalThreeDResultById {
  "fal-ai/hunyuan-3d/v3.1/pro/image-to-3d": FalAiHunyuan3dV31ProImageTo3dOutput;
  "fal-ai/hunyuan-3d/v3.1/pro/text-to-3d": FalAiHunyuan3dV31ProTextTo3dOutput;
  "fal-ai/hunyuan-3d/v3.1/rapid/image-to-3d": FalAiHunyuan3dV31RapidImageTo3dOutput;
  "fal-ai/hunyuan3d/v2": FalAiHunyuan3dV2Output;
  "fal-ai/hunyuan3d/v2/turbo": FalAiHunyuan3dV2TurboOutput;
  "fal-ai/hyper3d/rodin/v2.5": FalAiHyper3dRodinV25Output;
  "fal-ai/hyper3d/rodin/v2.5/text-to-3d": FalAiHyper3dRodinV25TextTo3dOutput;
  "fal-ai/trellis": FalAiTrellisOutput;
  "fal-ai/trellis-2": FalAiTrellis2Output;
  "fal-ai/triposr": FalAiTriposrOutput;
  "hitem3d/hi3d/v3.0/image-to-3d": Hitem3dHi3dV30ImageTo3dOutput;
  "meshy/v7/image-to-3d": MeshyV7ImageTo3dOutput;
  "meshy/v7/text-to-3d": MeshyV7TextTo3dOutput;
  "tripo3d/h3.1/image-to-3d": Tripo3dH31ImageTo3dOutput;
  "tripo3d/h3.1/text-to-3d": Tripo3dH31TextTo3dOutput;
  "tripo3d/p1/image-to-3d": Tripo3dP1ImageTo3dOutput;
  "tripo3d/p1/text-to-3d": Tripo3dP1TextTo3dOutput;
  "tripo3d/tripo/v2.5/image-to-3d": Tripo3dTripoV25ImageTo3dOutput;
  "tripo3d/tripo/v2.5/multiview-to-3d": Tripo3dTripoV25MultiviewTo3dOutput;
}
