// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/fal-ai__bytedance__seed-speech__tts__v2.json
//   data/fal/openapi/fal-ai__chatterbox__text-to-speech.json
//   data/fal/openapi/fal-ai__chatterbox__text-to-speech__multilingual.json
//   data/fal/openapi/fal-ai__elevenlabs__tts__eleven-v3.json
//   data/fal/openapi/fal-ai__elevenlabs__tts__multilingual-v2.json
//   data/fal/openapi/fal-ai__elevenlabs__tts__turbo-v2.5.json
//   data/fal/openapi/fal-ai__gemini-3.1-flash-tts.json
//   data/fal/openapi/fal-ai__gemini-tts.json
//   data/fal/openapi/fal-ai__inworld-tts.json
//   data/fal/openapi/fal-ai__kokoro__american-english.json
//   data/fal/openapi/fal-ai__kokoro__brazilian-portuguese.json
//   data/fal/openapi/fal-ai__kokoro__british-english.json
//   data/fal/openapi/fal-ai__kokoro__french.json
//   data/fal/openapi/fal-ai__kokoro__hindi.json
//   data/fal/openapi/fal-ai__kokoro__italian.json
//   data/fal/openapi/fal-ai__kokoro__japanese.json
//   data/fal/openapi/fal-ai__kokoro__mandarin-chinese.json
//   data/fal/openapi/fal-ai__kokoro__spanish.json
//   data/fal/openapi/fal-ai__minimax__speech-02-hd.json
//   data/fal/openapi/fal-ai__minimax__speech-2.8-hd.json
//   data/fal/openapi/fal-ai__minimax__speech-2.8-turbo.json
//   data/fal/openapi/fal-ai__qwen-3-tts__text-to-speech__1.7b.json
//   data/fal/openapi/xai__tts__v1.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * Wire types for every `fal.tts` endpoint — TYPE ONLY.
 *
 * One interface per endpoint, straight off that endpoint's published schema, in fal's own
 * field order (`x-fal-order-properties`). Nothing here is a runtime value, so importing a
 * body type costs nothing.
 */

import type {
  FalAudioFile,
  FalAudioSetting_6f22a6,
  FalFile,
  FalLoudnessNormalizationSetting,
  FalOutputFormat,
  FalPronunciationDict,
  FalSpeakerConfig,
  FalVoiceModify,
  FalVoiceSetting,
} from "./shared.gen";

/**
 * The request body `fal-ai/bytedance/seed-speech/tts/v2` accepts.
 *
 * ByteDance Seed Speech v2: 41 voices whose names carry their languages, plus a flat
 * mp3/opus codec field.
 *
 * Docs: https://fal.ai/models/fal-ai/bytedance/seed-speech/tts/v2/api
 */
export interface FalAiBytedanceSeedSpeechTtsV2Input {
  /** The text to synthesize into speech. */
  text: string;
  /**
   * Voice to use for synthesis. The preset name encodes the voice and its supported language
   * codes. 'mixed_en_zh' means the voice can seamlessly blend English and Chinese; separate
   * codes (e.g. 'en_zh') mean the voice supports each language independently. Default:
   * `"stokie_en"`.
   */
  voice?: "vivi_mixed_en_zh_ja_es_id" | "mindy_en_es_id_pt_zh" | "stokie_en" | "dacey_en" | "tim_en" | "kian_en_zh" | "cedric_en_zh" | "sophie_en_zh" | "jean_en_zh" | "magnus_en_zh" | "mabel_en_zh" | "nadia_en_zh" | "opal_en_zh" | "pearl_en_zh" | "quentin_en_zh" | "vienna_mixed_en_zh" | "alina_mixed_en_zh" | "corinne_mixed_en_zh" | "esther_mixed_en_zh" | "freya_mixed_en_zh" | "gigi_mixed_en_zh" | "holly_mixed_en_zh" | "lyla_mixed_en_zh" | "daisy_mixed_en_zh" | "tracy_es_zh" | "jess_ja_es_id_pt_en_zh" | "pinky_es_ko_mixed_en_zh" | "sweety_ja_es" | "sandy_es_mixed_en_zh" | "sven_de" | "minimi_ja" | "usseau_fr" | "felipe_es" | "han_id" | "martins_pt" | "enzo_it" | "shane_ko" | "bonnie_zh" | "felix_zh" | "celeste_zh" | "monkey_king_zh";
  /**
   * Output audio format. 'mp3' returns MP3 audio; 'opus' returns Opus audio in an Ogg
   * container. Default: `"mp3"`.
   */
  output_format?: "mp3" | "opus";
  /** Sample rate of the output audio in Hz. Default: `24000`. */
  sample_rate?: 8000 | 16000 | 22050 | 24000 | 32000 | 44100 | 48000;
  /** Speech speed. 1.0 is normal speed, 0.5 is half speed, 2.0 is double speed. Default: `1`. */
  speed?: number;
  /** Volume. 1.0 is normal volume, 0.5 is half, 2.0 is double. Default: `1`. */
  volume?: number;
  /**
   * Voice pitch shift in semitones. 0 is normal pitch, -12 lowers by one octave, 12 raises
   * by one octave. Default: `0`.
   */
  pitch?: number;
  /**
   * Force the text to be read as a single language, disabling automatic language detection.
   * Leave unset for automatic detection (including seamless Chinese/English mixing on
   * bilingual voices). Codes: zh (Chinese), en (English), ja (Japanese), es-mx (Mexican
   * Spanish), id (Indonesian), pt-br (Brazilian Portuguese), ko (Korean), it (Italian), de
   * (German), fr (French).
   */
  language?: "zh" | "en" | "ja" | "es-mx" | "id" | "pt-br" | "ko" | "it" | "de" | "fr" | null;
  /**
   * Optional natural-language instruction that steers the delivery (tone, emotion, pace,
   * volume), e.g. 'Speak in a cheerful tone' or 'Could you speak a bit slower?'. It is not
   * spoken aloud and does not affect billing.
   */
  voice_instruction?: string | null;
}

/**
 * The result document `fal-ai/bytedance/seed-speech/tts/v2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiBytedanceSeedSpeechTtsV2Output {
  /** The generated audio file. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/chatterbox/text-to-speech` accepts.
 *
 * Chatterbox. Its `audio_url` is a reference voice to clone rather than an input to
 * transcribe, so it stays an extra: this category has no canonical word for "speak like
 * this recording".
 *
 * Docs: https://fal.ai/models/fal-ai/chatterbox/text-to-speech/api
 */
export interface FalAiChatterboxTextToSpeechInput {
  /**
   * The text to be converted to speech (maximum 5000 characters). You can additionally add
   * the following emotive tags: <laugh>, <chuckle>, <sigh>, <cough>, <sniffle>, <groan>,
   * <yawn>, <gasp>.
   */
  text: string;
  /**
   * Optional URL to an audio file to use as a reference for the generated speech. If
   * provided, the model will try to match the style and tone of the reference audio.
   * Default:
   * `"https://storage.googleapis.com/chatterbox-demo-samples/prompts/male_rickmorty.mp3"`.
   * Carries a audio reference — an https URL or a `data:` URI.
   */
  audio_url?: string | null;
  /**
   * Exaggeration factor for the generated speech (0.0 = no exaggeration, 1.0 = maximum
   * exaggeration). Default: `0.25`.
   */
  exaggeration?: number;
  /** Temperature for generation (higher = more creative). Default: `0.7`. */
  temperature?: number;
  /** Default: `0.5`. */
  cfg?: number;
  /**
   * Useful to control the reproducibility of the generated audio. Assuming all other
   * properties didn't change, a fixed seed should always generate the exact same audio file.
   * Set to 0 for random seed..
   */
  seed?: number | null;
}

/**
 * The result document `fal-ai/chatterbox/text-to-speech` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiChatterboxTextToSpeechOutput {
  /** The generated speech audio. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/chatterbox/text-to-speech/multilingual` accepts.
 *
 * Chatterbox multilingual — `voice` is a language NAME here ("english", "japanese") rather
 * than a speaker, which is why the row's voices and its languages are the same 23 words.
 *
 * Docs: https://fal.ai/models/fal-ai/chatterbox/text-to-speech/multilingual/api
 */
export interface FalAiChatterboxTextToSpeechMultilingualInput {
  /**
   * The text to be converted to speech (maximum 300 characters). Supports 23 languages
   * including English, French, German, Spanish, Italian, Portuguese, Hindi, Arabic, Chinese,
   * Japanese, Korean, and more.
   */
  text: string;
  /**
   * Language code for synthesis. In case using custom please provide audio url and select
   * custom_audio_language. Default: `"english"`.
   */
  voice?: string;
  /**
   * If using a custom audio URL, specify the language of the audio here. Ignored if voice is
   * not a custom url.
   */
  custom_audio_language?: "english" | "arabic" | "danish" | "german" | "greek" | "spanish" | "finnish" | "french" | "hebrew" | "hindi" | "italian" | "japanese" | "korean" | "malay" | "dutch" | "norwegian" | "polish" | "portuguese" | "russian" | "swedish" | "swahili" | "turkish" | "chinese" | null;
  /**
   * Controls speech expressiveness and emotional intensity (0.25-2.0). 0.5 is neutral,
   * higher values increase expressiveness. Extreme values may be unstable. Default: `0.5`.
   */
  exaggeration?: number;
  /**
   * Controls randomness and variation in generation (0.05-5.0). Higher values create more
   * varied speech patterns. Default: `0.8`.
   */
  temperature?: number;
  /**
   * Configuration/pace weight controlling generation guidance (0.0-1.0). Use 0.0 for
   * language transfer to mitigate accent inheritance. Default: `0.5`.
   */
  cfg_scale?: number;
  /**
   * Random seed for reproducible results. Set to 0 for random generation, or provide a
   * specific number for consistent outputs.
   */
  seed?: number | null;
}

/**
 * The result document `fal-ai/chatterbox/text-to-speech/multilingual` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiChatterboxTextToSpeechMultilingualOutput {
  /** The generated multilingual speech audio file. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/elevenlabs/tts/eleven-v3` accepts.
 *
 * ElevenLabs v3. Its voice field is an OPEN string (the catalog is per-account and
 * includes clones), so the row publishes no `voices` list and `voice` keeps the wide
 * vocabulary.
 *
 * Docs: https://fal.ai/models/fal-ai/elevenlabs/tts/eleven-v3/api
 */
export interface FalAiElevenlabsTtsElevenV3Input {
  /** The text to convert to speech. */
  text: string;
  /** The voice to use for speech generation. Default: `"Rachel"`. */
  voice?: string;
  /** Voice stability (0-1) Default: `0.5`. */
  stability?: number;
  /** Whether to return timestamps for each word in the generated speech. Default: `false`. */
  timestamps?: boolean;
  /** Language code (ISO 639-1) used to enforce a language for the model. */
  language_code?: string | null;
  /**
   * This parameter controls text normalization with three modes: 'auto', 'on', and 'off'.
   * When set to 'auto', the system will automatically decide whether to apply text
   * normalization (e.g., spelling out numbers). With 'on', text normalization will always be
   * applied, while with 'off', it will be skipped. Default: `"auto"`.
   */
  apply_text_normalization?: "auto" | "on" | "off";
}

/**
 * The result document `fal-ai/elevenlabs/tts/eleven-v3` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiElevenlabsTtsElevenV3Output {
  /** The generated audio file. */
  audio: FalFile;
  /**
   * Timestamps for each word in the generated speech. Only returned if `timestamps` is set
   * to True in the request.
   */
  timestamps?: unknown[] | null;
}

/**
 * The request body `fal-ai/elevenlabs/tts/multilingual-v2` accepts.
 *
 * ElevenLabs Multilingual v2 — the generation with `speed`, `style` and
 * `similarity_boost`.
 *
 * Docs: https://fal.ai/models/fal-ai/elevenlabs/tts/multilingual-v2/api
 */
export interface FalAiElevenlabsTtsMultilingualV2Input {
  /** The text to convert to speech. */
  text: string;
  /** The voice to use for speech generation. Default: `"Rachel"`. */
  voice?: string;
  /** Voice stability (0-1) Default: `0.5`. */
  stability?: number;
  /** Similarity boost (0-1) Default: `0.75`. */
  similarity_boost?: number;
  /** Style exaggeration (0-1) Default: `0`. */
  style?: number;
  /**
   * Speech speed (0.7-1.2). Values below 1.0 slow down the speech, above 1.0 speed it up.
   * Extreme values may affect quality. Default: `1`.
   */
  speed?: number;
  /** Whether to return timestamps for each word in the generated speech. Default: `false`. */
  timestamps?: boolean;
  /**
   * The text that came before the text of the current request. Can be used to improve the
   * speech's continuity when concatenating together multiple generations or to influence the
   * speech's continuity in the current generation.
   */
  previous_text?: string | null;
  /**
   * The text that comes after the text of the current request. Can be used to improve the
   * speech's continuity when concatenating together multiple generations or to influence the
   * speech's continuity in the current generation.
   */
  next_text?: string | null;
  /**
   * Language code (ISO 639-1) used to enforce a language for the model. An error will be
   * returned if language code is not supported by the model.
   */
  language_code?: string | null;
  /**
   * This parameter controls text normalization with three modes: 'auto', 'on', and 'off'.
   * When set to 'auto', the system will automatically decide whether to apply text
   * normalization (e.g., spelling out numbers). With 'on', text normalization will always be
   * applied, while with 'off', it will be skipped. Default: `"auto"`.
   */
  apply_text_normalization?: "auto" | "on" | "off";
}

/**
 * The result document `fal-ai/elevenlabs/tts/multilingual-v2` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiElevenlabsTtsMultilingualV2Output {
  /** The generated audio file. */
  audio: FalFile;
  /**
   * Timestamps for each word in the generated speech. Only returned if `timestamps` is set
   * to True in the request.
   */
  timestamps?: unknown[] | null;
}

/**
 * The request body `fal-ai/elevenlabs/tts/turbo-v2.5` accepts.
 *
 * ElevenLabs Turbo v2.5, at half the per-character rate of v3.
 *
 * Docs: https://fal.ai/models/fal-ai/elevenlabs/tts/turbo-v2.5/api
 */
export interface FalAiElevenlabsTtsTurboV25Input {
  /** The text to convert to speech. */
  text: string;
  /** The voice to use for speech generation. Default: `"Rachel"`. */
  voice?: string;
  /** Voice stability (0-1) Default: `0.5`. */
  stability?: number;
  /** Similarity boost (0-1) Default: `0.75`. */
  similarity_boost?: number;
  /** Style exaggeration (0-1) Default: `0`. */
  style?: number;
  /**
   * Speech speed (0.7-1.2). Values below 1.0 slow down the speech, above 1.0 speed it up.
   * Extreme values may affect quality. Default: `1`.
   */
  speed?: number;
  /** Whether to return timestamps for each word in the generated speech. Default: `false`. */
  timestamps?: boolean;
  /**
   * The text that came before the text of the current request. Can be used to improve the
   * speech's continuity when concatenating together multiple generations or to influence the
   * speech's continuity in the current generation.
   */
  previous_text?: string | null;
  /**
   * The text that comes after the text of the current request. Can be used to improve the
   * speech's continuity when concatenating together multiple generations or to influence the
   * speech's continuity in the current generation.
   */
  next_text?: string | null;
  /**
   * Language code (ISO 639-1) used to enforce a language for the model. An error will be
   * returned if language code is not supported by the model.
   */
  language_code?: string | null;
  /**
   * This parameter controls text normalization with three modes: 'auto', 'on', and 'off'.
   * When set to 'auto', the system will automatically decide whether to apply text
   * normalization (e.g., spelling out numbers). With 'on', text normalization will always be
   * applied, while with 'off', it will be skipped. Default: `"auto"`.
   */
  apply_text_normalization?: "auto" | "on" | "off";
}

/**
 * The result document `fal-ai/elevenlabs/tts/turbo-v2.5` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiElevenlabsTtsTurboV25Output {
  /** The generated audio file. */
  audio: FalFile;
  /**
   * Timestamps for each word in the generated speech. Only returned if `timestamps` is set
   * to True in the request.
   */
  timestamps?: unknown[] | null;
}

/**
 * The request body `fal-ai/gemini-3.1-flash-tts` accepts.
 *
 * Gemini 3.1 Flash TTS: thirty named voices and an 87-member language enum spelled as
 * English NAMES ("English (US)"), which the generator maps back to primary subtags.
 *
 * Docs: https://fal.ai/models/fal-ai/gemini-3.1-flash-tts/api
 */
export interface FalAiGemini31FlashTtsInput {
  /**
   * The text to convert to speech. Gemini 3.1 Flash TTS supports natural-language prompting
   * for style, pace, accent, and emotional expression — include delivery instructions inline
   * with the text (e.g. 'Say cheerfully: Have a wonderful day!'). Supports expressive audio
   * tags like [sigh], [laughing], [whispering], [short pause] for fine-grained control over
   * delivery. For multi-speaker synthesis, prefix lines with speaker aliases defined in the
   * speakers field (e.g. 'Alice: Hello!\nBob: Hi!').
   */
  prompt: string;
  /**
   * Optional style and delivery instructions prepended to the prompt. Controls
   * expressiveness, accent, pace, tone, and emotional expression using natural language. Use
   * this to separate style control from the text content. Examples: 'Speak warmly and
   * slowly', 'Read this as a dramatic newscast', 'Use a British accent with a cheerful
   * tone', 'Whisper mysteriously'.
   */
  style_instructions?: string | null;
  /**
   * Voice preset for single-speaker synthesis. 30 distinct voices are available. Ignored
   * when speakers is set. Popular choices: Kore (strong, firm female), Puck (upbeat, lively
   * male), Charon (calm, professional male), Zephyr (bright, clear female), Aoede (warm,
   * melodic female). Default: `"Kore"`.
   */
  voice?: "Achernar" | "Achird" | "Algenib" | "Algieba" | "Alnilam" | "Aoede" | "Autonoe" | "Callirrhoe" | "Charon" | "Despina" | "Enceladus" | "Erinome" | "Fenrir" | "Gacrux" | "Iapetus" | "Kore" | "Laomedeia" | "Leda" | "Orus" | "Pulcherrima" | "Puck" | "Rasalgethi" | "Sadachbia" | "Sadaltager" | "Schedar" | "Sulafat" | "Umbriel" | "Vindemiatrix" | "Zephyr" | "Zubenelgenubi";
  /**
   * Language for multilingual synthesis. When set, steers the model to speak in the
   * specified language. If not set, the model auto-detects the language from the text.
   */
  language_code?: "Arabic (Egypt)" | "Bangla (Bangladesh)" | "Dutch (Netherlands)" | "English (India)" | "English (US)" | "French (France)" | "German (Germany)" | "Hindi (India)" | "Indonesian (Indonesia)" | "Italian (Italy)" | "Japanese (Japan)" | "Korean (South Korea)" | "Marathi (India)" | "Polish (Poland)" | "Portuguese (Brazil)" | "Romanian (Romania)" | "Russian (Russia)" | "Spanish (Spain)" | "Tamil (India)" | "Telugu (India)" | "Thai (Thailand)" | "Turkish (Turkey)" | "Ukrainian (Ukraine)" | "Vietnamese (Vietnam)" | "Afrikaans (South Africa)" | "Albanian (Albania)" | "Amharic (Ethiopia)" | "Arabic (World)" | "Armenian (Armenia)" | "Azerbaijani (Azerbaijan)" | "Basque (Spain)" | "Belarusian (Belarus)" | "Bulgarian (Bulgaria)" | "Burmese (Myanmar)" | "Catalan (Spain)" | "Cebuano (Philippines)" | "Chinese Mandarin (China)" | "Chinese Mandarin (Taiwan)" | "Croatian (Croatia)" | "Czech (Czech Republic)" | "Danish (Denmark)" | "English (Australia)" | "English (UK)" | "Estonian (Estonia)" | "Filipino (Philippines)" | "Finnish (Finland)" | "French (Canada)" | "Galician (Spain)" | "Georgian (Georgia)" | "Greek (Greece)" | "Gujarati (India)" | "Haitian Creole (Haiti)" | "Hebrew (Israel)" | "Hungarian (Hungary)" | "Icelandic (Iceland)" | "Javanese (Java)" | "Kannada (India)" | "Konkani (India)" | "Lao (Laos)" | "Latin (Vatican City)" | "Latvian (Latvia)" | "Lithuanian (Lithuania)" | "Luxembourgish (Luxembourg)" | "Macedonian (North Macedonia)" | "Maithili (India)" | "Malagasy (Madagascar)" | "Malay (Malaysia)" | "Malayalam (India)" | "Mongolian (Mongolia)" | "Nepali (Nepal)" | "Norwegian Bokmal (Norway)" | "Norwegian Nynorsk (Norway)" | "Odia (India)" | "Pashto (Afghanistan)" | "Persian (Iran)" | "Portuguese (Portugal)" | "Punjabi (India)" | "Serbian (Serbia)" | "Sindhi (India)" | "Sinhala (Sri Lanka)" | "Slovak (Slovakia)" | "Slovenian (Slovenia)" | "Spanish (Latin America)" | "Spanish (Mexico)" | "Swahili (Kenya)" | "Swedish (Sweden)" | "Urdu (Pakistan)" | null;
  /**
   * Multi-speaker voice configuration. When set, enables multi-speaker synthesis where
   * different parts of the text are spoken by different voices. Each speaker needs a voice
   * and a speaker_id (alias) that matches prefixes in the prompt.
   */
  speakers?: FalSpeakerConfig[] | unknown[] | null;
  /**
   * Controls the randomness of the speech output. Higher values produce more creative and
   * varied delivery, while lower values make the output more predictable and focused.
   * Default: `1`.
   */
  temperature?: number;
  /**
   * Audio output format. mp3: compressed, small file size (recommended). wav: uncompressed
   * PCM wrapped in WAV (24 kHz, 16-bit mono). ogg_opus: Ogg container with Opus codec, good
   * quality-to-size ratio. Default: `"mp3"`.
   */
  output_format?: "wav" | "mp3" | "ogg_opus";
}

/**
 * The result document `fal-ai/gemini-3.1-flash-tts` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiGemini31FlashTtsOutput {
  /** The generated audio file. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/gemini-tts` accepts.
 *
 * Gemini TTS. `model` is a REAL wire field ("gemini-2.5-flash-tts" | "gemini-2.5-pro-tts")
 * choosing the tier, and it also doubles the price — the sync-lipsync/v2 situation in the
 * speech category. Allow-listed under R6, reviewed 2026-08-25.
 *
 * Docs: https://fal.ai/models/fal-ai/gemini-tts/api
 */
export interface FalAiGeminiTtsInput {
  /**
   * The text to convert to speech. Gemini TTS supports natural-language prompting for style,
   * pace, accent, and emotional expression — include delivery instructions inline with the
   * text (e.g. 'Say cheerfully: Have a wonderful day!'). For multi-speaker synthesis, prefix
   * lines with speaker aliases defined in the speakers field (e.g. 'Alice: Hello!\nBob:
   * Hi!'). Supports inline pace/style markers like [slowly], [whispering], [excited],
   * [extremely fast].
   */
  prompt: string;
  /**
   * Optional style and delivery instructions prepended to the prompt. Controls
   * expressiveness, accent, pace, tone, and emotional expression using natural language. Use
   * this to separate style control from the text content. Examples: 'Speak warmly and
   * slowly', 'Read this as a dramatic newscast', 'Use a British accent with a cheerful
   * tone', 'Whisper mysteriously'.
   */
  style_instructions?: string | null;
  /**
   * Voice preset for single-speaker synthesis. 30 distinct voices are available. Ignored
   * when speakers is set. Popular choices: Kore (strong, firm female), Puck (upbeat, lively
   * male), Charon (calm, professional male), Zephyr (bright, clear female), Aoede (warm,
   * melodic female). Default: `"Kore"`.
   */
  voice?: "Achernar" | "Achird" | "Algenib" | "Algieba" | "Alnilam" | "Aoede" | "Autonoe" | "Callirrhoe" | "Charon" | "Despina" | "Enceladus" | "Erinome" | "Fenrir" | "Gacrux" | "Iapetus" | "Kore" | "Laomedeia" | "Leda" | "Orus" | "Pulcherrima" | "Puck" | "Rasalgethi" | "Sadachbia" | "Sadaltager" | "Schedar" | "Sulafat" | "Umbriel" | "Vindemiatrix" | "Zephyr" | "Zubenelgenubi";
  /**
   * Which Gemini TTS model to use. gemini-2.5-flash-tts: low latency, cost-efficient for
   * everyday applications (recommended). gemini-2.5-pro-tts: highest quality, best for
   * structured workflows like podcasts, audiobooks, and customer support. Default:
   * `"gemini-2.5-flash-tts"`.
   */
  model?: "gemini-2.5-flash-tts" | "gemini-2.5-pro-tts";
  /**
   * Language for multilingual synthesis. When set, steers the model to speak in the
   * specified language. Supports 24 GA languages and 60+ Preview languages. If not set, the
   * model auto-detects the language from the text.
   */
  language_code?: "Arabic (Egypt)" | "Bangla (Bangladesh)" | "Dutch (Netherlands)" | "English (India)" | "English (US)" | "French (France)" | "German (Germany)" | "Hindi (India)" | "Indonesian (Indonesia)" | "Italian (Italy)" | "Japanese (Japan)" | "Korean (South Korea)" | "Marathi (India)" | "Polish (Poland)" | "Portuguese (Brazil)" | "Romanian (Romania)" | "Russian (Russia)" | "Spanish (Spain)" | "Tamil (India)" | "Telugu (India)" | "Thai (Thailand)" | "Turkish (Turkey)" | "Ukrainian (Ukraine)" | "Vietnamese (Vietnam)" | "Afrikaans (South Africa)" | "Albanian (Albania)" | "Amharic (Ethiopia)" | "Arabic (World)" | "Armenian (Armenia)" | "Azerbaijani (Azerbaijan)" | "Basque (Spain)" | "Belarusian (Belarus)" | "Bulgarian (Bulgaria)" | "Burmese (Myanmar)" | "Catalan (Spain)" | "Cebuano (Philippines)" | "Chinese Mandarin (China)" | "Chinese Mandarin (Taiwan)" | "Croatian (Croatia)" | "Czech (Czech Republic)" | "Danish (Denmark)" | "English (Australia)" | "English (UK)" | "Estonian (Estonia)" | "Filipino (Philippines)" | "Finnish (Finland)" | "French (Canada)" | "Galician (Spain)" | "Georgian (Georgia)" | "Greek (Greece)" | "Gujarati (India)" | "Haitian Creole (Haiti)" | "Hebrew (Israel)" | "Hungarian (Hungary)" | "Icelandic (Iceland)" | "Javanese (Java)" | "Kannada (India)" | "Konkani (India)" | "Lao (Laos)" | "Latin (Vatican City)" | "Latvian (Latvia)" | "Lithuanian (Lithuania)" | "Luxembourgish (Luxembourg)" | "Macedonian (North Macedonia)" | "Maithili (India)" | "Malagasy (Madagascar)" | "Malay (Malaysia)" | "Malayalam (India)" | "Mongolian (Mongolia)" | "Nepali (Nepal)" | "Norwegian Bokmal (Norway)" | "Norwegian Nynorsk (Norway)" | "Odia (India)" | "Pashto (Afghanistan)" | "Persian (Iran)" | "Portuguese (Portugal)" | "Punjabi (India)" | "Serbian (Serbia)" | "Sindhi (India)" | "Sinhala (Sri Lanka)" | "Slovak (Slovakia)" | "Slovenian (Slovenia)" | "Spanish (Latin America)" | "Spanish (Mexico)" | "Swahili (Kenya)" | "Swedish (Sweden)" | "Urdu (Pakistan)" | null;
  /**
   * Multi-speaker voice configuration. When set, enables multi-speaker synthesis where
   * different parts of the text are spoken by different voices. Each speaker needs a voice
   * and a speaker_id (alias) that matches prefixes in the prompt. Requires
   * gemini-2.5-pro-tts or gemini-2.5-flash-tts model. Not supported with
   * gemini-2.5-flash-lite-preview-tts.
   */
  speakers?: FalSpeakerConfig[] | null;
  /**
   * Controls the randomness of the speech output. Higher values produce more creative and
   * varied delivery, while lower values make the output more predictable and focused.
   * Default: `1`.
   */
  temperature?: number;
  /**
   * Audio output format. mp3: compressed, small file size (recommended). wav: uncompressed
   * PCM wrapped in WAV (24 kHz, 16-bit mono). ogg_opus: Ogg container with Opus codec, good
   * quality-to-size ratio. Default: `"mp3"`.
   */
  output_format?: "wav" | "mp3" | "ogg_opus";
}

/**
 * The result document `fal-ai/gemini-tts` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiGeminiTtsOutput {
  /** The generated audio file. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/inworld-tts` accepts.
 *
 * Inworld TTS: 118 named voices, each tagged with its language in the name ("Craig (en)").
 *
 * Docs: https://fal.ai/models/fal-ai/inworld-tts/api
 */
export interface FalAiInworldTtsInput {
  /** The text to synthesize into speech. Maximum 2000 characters. */
  text: string;
  /** The voice to use for synthesis. Default: `"Craig (en)"`. */
  voice?: "Loretta (en)" | "Darlene (en)" | "Marlene (en)" | "Hank (en)" | "Evelyn (en)" | "Celeste (en)" | "Pippa (en)" | "Tessa (en)" | "Liam (en)" | "Callum (en)" | "Hamish (en)" | "Abby (en)" | "Graham (en)" | "Rupert (en)" | "Mortimer (en)" | "Snik (en)" | "Anjali (en)" | "Saanvi (en)" | "Arjun (en)" | "Claire (en)" | "Oliver (en)" | "Simon (en)" | "Elliot (en)" | "James (en)" | "Serena (en)" | "Gareth (en)" | "Vinny (en)" | "Lauren (en)" | "Jessica (en)" | "Ethan (en)" | "Tyler (en)" | "Jason (en)" | "Chloe (en)" | "Veronica (en)" | "Victoria (en)" | "Miranda (en)" | "Sebastian (en)" | "Victor (en)" | "Malcolm (en)" | "Kayla (en)" | "Nate (en)" | "Jake (en)" | "Brian (en)" | "Amina (en)" | "Kelsey (en)" | "Derek (en)" | "Grant (en)" | "Evan (en)" | "Alex (en)" | "Ashley (en)" | "Craig (en)" | "Deborah (en)" | "Dennis (en)" | "Edward (en)" | "Elizabeth (en)" | "Hades (en)" | "Julia (en)" | "Pixie (en)" | "Mark (en)" | "Olivia (en)" | "Priya (en)" | "Ronald (en)" | "Sarah (en)" | "Shaun (en)" | "Theodore (en)" | "Timothy (en)" | "Wendy (en)" | "Dominus (en)" | "Hana (en)" | "Clive (en)" | "Carter (en)" | "Blake (en)" | "Luna (en)" | "Yichen (zh)" | "Xiaoyin (zh)" | "Xinyi (zh)" | "Jing (zh)" | "Erik (nl)" | "Katrien (nl)" | "Lennart (nl)" | "Lore (nl)" | "Alain (fr)" | "Hélène (fr)" | "Mathieu (fr)" | "Étienne (fr)" | "Johanna (de)" | "Josef (de)" | "Gianni (it)" | "Orietta (it)" | "Asuka (ja)" | "Satoshi (ja)" | "Hyunwoo (ko)" | "Minji (ko)" | "Seojun (ko)" | "Yoona (ko)" | "Szymon (pl)" | "Wojciech (pl)" | "Heitor (pt)" | "Maitê (pt)" | "Diego (es)" | "Lupita (es)" | "Miguel (es)" | "Rafael (es)" | "Svetlana (ru)" | "Elena (ru)" | "Dmitry (ru)" | "Nikolai (ru)" | "Riya (hi)" | "Manoj (hi)" | "Yael (he)" | "Oren (he)" | "Nour (ar)" | "Omar (ar)";
  /** The sample rate in Hz for the output audio. Default: `48000`. */
  sample_rate_hertz?: 8000 | 16000 | 24000 | 32000 | 40000 | 48000;
}

/**
 * The result document `fal-ai/inworld-tts` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiInworldTtsOutput {
  /** Generated audio file. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/american-english` accepts.
 *
 * Kokoro, one endpoint per language. Twenty American English voices — the largest of the
 * nine lists, and the reason `voices` is per model rather than per adapter.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/american-english/api
 */
export interface FalAiKokoroAmericanEnglishInput {
  /** Default: `""`. */
  prompt?: string;
  /** Voice ID for the desired voice. Default: `"af_heart"`. */
  voice?: "af_heart" | "af_alloy" | "af_aoede" | "af_bella" | "af_jessica" | "af_kore" | "af_nicole" | "af_nova" | "af_river" | "af_sarah" | "af_sky" | "am_adam" | "am_echo" | "am_eric" | "am_fenrir" | "am_liam" | "am_michael" | "am_onyx" | "am_puck" | "am_santa";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/american-english` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroAmericanEnglishOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/brazilian-portuguese` accepts.
 *
 * Kokoro Brazilian Portuguese — three voices. fal serves NINE Kokoro languages, not the
 * twelve the model's own card lists; the three the plan expected (German, Korean,
 * Vietnamese) have no endpoint here.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/brazilian-portuguese/api
 */
export interface FalAiKokoroBrazilianPortugueseInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "pf_dora" | "pm_alex" | "pm_santa";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/brazilian-portuguese` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroBrazilianPortugueseOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/british-english` accepts.
 *
 * Kokoro British English — eight voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/british-english/api
 */
export interface FalAiKokoroBritishEnglishInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "bf_alice" | "bf_emma" | "bf_isabella" | "bf_lily" | "bm_daniel" | "bm_fable" | "bm_george" | "bm_lewis";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/british-english` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroBritishEnglishOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/french` accepts.
 *
 * Kokoro French. Its `voice` is a CONST rather than an enum — one voice, `ff_siwis` —
 * which is the single-value-enum lowering seen from the unified side.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/french/api
 */
export interface FalAiKokoroFrenchInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "ff_siwis";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/french` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroFrenchOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/hindi` accepts.
 *
 * Kokoro Hindi — four voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/hindi/api
 */
export interface FalAiKokoroHindiInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "hf_alpha" | "hf_beta" | "hm_omega" | "hm_psi";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/hindi` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroHindiOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/italian` accepts.
 *
 * Kokoro Italian — two voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/italian/api
 */
export interface FalAiKokoroItalianInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "if_sara" | "im_nicola";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/italian` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroItalianOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/japanese` accepts.
 *
 * Kokoro Japanese — five voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/japanese/api
 */
export interface FalAiKokoroJapaneseInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "jf_alpha" | "jf_gongitsune" | "jf_nezumi" | "jf_tebukuro" | "jm_kumo";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/japanese` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroJapaneseOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/mandarin-chinese` accepts.
 *
 * Kokoro Mandarin Chinese — eight voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/mandarin-chinese/api
 */
export interface FalAiKokoroMandarinChineseInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "zf_xiaobei" | "zf_xiaoni" | "zf_xiaoxiao" | "zf_xiaoyi" | "zm_yunjian" | "zm_yunxi" | "zm_yunxia" | "zm_yunyang";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/mandarin-chinese` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroMandarinChineseOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/kokoro/spanish` accepts.
 *
 * Kokoro Spanish — three voices.
 *
 * Docs: https://fal.ai/models/fal-ai/kokoro/spanish/api
 */
export interface FalAiKokoroSpanishInput {
  prompt: string;
  /** Voice ID for the desired voice. */
  voice: "ef_dora" | "em_alex" | "em_santa";
  /** Speed of the generated audio. Default is 1.0. Default: `1`. */
  speed?: number;
}

/**
 * The result document `fal-ai/kokoro/spanish` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiKokoroSpanishOutput {
  /** The generated music. */
  audio: FalFile;
}

/**
 * The request body `fal-ai/minimax/speech-02-hd` accepts.
 *
 * Nested $ref inputs (VoiceSetting / AudioSetting / PronunciationDict) — the
 * component-lowering case. Its `output_format` is `url | hex`, a DELIVERY switch rather
 * than a codec, which is why the row publishes no codecs.
 *
 * Docs: https://fal.ai/models/fal-ai/minimax/speech-02-hd/api
 */
export interface FalAiMinimaxSpeech02HdInput {
  /** Text to convert to speech (max 5000 characters, minimum 1 non-whitespace character) */
  text: string;
  /**
   * Voice configuration settings. Default:
   * `{"english_normalization":false,"pitch":0,"speed":1,"voice_id":"Wise_Woman","vol":1}`.
   */
  voice_setting?: FalVoiceSetting;
  /** Audio configuration settings. */
  audio_setting?: FalAudioSetting_6f22a6;
  /** Enhance recognition of specified languages and dialects. */
  language_boost?: "Chinese" | "Chinese,Yue" | "English" | "Arabic" | "Russian" | "Spanish" | "French" | "Portuguese" | "German" | "Turkish" | "Dutch" | "Ukrainian" | "Vietnamese" | "Indonesian" | "Japanese" | "Italian" | "Korean" | "Thai" | "Polish" | "Romanian" | "Greek" | "Czech" | "Finnish" | "Hindi" | "Bulgarian" | "Danish" | "Hebrew" | "Malay" | "Slovak" | "Swedish" | "Croatian" | "Hungarian" | "Norwegian" | "Slovenian" | "Catalan" | "Nynorsk" | "Afrikaans" | "auto" | null;
  /** Format of the output content (non-streaming only) Default: `"hex"`. */
  output_format?: "url" | "hex";
  /** Custom pronunciation dictionary for text replacement. */
  pronunciation_dict?: FalPronunciationDict | null;
}

/**
 * The result document `fal-ai/minimax/speech-02-hd` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiMinimaxSpeech02HdOutput {
  /** The generated audio file. */
  audio: FalFile;
  /** Duration of the audio in milliseconds. */
  duration_ms: number;
}

/**
 * The request body `fal-ai/minimax/speech-2.8-hd` accepts.
 *
 * MiniMax Speech 2.8 HD. Its text param is `prompt` where the 02 generation spells it
 * `text`, and its voice lives NESTED in `voice_setting.voice_id` — so this row publishes
 * no `voiceWire` and the canonical `voice` is refused by name, pointing at
 * providerOptions.
 *
 * Docs: https://fal.ai/models/fal-ai/minimax/speech-2.8-hd/api
 */
export interface FalAiMinimaxSpeech28HdInput {
  /**
   * Text to convert to speech. Use `<#x#>` for pauses (x = 0.01-99.99 seconds). Supports
   * interjection tags: `(laughs)`, `(sighs)`, `(coughs)`, `(clears throat)`, `(gasps)`,
   * `(sniffs)`, `(groans)`, `(yawns)`.
   */
  prompt: string;
  /**
   * Voice configuration settings. Default:
   * `{"english_normalization":false,"pitch":0,"speed":1,"voice_id":"Wise_Woman","vol":1}`.
   */
  voice_setting?: FalVoiceSetting;
  /** Audio configuration settings. */
  audio_setting?: FalAudioSetting_6f22a6;
  /** Enhance recognition of specified languages and dialects. */
  language_boost?: "Chinese" | "Chinese,Yue" | "English" | "Arabic" | "Russian" | "Spanish" | "French" | "Portuguese" | "German" | "Turkish" | "Dutch" | "Ukrainian" | "Vietnamese" | "Indonesian" | "Japanese" | "Italian" | "Korean" | "Thai" | "Polish" | "Romanian" | "Greek" | "Czech" | "Finnish" | "Hindi" | "Bulgarian" | "Danish" | "Hebrew" | "Malay" | "Slovak" | "Swedish" | "Croatian" | "Hungarian" | "Norwegian" | "Slovenian" | "Catalan" | "Nynorsk" | "Afrikaans" | "auto" | null;
  /** Format of the output content (non-streaming only) Default: `"hex"`. */
  output_format?: "url" | "hex";
  /** Custom pronunciation dictionary for text replacement. */
  pronunciation_dict?: FalPronunciationDict | null;
  /** Loudness normalization settings for the audio. */
  normalization_setting?: FalLoudnessNormalizationSetting;
  /** Voice modification settings to adjust pitch, intensity, and timbre. */
  voice_modify?: FalVoiceModify | null;
}

/**
 * The result document `fal-ai/minimax/speech-2.8-hd` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiMinimaxSpeech28HdOutput {
  /** The generated audio file. */
  audio: FalFile;
  /** Duration of the audio in milliseconds. */
  duration_ms: number;
}

/**
 * The request body `fal-ai/minimax/speech-2.8-turbo` accepts.
 *
 * MiniMax Speech 2.8 Turbo — same surface as the HD arm at a lower rate.
 *
 * Docs: https://fal.ai/models/fal-ai/minimax/speech-2.8-turbo/api
 */
export interface FalAiMinimaxSpeech28TurboInput {
  /**
   * Text to convert to speech. Use `<#x#>` for pauses (x = 0.01-99.99 seconds). Supports
   * interjection tags: `(laughs)`, `(sighs)`, `(coughs)`, `(clears throat)`, `(gasps)`,
   * `(sniffs)`, `(groans)`, `(yawns)`.
   */
  prompt: string;
  /**
   * Voice configuration settings. Default:
   * `{"english_normalization":false,"pitch":0,"speed":1,"voice_id":"Wise_Woman","vol":1}`.
   */
  voice_setting?: FalVoiceSetting;
  /** Audio configuration settings. */
  audio_setting?: FalAudioSetting_6f22a6;
  /** Enhance recognition of specified languages and dialects. */
  language_boost?: "Chinese" | "Chinese,Yue" | "English" | "Arabic" | "Russian" | "Spanish" | "French" | "Portuguese" | "German" | "Turkish" | "Dutch" | "Ukrainian" | "Vietnamese" | "Indonesian" | "Japanese" | "Italian" | "Korean" | "Thai" | "Polish" | "Romanian" | "Greek" | "Czech" | "Finnish" | "Hindi" | "Bulgarian" | "Danish" | "Hebrew" | "Malay" | "Slovak" | "Swedish" | "Croatian" | "Hungarian" | "Norwegian" | "Slovenian" | "Catalan" | "Nynorsk" | "Afrikaans" | "auto" | null;
  /** Format of the output content (non-streaming only) Default: `"hex"`. */
  output_format?: "url" | "hex";
  /** Custom pronunciation dictionary for text replacement. */
  pronunciation_dict?: FalPronunciationDict | null;
  /** Loudness normalization settings for the audio. */
  normalization_setting?: FalLoudnessNormalizationSetting;
  /** Voice modification settings to adjust pitch, intensity, and timbre. */
  voice_modify?: FalVoiceModify | null;
}

/**
 * The result document `fal-ai/minimax/speech-2.8-turbo` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiMinimaxSpeech28TurboOutput {
  /** The generated audio file. */
  audio: FalFile;
  /** Duration of the audio in milliseconds. */
  duration_ms: number;
}

/**
 * The request body `fal-ai/qwen-3-tts/text-to-speech/1.7b` accepts.
 *
 * Qwen 3 TTS (1.7B). Declares BOTH `text` and `prompt`: `text` is what is spoken and
 * `prompt` is a style instruction, so the curated textParam is `text` and `prompt` stays a
 * per-model extra.
 *
 * Docs: https://fal.ai/models/fal-ai/qwen-3-tts/text-to-speech/1.7b/api
 */
export interface FalAiQwen3TtsTextToSpeech17bInput {
  /** The text to be converted to speech. */
  text: string;
  /**
   * Optional prompt to guide the style of the generated speech. This prompt will be ignored
   * if a speaker embedding is provided.
   */
  prompt?: string | null;
  /**
   * The voice to be used for speech synthesis, will be ignored if a speaker embedding is
   * provided. Check out the
   * **[documentation](https://github.com/QwenLM/Qwen3-TTS/tree/main?tab=readme-ov-file#custom-voice-generate)**
   * for each voice's details and which language they primarily support.
   */
  voice?: "Vivian" | "Serena" | "Uncle_Fu" | "Dylan" | "Eric" | "Ryan" | "Aiden" | "Ono_Anna" | "Sohee" | null;
  /** The language of the voice. Default: `"Auto"`. */
  language?: "Auto" | "English" | "Chinese" | "Spanish" | "French" | "German" | "Italian" | "Japanese" | "Korean" | "Portuguese" | "Russian";
  /**
   * URL to a speaker embedding file in safetensors format, from
   * `fal-ai/qwen-3-tts/clone-voice` endpoint. If provided, the TTS model will use the cloned
   * voice for synthesis instead of the predefined voices. Carries a url reference — an https
   * URL or a `data:` URI.
   */
  speaker_voice_embedding_file_url?: string | null;
  /**
   * Optional reference text that was used when creating the speaker embedding. Providing
   * this can improve synthesis quality when using a cloned voice.
   */
  reference_text?: string | null;
  /** Top-k sampling parameter. Default: `50`. */
  top_k?: number | null;
  /** Top-p sampling parameter. Default: `1`. */
  top_p?: number | null;
  /** Sampling temperature; higher => more random. Default: `0.9`. */
  temperature?: number | null;
  /** Penalty to reduce repeated tokens/codes. Default: `1.05`. */
  repetition_penalty?: number | null;
  /** Sampling switch for the sub-talker. Default: `true`. */
  subtalker_dosample?: boolean | null;
  /** Top-k for sub-talker sampling. Default: `50`. */
  subtalker_top_k?: number | null;
  /** Top-p for sub-talker sampling. Default: `1`. */
  subtalker_top_p?: number | null;
  /** Temperature for sub-talker sampling. Default: `0.9`. */
  subtalker_temperature?: number | null;
  /** Maximum number of new codec tokens to generate. Default: `200`. */
  max_new_tokens?: number | null;
}

/**
 * The result document `fal-ai/qwen-3-tts/text-to-speech/1.7b` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface FalAiQwen3TtsTextToSpeech17bOutput {
  /** The generated speech audio file. */
  audio: FalAudioFile;
}

/**
 * The request body `xai/tts/v1` accepts.
 *
 * xAI TTS — a vendor-namespaced id documenting the internal route `/fal-ai/xai-tts/v1`.
 * Its `output_format` is an OBJECT ($ref OutputFormat with codec/sample_rate/bit_rate),
 * not a flat enum, so the row publishes an empty `codecs` and the canonical `outputFormat`
 * is refused by name.
 *
 * fal serves this endpoint at two live routes: the published id above, and the internal
 * alias `fal-ai/xai-tts/v1` its OpenAPI document is written against. unmodel submits to
 * the published id — that is the one fal documents and the one this catalog is keyed on.
 *
 * Docs: https://fal.ai/models/fal-ai/xai-tts/v1/api
 */
export interface XaiTtsV1Input {
  /**
   * The text to convert to speech. Maximum 15,000 characters. Supports speech tags for
   * expressive delivery: inline tags like [laugh], [pause], [sigh] and wrapping tags like
   * <whisper>text</whisper>, <slow>text</slow>.
   */
  text: string;
  /** Built-in xAI voice to use for synthesis. Default: `"eve"`. */
  voice?: "carina" | "zagan" | "helix" | "orion" | "luna" | "iris" | "altair" | "zenith" | "perseus" | "helios" | "lux" | "kepler" | "rigel" | "cosmo" | "celeste" | "ursa" | "sirius" | "lumen" | "castor" | "naksh" | "atlas" | "aurora" | "liora" | "ara" | "eve" | "leo" | "rex" | "sal";
  /**
   * BCP-47 language code or 'auto' for automatic detection. Supported: en, zh, fr, de, hi,
   * id, it, ja, ko, pt-BR, pt-PT, ru, es-MX, es-ES, tr, vi, bn, ar-EG, ar-SA, ar-AE.
   * Default: `"auto"`.
   */
  language?: "auto" | "en" | "ar-EG" | "ar-SA" | "ar-AE" | "bn" | "zh" | "fr" | "de" | "hi" | "id" | "it" | "ja" | "ko" | "pt-BR" | "pt-PT" | "ru" | "es-MX" | "es-ES" | "tr" | "vi";
  /** Output format configuration. Defaults to MP3 at 24 kHz / 128 kbps. */
  output_format?: FalOutputFormat;
}

/**
 * The result document `xai/tts/v1` produces.
 *
 * This is the body of the queue RESULT, fetched from the `response_url` a submit returns —
 * not the body of the submit response itself, which is the queue envelope.
 */
export interface XaiTtsV1Output {
  /** The generated audio file. */
  audio: FalFile;
}

/**
 * Endpoint id → its request body type.
 *
 * A map rather than a union: a union of 23 object types re-instantiates at every
 * comparison site, while a keyed lookup resolves in one. The per-endpoint narrowing a
 * validator exposes indexes into this, so adding an endpoint costs one line here and
 * nothing at any call site.
 */
export interface FalTtsBodyById {
  "fal-ai/bytedance/seed-speech/tts/v2": FalAiBytedanceSeedSpeechTtsV2Input;
  "fal-ai/chatterbox/text-to-speech": FalAiChatterboxTextToSpeechInput;
  "fal-ai/chatterbox/text-to-speech/multilingual": FalAiChatterboxTextToSpeechMultilingualInput;
  "fal-ai/elevenlabs/tts/eleven-v3": FalAiElevenlabsTtsElevenV3Input;
  "fal-ai/elevenlabs/tts/multilingual-v2": FalAiElevenlabsTtsMultilingualV2Input;
  "fal-ai/elevenlabs/tts/turbo-v2.5": FalAiElevenlabsTtsTurboV25Input;
  "fal-ai/gemini-3.1-flash-tts": FalAiGemini31FlashTtsInput;
  "fal-ai/gemini-tts": FalAiGeminiTtsInput;
  "fal-ai/inworld-tts": FalAiInworldTtsInput;
  "fal-ai/kokoro/american-english": FalAiKokoroAmericanEnglishInput;
  "fal-ai/kokoro/brazilian-portuguese": FalAiKokoroBrazilianPortugueseInput;
  "fal-ai/kokoro/british-english": FalAiKokoroBritishEnglishInput;
  "fal-ai/kokoro/french": FalAiKokoroFrenchInput;
  "fal-ai/kokoro/hindi": FalAiKokoroHindiInput;
  "fal-ai/kokoro/italian": FalAiKokoroItalianInput;
  "fal-ai/kokoro/japanese": FalAiKokoroJapaneseInput;
  "fal-ai/kokoro/mandarin-chinese": FalAiKokoroMandarinChineseInput;
  "fal-ai/kokoro/spanish": FalAiKokoroSpanishInput;
  "fal-ai/minimax/speech-02-hd": FalAiMinimaxSpeech02HdInput;
  "fal-ai/minimax/speech-2.8-hd": FalAiMinimaxSpeech28HdInput;
  "fal-ai/minimax/speech-2.8-turbo": FalAiMinimaxSpeech28TurboInput;
  "fal-ai/qwen-3-tts/text-to-speech/1.7b": FalAiQwen3TtsTextToSpeech17bInput;
  "xai/tts/v1": XaiTtsV1Input;
}

/** Endpoint id → its result document type. */
export interface FalTtsResultById {
  "fal-ai/bytedance/seed-speech/tts/v2": FalAiBytedanceSeedSpeechTtsV2Output;
  "fal-ai/chatterbox/text-to-speech": FalAiChatterboxTextToSpeechOutput;
  "fal-ai/chatterbox/text-to-speech/multilingual": FalAiChatterboxTextToSpeechMultilingualOutput;
  "fal-ai/elevenlabs/tts/eleven-v3": FalAiElevenlabsTtsElevenV3Output;
  "fal-ai/elevenlabs/tts/multilingual-v2": FalAiElevenlabsTtsMultilingualV2Output;
  "fal-ai/elevenlabs/tts/turbo-v2.5": FalAiElevenlabsTtsTurboV25Output;
  "fal-ai/gemini-3.1-flash-tts": FalAiGemini31FlashTtsOutput;
  "fal-ai/gemini-tts": FalAiGeminiTtsOutput;
  "fal-ai/inworld-tts": FalAiInworldTtsOutput;
  "fal-ai/kokoro/american-english": FalAiKokoroAmericanEnglishOutput;
  "fal-ai/kokoro/brazilian-portuguese": FalAiKokoroBrazilianPortugueseOutput;
  "fal-ai/kokoro/british-english": FalAiKokoroBritishEnglishOutput;
  "fal-ai/kokoro/french": FalAiKokoroFrenchOutput;
  "fal-ai/kokoro/hindi": FalAiKokoroHindiOutput;
  "fal-ai/kokoro/italian": FalAiKokoroItalianOutput;
  "fal-ai/kokoro/japanese": FalAiKokoroJapaneseOutput;
  "fal-ai/kokoro/mandarin-chinese": FalAiKokoroMandarinChineseOutput;
  "fal-ai/kokoro/spanish": FalAiKokoroSpanishOutput;
  "fal-ai/minimax/speech-02-hd": FalAiMinimaxSpeech02HdOutput;
  "fal-ai/minimax/speech-2.8-hd": FalAiMinimaxSpeech28HdOutput;
  "fal-ai/minimax/speech-2.8-turbo": FalAiMinimaxSpeech28TurboOutput;
  "fal-ai/qwen-3-tts/text-to-speech/1.7b": FalAiQwen3TtsTextToSpeech17bOutput;
  "xai/tts/v1": XaiTtsV1Output;
}
