// Generated by scripts/codegen-fal.ts — DO NOT EDIT.
// Source: https://api.fal.ai/v1/models?endpoint_id=<id>&expand=openapi-3.0
// Committed snapshots:
//   data/fal/openapi/blackforestlabs__flux-video-upscale.json
//   data/fal/openapi/fal-ai__aura-sr.json
//   data/fal/openapi/fal-ai__clarity-upscaler.json
//   data/fal/openapi/fal-ai__esrgan.json
//   data/fal/openapi/fal-ai__recraft__upscale__crisp.json
//   data/fal/openapi/fal-ai__seedvr__upscale__image.json
//   data/fal/openapi/fal-ai__seedvr__upscale__video.json
//   data/fal/openapi/topaz__upscale__image__generative.json
//   data/fal/openapi/topaz__upscale__image__precision.json
//   data/fal/openapi/topaz__upscale__video__generative.json
//   data/fal/openapi/topaz__upscale__video__precision.json
// Regenerate with `bun run codegen:fal` (or `bun run codegen:fal:refresh` to re-fetch the snapshots).

/**
 * The ONE request schema for every `fal.upscale` endpoint.
 *
 * One `z.looseObject` for the whole category, not 11 per-endpoint schemas: zod objects are
 * built eagerly, and a hundred of them would be constructed on import for the one the
 * caller actually used.
 *
 * Every field is optional here and every bound is absent. That is not laxity, it is the
 * division of labour — this schema is the SHAPE gate (is `prompt` a string?), and what
 * each endpoint requires, ranges and enumerates lives in `FAL_UPSCALE_SHAPES`, where the
 * hand check battery can turn a violation into a message that names the endpoint and cites
 * its own documentation.
 *
 * `looseObject` because fal ships new parameters between refreshes and a caller who reads
 * the release notes before we do should not be blocked; unknown keys are reported as
 * warnings by the hand battery instead.
 */

import { z } from "zod";

export const falUpscaleInputSchema = z.looseObject({
  /**
   * The endpoint to submit to — unmodel's route selector, not a fal body field.
   *
   * Stripped in `finalize` and interpolated into `.request.url`; fal never receives it. It
   * is declared here only so the pipeline does not report the one required parameter as an
   * unknown one. The selector is `endpoint` rather than `model` because `model` is a REAL
   * wire field on several fal endpoints.
   */
  endpoint: z.string(),
  H264_output: z.boolean().optional(),
  autoprompt: z.boolean().nullable().optional(),
  checkpoint: z.enum(["v1", "v2"]).optional(),
  compression: z.number().nullable().optional(),
  /**
   * Every `fal.upscale` endpoint types `creativity` as number, but they disagree on the
   * details (blackforestlabs/flux-video-upscale, fal-ai/clarity-upscaler,
   * topaz/upscale/image/generative), so the union takes the bare type and the exact
   * vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  creativity: z.number().nullable().optional(),
  crop_to_fill: z.boolean().optional(),
  denoise: z.number().nullable().optional(),
  detail: z.number().nullable().optional(),
  enable_safety_checker: z.boolean().optional(),
  enhancement_strength: z.enum(["low", "medium", "high"]).nullable().optional(),
  face: z.boolean().optional(),
  face_enhancement: z.boolean().optional(),
  face_enhancement_creativity: z.number().optional(),
  face_enhancement_strength: z.number().optional(),
  fix_compression: z.number().nullable().optional(),
  grain: z.number().nullable().optional(),
  guidance_scale: z.number().optional(),
  halo: z.number().nullable().optional(),
  /**
   * Every `fal.upscale` endpoint types `image_url` as string, but they disagree on the
   * details (fal-ai/aura-sr, fal-ai/clarity-upscaler, fal-ai/esrgan,
   * fal-ai/recraft/upscale/crisp, fal-ai/seedvr/upscale/image,
   * topaz/upscale/image/generative, topaz/upscale/image/precision), so the union takes the
   * bare type and the exact vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  image_url: z.string().optional(),
  /**
   * Every `fal.upscale` endpoint types `model` as string, but they disagree on the details
   * (fal-ai/esrgan, topaz/upscale/image/generative, topaz/upscale/image/precision,
   * topaz/upscale/video/generative, topaz/upscale/video/precision), so the union takes the
   * bare type and the exact vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  model: z.string().optional(),
  negative_prompt: z.string().optional(),
  noise: z.number().nullable().optional(),
  noise_scale: z.number().optional(),
  num_inference_steps: z.number().optional(),
  /**
   * Every `fal.upscale` endpoint types `output_format` as string, but they disagree on the
   * details (fal-ai/esrgan, fal-ai/seedvr/upscale/image, fal-ai/seedvr/upscale/video,
   * topaz/upscale/image/generative, topaz/upscale/image/precision), so the union takes the
   * bare type and the exact vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  output_format: z.string().optional(),
  output_quality: z.enum(["low", "medium", "high", "maximum"]).optional(),
  output_write_mode: z.enum(["fast", "balanced", "small"]).optional(),
  overlapping_tiles: z.boolean().optional(),
  /**
   * Every `fal.upscale` endpoint types `prompt` as string, but they disagree on the details
   * (blackforestlabs/flux-video-upscale, fal-ai/clarity-upscaler,
   * topaz/upscale/image/generative), so the union takes the bare type and the exact
   * vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  prompt: z.string().nullable().optional(),
  recover_detail: z.number().nullable().optional(),
  resemblance: z.number().optional(),
  safety_tolerance: z.number().optional(),
  scale: z.number().optional(),
  /**
   * Every `fal.upscale` endpoint types `seed` as number, but they disagree on the details
   * (fal-ai/clarity-upscaler, fal-ai/seedvr/upscale/image, fal-ai/seedvr/upscale/video), so
   * the union takes the bare type and the exact vocabulary is enforced per endpoint from
   * FAL_UPSCALE_SHAPES.
   */
  seed: z.number().nullable().optional(),
  sharpen: z.number().nullable().optional(),
  softness: z.number().nullable().optional(),
  strength: z.number().nullable().optional(),
  subject_detection: z.enum(["All", "Foreground", "Background"]).optional(),
  sync_mode: z.boolean().optional(),
  target_fps: z.number().nullable().optional(),
  target_resolution: z.enum(["720p", "1080p", "1440p", "2160p"]).optional(),
  texture: z.number().nullable().optional(),
  tile: z.number().optional(),
  /**
   * Every `fal.upscale` endpoint types `upscale_factor` as number, but they disagree on the
   * details (blackforestlabs/flux-video-upscale, fal-ai/aura-sr, fal-ai/clarity-upscaler,
   * fal-ai/seedvr/upscale/image, fal-ai/seedvr/upscale/video,
   * topaz/upscale/image/generative, topaz/upscale/image/precision,
   * topaz/upscale/video/generative, topaz/upscale/video/precision), so the union takes the
   * bare type and the exact vocabulary is enforced per endpoint from FAL_UPSCALE_SHAPES.
   */
  upscale_factor: z.number().optional(),
  upscale_mode: z.enum(["target", "factor"]).optional(),
  video_url: z.string().optional(),
});
