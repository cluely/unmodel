/**
 * The `fal.threeD` adapter's **data**: the endpoint roster and the per-model
 * narrowing table.
 *
 * The leaf rule (A10b in `test/import-graph.test.ts`) again: import-free apart
 * from the one generated table it re-exports, so `unmodel/fal/values` can
 * publish these rows without dragging zod or the unified kernel behind them.
 *
 * One field decides both of this category's content words. `inputs` says which
 * moods a route reads — `["text"]`, `["image"]`, or both at
 * `fal-ai/hyper3d/rodin/v2.5` — and `prompt` and `image` move in opposite
 * directions off it. `imageWire` says where the reference lands (four spellings
 * across seven vendors) and `imageWireList` says whether that field is an array.
 * `seedWire` picks the geometry seed at the four Tripo routes, which publish
 * three seeds each.
 */

export {
  FAL_THREE_D_PARAM_SHAPES as FAL_THREE_D_MODEL_PARAMS,
  FAL_THREE_D_MODELS as MODELS,
} from "./gen/three-d-params.gen";
